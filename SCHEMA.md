# SCHEMA.md — Public Schema Documentation

_This file is generated by `scripts/introspect_schema.py`. Run the script against the live database to populate it with real schema data._

> **Note:** This placeholder was committed before the script was executed against the live database.
> Run `python scripts/introspect_schema.py` to replace this file with the actual schema.

Row counts are **approximate** — sourced from `pg_class.reltuples`, not `COUNT(*)`.
