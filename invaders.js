// invaders.js — Invaders module stub.
// Full implementation is delivered by a sibling card.

/** Placeholder invader grid state. */
export const invaderState = {
  grid: [],
  direction: 1,
  speed: 30,
};

/** Update invaders (stub — no-op until sibling card implements it). */
export function updateInvaders(dt) {
  // TODO: implemented by the invaders sibling card.
}

/** Render invaders (stub — no-op until sibling card implements it). */
export function renderInvaders(ctx) {
  // TODO: implemented by the invaders sibling card.
}
