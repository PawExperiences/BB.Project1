package com.buildboard.calculator;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Placeholder test class that proves the Maven + JUnit 5 skeleton is wired
 * correctly. This test is intentionally trivial; real tests will arrive with
 * their respective implementation cards.
 */
class SkeletonTest {

    @Test
    void skeletonSmokeTest() {
        assertTrue(true, "Skeleton project is set up correctly.");
    }
}
