package com.buildboard.calculator;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/**
 * Placeholder test proving the Maven skeleton works end to end: Surefire
 * discovers this JUnit 5 (Jupiter) test and {@code mvn -B test} runs green
 * from a clean checkout. This scaffolding card's entire test deliverable is
 * a trivially-green test by design; the real tests-first cycle applies to
 * the sibling feature cards (e.g. EvaluatorTest) that build on this
 * skeleton.
 */
class SkeletonTest {

    @Test
    void skeletonIsInPlace() {
        assertTrue(true);
    }
}
