{
  "name": "badge-maker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "example": "npm run build && node examples/badge.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4"
  }
}
