[project]
name = "units"
version = "0.1.0"
description = "Convert between common length and mass units."
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
pythonpath = ["src"]
