// Space Invaders – skeleton entry point
// Game logic will be added in subsequent tasks.

const canvas = document.getElementById('gameCanvas');
const ctx = canvas.getContext('2d');

// Placeholder: fill canvas so it is visibly initialised.
ctx.fillStyle = '#000';
ctx.fillRect(0, 0, canvas.width, canvas.height);
