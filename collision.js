// collision.js — Collision detection module stub.
// Full implementation is delivered by the sibling card
// 'Sprite rendering and collision detection'.

/**
 * Basic axis-aligned bounding-box test (stub).
 * @param {object} a - { x, y, width, height }
 * @param {object} b - { x, y, width, height }
 * @returns {boolean}
 */
export function aabbIntersects(a, b) {
  // TODO: implemented by 'Sprite rendering and collision detection' card.
  return false;
}

/**
 * Check all bullet-vs-invader collisions (stub).
 * @returns {Array} list of collision results
 */
export function checkBulletInvaderCollisions(bullets, invaders) {
  // TODO: implemented by 'Sprite rendering and collision detection' card.
  return [];
}
