// collision.js — Collision detection stub
// Full implementation is owned by the 'Sprite rendering and collision
// detection' card.

/**
 * checkCollision — placeholder that always returns false.
 * Signature will be formalised in the collision card.
 *
 * @param {object} _a - First bounding-box object { x, y, width, height }
 * @param {object} _b - Second bounding-box object { x, y, width, height }
 * @returns {boolean}
 */
export function checkCollision(_a, _b) {
  return false;
}
