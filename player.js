// player.js — Player ship stub
// Full implementation is owned by the 'Keyboard input and the player ship' card.
// This stub ensures imports from other modules succeed without errors.

/**
 * Placeholder export — will be replaced by the real Player class / update
 * function in the next card.  Sibling modules that need to reference the
 * player can import this symbol and check for a no-op gracefully.
 */
export const player = null;
