// player.js — Player module stub.
// Full implementation is delivered by the sibling card
// 'Keyboard input and the player ship'.

/** Placeholder player state. */
export const playerState = {
  x: 0,
  y: 0,
  width: 48,
  height: 32,
  alive: true,
};

/** Update the player (stub — no-op until sibling card implements it). */
export function updatePlayer(dt, input) {
  // TODO: implemented by 'Keyboard input and the player ship' card.
}

/** Render the player (stub — no-op until sibling card implements it). */
export function renderPlayer(ctx) {
  // TODO: implemented by 'Keyboard input and the player ship' card.
}
