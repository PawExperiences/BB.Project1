// player.js — Player ship stub.
// Full implementation is owned by the 'Keyboard input and the player ship' card.

/**
 * Initialise the player entity.
 * @param {number} _canvasWidth
 * @param {number} _canvasHeight
 */
export function initPlayer(_canvasWidth, _canvasHeight) {
  // TODO: implement in the Player card
}

/**
 * Update the player for one fixed timestep.
 * @param {number} _dt  Delta time in seconds
 */
export function updatePlayer(_dt) {
  // TODO: implement in the Player card
}

/**
 * Render the player onto the canvas.
 * @param {CanvasRenderingContext2D} _ctx
 */
export function renderPlayer(_ctx) {
  // TODO: implement in the Player card
}
