# e2e prime tester 0.3.0 (0.3.0)

## e2e prime tester 0.3.0

This is the **inaugural release** of the `e2e prime tester` project. It ships a self-contained, dependency-free C++ console application (`prime_tester`) that offers two complementary paths for prime-number work: single-number trial-division testing via `is_prime()`, and bulk prime generation up to an arbitrary limit via a Sieve of Eratosthenes (`primes_up_to()`). The executable is built with CMake (C++17, no external libraries) and works on Linux, macOS, and Windows.

### Highlights
- **Argv & stdin modes**: test individual numbers as CLI arguments or pipe a stream of tokens; invalid/overflow tokens are reported to stderr without halting the run.
- **`--upto N` mode**: generate every prime up to N using the sieve; validated correct to 10⁸ (returns 5,761,455 primes, last element 99,999,989).
- **Zero dependencies**: pure C++17 standard library; `cmake -B build && cmake --build build` is all you need.
- **Hardened input handling**: `std::stoll` with overflow detection; non-integer tokens produce a clear error and exit code 1.
- **Complete documentation**: README with build + usage instructions, CHANGELOG, CONTRIBUTING guide, and RELEASING checklist included.
- **CI pipeline**: BuildBoard-managed `.github/workflows/build.yml` keeps the CMake build verified on every push.

## Highlights

- First release of `prime_tester`: a dependency-free C++17 console app built with CMake.
- `is_prime(long long)` — trial division with 6k±1 optimisation, correct for all edge cases including negatives and overflow.
- `primes_up_to(long long)` — Sieve of Eratosthenes, O(n log log n), verified correct to 10⁸.
- `--upto N` CLI mode prints all primes up to N; existing single-number mode unchanged.
- Hardened input parsing: overflow and non-integer tokens go to stderr; exit code 1 signals any parse error.
- CI, CHANGELOG, CONTRIBUTING, and RELEASING docs all land in this release.

## Changelog

# Changelog

## [0.3.0] - Initial Release

### Added
- **Prime Tester Console App** (`prime_tester` executable)
  - `src/prime.h`: Declares `bool is_prime(long long n);` with include guard.
  - `src/prime.cpp`: Implements trial-division primality test with 6k±1 optimisation; correct edge-cases for n < 2, n = 2, n = 3, and even n > 2.
  - `src/main.cpp`: Dual-mode entry point — argv mode (each CLI argument tested) and stdin mode (whitespace-delimited tokens read until EOF); invalid/overflow tokens reported to stderr with exit code 1.
  - `CMakeLists.txt`: Root-level CMake build file; `cmake_minimum_required(VERSION 3.16)`, project `prime_tester`, C++17, single executable target from `src/main.cpp` and `src/prime.cpp`.
  - `README.md`: Project description, build instructions, argv/stdin usage examples, edge-case notes.
- **Sieve of Eratosthenes — Range Primes + `--upto` Benchmark Mode**
  - `src/sieve.h`: Declares `std::vector<long long> primes_up_to(long long n);`.
  - `src/sieve.cpp`: Implements standard Sieve of Eratosthenes using `std::vector<bool>`; correct up to n = 10⁸ (5,761,455 primes); returns empty vector for n < 2.
  - `src/main.cpp` extended: `--upto N` mode calls `primes_up_to(N)` and prints each prime on its own line; missing/invalid N exits 1 with usage message.
  - `CMakeLists.txt` updated: `src/sieve.cpp` added to the `prime_tester` source list.
- **CI / Build workflow** (`.github/workflows/build.yml`): BuildBoard-managed CMake build workflow scaffolded/updated.
- **Repository documentation**: `CHANGELOG.md`, `CONTRIBUTING.md`, `RELEASING.md` created.
- **Release helper scripts**: `release/scripts/release.{py,sh,ps1}`, `release/scripts/run.{py,sh,ps1}`.

## Release steps

1. [manual] Confirm all bundled tasks are merged to main - Verify that commits a9d1c40 (Prime tester console app) and a3c4006 (Sieve + --upto) are reachable from the tip of the default branch (`main` or `master`) on https://github.com/PawExperiences/BB.Project1.git. Run: `git log --oneline origin/main | grep -E 'a9d1c40|a3c4006'`. If either is missing, stop and resolve before proceeding.
2. [automated] Pull latest default branch locally - git checkout main && git pull --ff-only origin main
3. [manual] Verify CI build workflow currency - Open `.github/workflows/build.yml` and confirm the build command is `cmake -B build && cmake --build build` and the artifact path is `build/prime_tester` (or `build/prime_tester.exe` on Windows). This workflow is owned and re-scaffolded by BuildBoard from the build resource's `command` + `artifact_path`. If the CMake invocation or output path changed, update the build resource config so BuildBoard re-scaffolds the workflow — do NOT hand-edit the workflow file. Confirm the current workflow matches what this release ships before tagging.
4. [automated] Run CMake build on clean checkout - rm -rf build && cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build --config Release
Expected output: executable at `build/prime_tester` (Linux/macOS) or `build/Release/prime_tester.exe` (Windows). Exit code must be 0.
5. [manual] Run acceptance-criteria smoke tests - Execute each of the following and confirm results:
  1. `./build/prime_tester 2 3 4` → stdout: `2 is prime`, `3 is prime`, `4 is not prime`; exit 0.
  2. `printf '17\n18\n' | ./build/prime_tester` → stdout: `17 is prime`, `18 is not prime`; exit 0.
  3. `./build/prime_tester abc` → stderr: `not a number: abc`; stdout: empty; exit 1.
  4. `./build/prime_tester 2 foo 5` → stdout: `2 is prime`, `5 is prime`; stderr: `not a number: foo`; exit 1.
  5. `./build/prime_tester --upto 20` → stdout: `2 3 5 7 11 13 17 19` (one per line); exit 0.
  6. `./build/prime_tester --upto 0` → no stdout; exit 0.
  7. `./build/prime_tester --upto` (no N) → stderr usage message; exit 1.
  8. `./build/prime_tester 99999999999999999999` → stderr: `not a number: 99999999999999999999`; exit 1.
6. [manual] Update CHANGELOG.md with 0.3.0 section - Prepend the changelog entry from this runbook to `CHANGELOG.md`. Commit with message: `docs: add 0.3.0 changelog entry`. Push to main (or open a PR if branch protection requires it).
7. [automated] Create and push the release tag - git tag -a v0.3.0 -m 'Release e2e prime tester 0.3.0'
git push origin v0.3.0
This is an additive action only — do NOT delete or move any existing tag.
8. [automated] Package release artifact - Run: `cmake --install build --prefix dist` (if an install target is defined) or manually copy `build/prime_tester` (Linux/macOS) / `build/Release/prime_tester.exe` (Windows) into a zip/tar archive named `prime_tester-0.3.0-<os>.tar.gz` (or `.zip` on Windows). The release script (`release/scripts/release.{py,sh,ps1}`) performs this step.
9. [automated] Create GitHub Release and upload artifact - Using the GitHub CLI (or the release script):
  gh release create v0.3.0 \
    --title 'e2e prime tester 0.3.0' \
    --notes-file <(echo '<releaseNotes markdown from this runbook>') \
    prime_tester-0.3.0-*.tar.gz
Confirm the release is published at https://github.com/PawExperiences/BB.Project1/releases/tag/v0.3.0.
10. [manual] Finalise docs/releases/0-3-0.md - Review `docs/releases/0-3-0.md` (already scaffolded in the diffstat). Update any placeholder text with the finalised release narrative and commit. Push to main.
11. [manual] Post-release verification - Download the published release asset from GitHub, extract it, and re-run at minimum smoke tests #1 and #5 from the acceptance step to confirm the distributed binary is functional.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Tags the repository as v0.3.0, builds the prime_tester executable with CMake, packages it into a platform-appropriate archive, and uploads it to the GitHub Release via the gh CLI. Run this after all smoke tests pass and CHANGELOG is updated.
- **release** `release/scripts/release.sh` (sh) -- Tags the repository as v0.3.0, builds the prime_tester executable with CMake, packages it into a .tar.gz archive, and uploads it to the GitHub Release via the gh CLI. Run this after all smoke tests pass and CHANGELOG is updated.
- **release** `release/scripts/release.ps1` (powershell) -- Tags the repository as v0.3.0, builds the prime_tester executable with CMake, packages it into a .zip archive, and uploads it to the GitHub Release via the gh CLI. Run this after all smoke tests pass and CHANGELOG is updated.
- **run** `release/scripts/run.py` (python) -- Builds prime_tester (if not already built) and then starts the executable in interactive stdin mode, forwarding all additional CLI arguments. Run this to manually test or demonstrate the application after a fresh checkout.
- **run** `release/scripts/run.sh` (sh) -- Builds prime_tester with CMake (if the executable is not already present) and then launches it, forwarding all arguments. Run this to manually test or demonstrate the application after a fresh checkout.
- **run** `release/scripts/run.ps1` (powershell) -- Builds prime_tester with CMake (if the executable is not already present) and then launches it, forwarding all arguments. Run this to manually test or demonstrate the application after a fresh checkout on Windows.

## Rollback

1. Because this is the first release (no previous tag exists), a rollback means removing the 0.3.0 tag and GitHub Release so that consumers cannot download the artifact — DO NOT delete the tag or release without a human authorising it (protected remote state rule).
2. If the tag has already been pushed, open a GitHub issue tracking the rollback request and assign it to a maintainer; do not force-push or delete the tag autonomously.
3. Communicate to any downstream consumers (via the issue or release notes) that 0.3.0 is being withdrawn and they should not use the artifact.
4. If the binary was distributed separately (e.g. via a release asset or package registry), follow the registry's deprecation/yanking procedure rather than deletion where possible.
5. Once a fixed version is ready, release it as 0.3.1 (patch) or 0.4.0 (minor) and link it from the rollback issue.

