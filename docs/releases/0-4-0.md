# e2e calculator cc 0.4.0 (0.4.0)

This would-be 0.4.0 release for "e2e calculator (claude code cli)" has no calculator content behind it. Every commit and every line of diff in the range provided belongs to a different, already-released project ("e2e space invaders" 0.5.0) that was deleted by a repository reset; zero tasks were bundled into this release. Before this can ship as a real 0.4.0, a human needs to confirm whether calculator commits exist elsewhere (a branch or range that wasn't included here) or whether this release request was simply raised too early, before any calculator work landed. This runbook documents that gap and provides the generic, idempotent tag/publish mechanics so the actual release can go out cleanly once real content is present — it does not claim any calculator feature was built, because none is evidenced.

## Highlights

- No calculator application commits or bundled tasks were found anywhere in the range supplied for this release.
- The supplied git history is entirely from an unrelated prior project ("e2e space invaders", already released as 0.5.0) being deleted via a full repository reset.
- README.md, CHANGELOG.md, and docs/releases/*.md were all removed by the reset commit and have not been recreated for the calculator project yet.
- A generic, idempotent tag-and-publish release script is provided so the release mechanics are ready the moment real calculator content lands.
- CI build-workflow currency cannot be confirmed this cycle — no toolchain/build command evidence exists yet for the calculator app.

## Changelog

## [0.4.0] - Pending verification

### Note
No commits or bundled tasks for the "e2e calculator" application were found in the supplied range. The git log/diffstat given for this release (2f42bad..ec76b45) contains only work from an unrelated prior project ("e2e space invaders", already shipped as its own 0.5.0) followed by a repository reset that deletes it (785 deletions, 0 additions, across CHANGELOG.md, README.md, docs/releases/0-5-0.md, game.js, gameConfig.js, index.html, input.js, player.js, and the old release/scripts/*). There is no calculator source, test, or doc file anywhere in this diff.

### Added
- Nothing calculator-related — no additions appear in the supplied diff.

### Changed
- Repository reset (`2f42bad chore: reset for the next e2e project`): removed the previous project's app files, README, CHANGELOG, docs, and release helper scripts (net -785 lines) in preparation for this project.

### Fixed
- Nothing to report — no calculator functionality exists yet in this range to have fixed anything.

## Release steps

1. [manual] Verify release scope matches the calculator project - The git log/diffstat supplied for this release contains only commits and deletions from an unrelated prior project ('e2e space invaders', shipped as 0.5.0), ending in a full repo reset (785 deletions, 0 additions). No calculator source, tests, docs, or bundled tasks are present anywhere in the range. A human must confirm: (a) whether calculator commits exist on another branch/range that were omitted from this bundle, or (b) this really is the still-empty starting state of the calculator project and v0.4.0 should not be tagged yet. Do not proceed to tagging/publishing until this is resolved.
2. [manual] Confirm CI build workflow matches this release's toolchain - Check .github/workflows/build.yml (owned and re-scaffolded by BuildBoard from the build resource's `command` + `artifact_path`) against the toolchain this release actually ships. No build command, language, or artifact path was supplied or changed in this task's inputs, so there is currently no evidence of what the calculator's build resource should be. As soon as calculator source lands with a real build command or artifact path, update the build resource config in BuildBoard (not the workflow file directly) so build.yml re-scaffolds to match — this is required before v0.4.0 ships so CI never drifts from the real build.
3. [manual] Recreate baseline docs removed by the reset - README.md, CHANGELOG.md, and docs/releases/0-5-0.md were deleted by commit 2f42bad and nothing has replaced them for the calculator project in this range. Author minimal versions (README with what the calculator is/how to run it, CHANGELOG seeded from this release) before publishing a public release page.
4. [automated] Tag and publish the release - Only once the scope-verification step above is resolved and real calculator content is present on the default branch: run `RELEASE_VERSION=0.4.0 python3 release/scripts/release.py` (or release.sh / release.ps1) to create annotated tag v0.4.0 at HEAD, push it to origin, and publish the GitHub release via the `gh` CLI using CHANGELOG.md as the notes body. Never run this against the current HEAD while step 1 is unresolved — it would tag and publish a release with no calculator content behind it.
5. [manual] Spot-check the published GitHub release - After running the release script, open https://github.com/PawExperiences/BB.Project1/releases/tag/v0.4.0 and confirm the tag, title, and notes rendered correctly and that no unintended leftover files from the prior project were included.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Idempotently creates annotated git tag v0.4.0 at HEAD, pushes it to origin (additive only, never force/rewrite), and publishes the GitHub release via the `gh` CLI (falls back to printing the manual `gh release create` command if `gh` isn't installed). Run this only after the scope-verification manual step confirms real calculator content is present.
- **release** `release/scripts/release.sh` (sh) -- POSIX-sh equivalent of release.py: idempotently tags HEAD as v0.4.0, pushes the tag to origin, and publishes the GitHub release via `gh` (prints the manual command if `gh` is missing). Run this only after the scope-verification manual step confirms real calculator content is present.
- **release** `release/scripts/release.ps1` (powershell) -- ASCII PowerShell 5.1-compatible equivalent of release.py: idempotently tags HEAD as v0.4.0, pushes the tag to origin, and publishes the GitHub release via `gh` (prints the manual command if `gh` is missing). Run this only after the scope-verification manual step confirms real calculator content is present.

## Rollback

Nothing functional is being shipped in this range, so there is nothing to roll back at the application level. If release/scripts/release.* is run prematurely and creates tag v0.4.0 / a GitHub release with no real content: do NOT delete the tag or release (remote deletions are forbidden) — instead edit the GitHub release in place to mark it superseded/pre-release, publish a corrective v0.4.0 (or next patch) once real calculator content lands, and note in the release body that the prior v0.4.0 publication was a process error with no functional payload.

