# e2e prime tester cc 0.6.0 (0.6.0)

# e2e prime tester cc 0.6.0

This is the first release of **prime_tester**, a small, dependency-free C++17 command-line tool for checking whether integers are prime. Feed it numbers as command-line arguments or pipe them one per line via stdin, and it reports `<n> is prime` / `<n> is not prime` for each; malformed tokens are reported on stderr without stopping the rest of the run. This release also adds a `--upto N` mode that enumerates every prime up to N using a Sieve of Eratosthenes, a CTest benchmark that tracks sieve performance at N ~ 10,000,000 over time, and a README you can use to build and manually verify the tool end-to-end without reading a line of source.

## Highlights
- Native, dependency-free C++17 CLI: a single `prime_tester` executable, no third-party libraries, no install step.
- Trial-division primality check (`is_prime`) with the 6k±1 optimisation, exposed as a reusable, independently testable function.
- New `--upto N` range mode backed by a Sieve of Eratosthenes for fast enumeration of every prime up to N.
- CTest benchmark timing the sieve at N ~ 10,000,000 (informational only, no pass/fail gate).
- README **Build** and **Manual Verification** sections with a copy-pasteable, 8-scenario command / output / exit-status table.

## Quick start
```
cmake -B build
cmake --build build
./build/prime_tester 17
./build/prime_tester --upto 30
```


## Highlights

- First release of prime_tester: a native, dependency-free C++17 CLI for primality testing.
- Trial-division `is_prime(long long n)` with the 6k±1 optimisation, usable from argv or stdin.
- New `--upto N` sieve mode (Sieve of Eratosthenes) for fast range enumeration of primes.
- CTest benchmark tracks sieve performance at N ~ 10,000,000 as the codebase evolves.
- README now has copy-pasteable Build and Manual Verification (8-scenario) sections.

## Changelog

## 0.6.0 - 2026-08-11

### Added
- `prime_tester` CLI (native C++17, CMake-built, zero third-party dependencies): reports primality for integers passed as command-line arguments or read one-per-line from stdin, printing `<n> is prime` / `<n> is not prime` for each input in order (`src/main.cpp`).
- Reusable primality core `bool is_prime(long long n)` (`src/prime.h`, `src/prime.cpp`) using trial division to `sqrt(n)` with the 6k±1 optimisation. `is_prime` is false for every n < 2, true for 2 and 3, false for even n > 2.
- Malformed-input handling: a token that isn't a parseable `long long` prints exactly `not a number: <token>` to stderr, processing continues for the remaining tokens/lines, and the process exits 1 if any token was malformed (0 if every token was valid; 0 on empty stdin).
- `--upto N` range mode backed by a new Sieve of Eratosthenes, `std::vector<long long> primes_up_to(long long n)` (`src/sieve.h`, `src/sieve.cpp`): prints every prime from 2 up to N inclusive, one per line, ascending; prints nothing and exits 0 for N < 2. The existing single-number trial-division path is unchanged.
- CTest-registered sieve benchmark (`src/bench_sieve.cpp`) timing `primes_up_to` at N ~ 10,000,000 and reporting elapsed time as informational output only (no pass/fail threshold).
- `CMakeLists.txt`: CMake >= 3.16, C++17, a single `prime_tester` executable target, no third-party dependencies, no install() step.
- `README.md`: new **Build** section with the exact configure/build commands and where to find/run the executable, and a new **Manual Verification** section with a Command / Expected stdout / Expected exit status table covering all 8 core scenarios (prime, composite, 0, 1, negative, non-numeric token, empty stdin, `--upto 30`).


## Release steps

1. [manual] Confirm CI is green on the release commit - In GitHub Actions, confirm the `build.yml` workflow run for commit c3b6d83 (HEAD; merges PR #219 'Manual verification steps in the README') on the default branch succeeded, including the CTest run (unit checks plus the informational sieve benchmark).
2. [manual] Confirm CI build workflow currency for this release's toolchain - This is the project's first release, establishing its toolchain. In the BuildBoard project settings, confirm the build resource's `command` is `cmake -B build && cmake --build build` (optionally followed by `ctest --output-on-failure`) and its `artifact_path` is `build/prime_tester`. BuildBoard re-scaffolds `.github/workflows/build.yml` from these two values on every build — if either doesn't match what this release actually ships, update the build resource config now (not the workflow file) so CI stays in sync with the real build.
3. [automated] Run the automated release build, smoke test, tag, and publish - From a clean checkout of c3b6d83 on the default branch, run `python3 release/scripts/release.py` (or `sh release/scripts/release.sh` / `pwsh release/scripts/release.ps1`, with `gh auth login` already done). It runs `cmake -B build && cmake --build build`, runs `ctest --output-on-failure`, smoke-tests the `build/prime_tester` binary against all 8 README Manual Verification scenarios, creates and pushes annotated tag `v0.6.0` (skipping if it already exists), and — if the `gh` CLI is available — publishes a GitHub release for `v0.6.0` with the binary attached (skipping if the release already exists).
4. [manual] Verify the published GitHub release - Open the `v0.6.0` release page on GitHub. Confirm the release body matches `releaseNotes`, the `prime_tester` binary is attached as a downloadable asset, and the tag points at commit c3b6d83.
5. [manual] Announce availability - Post the release link and the `highlights` bullets in the project's release channel. No further deployment/migration action is required — this is a standalone CLI with no server component and no data to migrate.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Configures and builds the CMake project (`cmake -B build && cmake --build build`), runs `ctest`, smoke-tests `build/prime_tester` against the 8 README Manual Verification scenarios (exact expected stdout/stderr/exit-status per scenario), then creates+pushes the annotated tag `v0.6.0` and publishes a GitHub release with the binary attached via `gh` if available. Run this once from a clean checkout of the release commit, after CI is green, to perform the release end-to-end; safe to re-run (skips tag/release creation if they already exist).
- **release** `release/scripts/release.sh` (sh) -- POSIX-sh equivalent of release.py: builds via `cmake -B build && cmake --build build`, runs `ctest`, smoke-tests `build/prime_tester` against the 8 README Manual Verification scenarios, then creates+pushes tag `v0.6.0` and publishes a GitHub release with `gh` if available. Run this once from a clean checkout of the release commit, after CI is green, to perform the release end-to-end; safe to re-run.
- **release** `release/scripts/release.ps1` (powershell) -- ASCII PowerShell (5.1-compatible) equivalent of release.py: builds via `cmake -B build` / `cmake --build build`, runs `ctest`, smoke-tests `build\prime_tester.exe` (or `build/prime_tester`) against the 8 README Manual Verification scenarios, then creates+pushes tag `v0.6.0` and publishes a GitHub release with `gh` if available. Run this once from a clean checkout of the release commit, after CI is green, to perform the release end-to-end; safe to re-run.
- **run** `release/scripts/run.py` (python) -- Builds `build/prime_tester` first only if it doesn't already exist, then execs it, forwarding every argument/stdin unchanged. Use this any time you want to invoke the CLI (e.g. `python3 release/scripts/run.py 17` or `python3 release/scripts/run.py --upto 30`) without remembering the exact CMake build path.
- **run** `release/scripts/run.sh` (sh) -- POSIX-sh equivalent of run.py: builds `build/prime_tester` first only if it doesn't already exist, then execs it, forwarding every argument/stdin unchanged. Use this any time you want to invoke the CLI (e.g. `sh release/scripts/run.sh 17` or `sh release/scripts/run.sh --upto 30`).
- **run** `release/scripts/run.ps1` (powershell) -- ASCII PowerShell (5.1-compatible) equivalent of run.py: builds `build\prime_tester.exe` (or `build/prime_tester`) first only if it doesn't already exist, then runs it, forwarding every argument unchanged. Use this any time you want to invoke the CLI (e.g. `pwsh release/scripts/run.ps1 17` or `pwsh release/scripts/run.ps1 --upto 30`).

## Rollback

This is the first tagged release (previous version: none), so there is no prior version to roll back to. If a defect is found after publishing v0.6.0: (1) Do not delete or unpublish the v0.6.0 GitHub release or the v0.6.0 tag — removing remote state is forbidden. (2) If needed, edit the existing v0.6.0 release body in place (additive edit only) to add a known-issues note pointing users at the fix in progress. (3) Cut a follow-up patch release (e.g. v0.6.1) with the fix using the same release/scripts/release.* script once the fix is merged, and publish it as the recommended version. (4) Consumers building from source who need to avoid the defect can `git checkout a5baa6c` (the pre-project baseline commit, prior to the prime_tester feature commits) until v0.6.1 is available. (5) No data or schema migrations exist for this release, so no migration rollback is required.

