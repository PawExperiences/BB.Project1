# e2e calculator 0.2.0 (0.2.0)

## Calculator 0.2.0 — first release

The BuildBoard Calculator is a small desktop calculator for Java 21: a Swing window backed by a fully unit-tested, UI-free expression engine, shipped as one zero-dependency JAR. Build it with `mvn -B package`, run it with `java -jar`, and click or type expressions with proper operator precedence, parentheses, decimals and friendly inline error messages.

### Highlights
- Swing calculator window with an 18-button grid, full keyboard support (ENTER, ESCAPE and BACKSPACE included) and smart continue-or-restart behaviour after each result.
- Test-first expression core: `+ - * /` with correct precedence, nested parentheses, decimal literals and unary minus in both positions.
- Failures are never fatal: division by zero and malformed input surface as readable messages via a checked `CalculationException`, and every result is a finite number.
- One self-contained artifact: `mvn -B package` produces `target/calculator-0.1.0.jar` — no runtime dependencies, no installer.
- Headless-safe test suite: UI tests skip gracefully without a display, so CI stays green everywhere.

### Build and run
- Requirements: JDK 21 to build; a Java 21 runtime and a graphical display to run.
- Build: `mvn -B package`
- Run: `java -jar target/calculator-0.1.0.jar`

### Compatibility
First release — no breaking changes and no migrations.

> Artifact naming note: the groomed task specs pin the Maven version to `0.1.0`, so the JAR is `calculator-0.1.0.jar` while this release is tagged `v0.2.0`. See the runbook step "Decide Maven version vs release version": either bump the Maven version before tagging or ship the 0.1.0-named artifact knowingly.

## Highlights

- Desktop calculator with a Swing UI: 18-button grid, full keyboard support, inline error messages and standard continue-after-result behaviour.
- Expression engine built test-first: + - * / with real operator precedence, nested parentheses, decimals and unary minus in both positions.
- Errors never crash the app: division by zero and malformed input show readable messages via a checked CalculationException; every result is finite.
- Zero-dependency runnable JAR: `mvn -B package` then `java -jar target/calculator-0.1.0.jar` on any Java 21 machine with a display.
- Headless-friendly JUnit 5 suite: UI tests skip gracefully without a display, keeping CI green.

## Changelog

# Changelog — e2e calculator 0.2.0

First release of the Calculator (Java Swing) project. There is no previous release to diff against; everything below is new.

## Added
- Maven build foundation: `pom.xml` (groupId `com.buildboard`, artifactId `calculator`, packaging `jar`) targeting Java 21 via maven-compiler-plugin `<release>21</release>`, JUnit 5 (`org.junit.jupiter:junit-jupiter:5.10.2`, test scope) and maven-surefire-plugin 3.2.5, so `mvn -B test` runs green from a clean checkout. (#377)
- `SkeletonTest` smoke test plus a `README.md` documenting the planned project layout. (#377)
- Expression evaluation core `com.buildboard.calculator.Evaluator` — a UI-free class with a single static `evaluate(String)` API supporting `+ - * /` with correct precedence and left-associativity, nested parentheses, decimal literals, unary minus in leading and post-operator positions, and insignificant whitespace. (#378)
- Checked `com.buildboard.calculator.CalculationException` for every failure mode: division by zero and malformed input (unbalanced parentheses, empty/blank input) throw with descriptive, problem-naming messages; the evaluator never returns NaN, Infinity or a sentinel value. (#378)
- `EvaluatorTest` JUnit 5 suite written test-first (red/green), covering precedence, nested parentheses, decimals, both unary-minus positions, division by zero, unbalanced parentheses and empty input. (#378)
- Swing UI `CalculatorWindow`: title "Calculator", 320x420 initial and minimum size, a right-aligned non-editable display, and a GridBagLayout grid of 18 buttons (digits 0-9, `.`, `+ - * /`, `( )`, `C`, `=`). (#379)
- Result formatting with up to 10 significant digits and trailing zeros stripped (`1+2` -> `3`, `10/4` -> `2.5`, `2/3` -> `0.6666666667`); all arithmetic stays in `Evaluator`. (#379)
- Inline error handling: a `CalculationException` message is shown in the display and the next input clears it and starts a fresh expression; after a successful result a digit/`.`/`(` starts fresh while an operator continues from the result. (#379)
- Full keyboard support independent of focused component: printable keys append, ENTER evaluates, ESCAPE clears, BACKSPACE edits an in-progress expression. (#379)
- Headless-safe `CalculatorWindowTest` suite that skips frame-instantiating tests when no display is present. (#379)
- `Main` launcher that opens the window on the Swing EDT via `SwingUtilities.invokeLater`, plus maven-jar-plugin config stamping `Main-Class: com.buildboard.calculator.Main` into the manifest — `java -jar target/calculator-0.1.0.jar` runs the app with zero runtime dependencies. (#380)
- `MainTest` entry-point test and a `README.md` documenting the exact build (`mvn -B package`) and run (`java -jar target/calculator-0.1.0.jar`) commands. (#380)

## Changed
- Repository housekeeping: leftover files from earlier, unrelated e2e projects (JS game sources, C++ prime-tester sources, old release scripts and docs) were removed from the tree during this release window. They were never part of the calculator product.

## Fixed
- Nothing — first release, no prior defects to fix.

## Release steps

1. [manual] Verify the release tree matches the calculator scope - The diffstat supplied for this window is dominated by repo-reset deletions (it even lists the calculator sources as removed). Before anything else, inspect the intended tag commit (b0688be 'feat: Package as a runnable JAR (#380)' or newer) and confirm the tree contains pom.xml, src/main/java/com/buildboard/calculator/{Evaluator,CalculationException,CalculatorWindow,Main}.java, src/test/java/com/buildboard/calculator/{SkeletonTest,EvaluatorTest,CalculatorWindowTest,MainTest}.java and README.md. Do not tag a tree that fails this check.
2. [manual] Decide Maven version vs release version (0.1.0 vs 0.2.0) - The groomed acceptance criteria pin pom.xml <version>0.1.0</version> and target/calculator-0.1.0.jar, but this release is 0.2.0. Human decision: either merge a small PR bumping <version> to 0.2.0 (and the README run command), or ship calculator-0.1.0.jar under tag v0.2.0 with the mismatch noted in the release notes. The helper scripts auto-detect target/calculator-*.jar so they work either way.
3. [manual] Commit release documentation - On a release branch (e.g. chore/release-0-2-0), add the 0.2.0 entry to CHANGELOG.md and create docs/releases/0-2-0.md containing this runbook's releaseNotes; include the version bump from the previous step if chosen. Merge to main — the tag must point at a commit containing these files.
4. [manual] REQUIRED: confirm BuildBoard CI matches the Maven toolchain this release ships - This window replaced earlier e2e leftovers (C++/CMake, JS game) with a Java 21 + Maven build, so CI drift is likely. In the BuildBoard build resource, confirm: toolchain JDK 21, build command 'mvn -B package', artifact path matching the produced JAR ('target/calculator-0.1.0.jar', or 'target/calculator-0.2.0.jar' if the version was bumped). BuildBoard owns .github/workflows/build.yml and re-scaffolds it from that config on every build — if the resource still carries an older command or toolchain, updating the build resource config is REQUIRED before tagging; never hand-edit the workflow. The log shows 'ci: update BuildBoard build workflow' (3472e4f) but it predates the calculator cards, so verify rather than assume.
5. [automated] Clean build with the full test suite - From a clean checkout on JDK 21 run: mvn -B clean package. Expect BUILD SUCCESS with the whole JUnit 5 suite green (SkeletonTest, EvaluatorTest, CalculatorWindowTest with UI tests skipping headless, MainTest) and exactly one target/calculator-*.jar produced.
6. [automated] Verify the JAR manifest entry point - Confirm the manifest contains 'Main-Class: com.buildboard.calculator.Main': unzip -p target/calculator-*.jar META-INF/MANIFEST.MF | grep 'Main-Class'. Automated inside release/scripts/release.{py,sh,ps1}.
7. [manual] Manual UI smoke test on a machine with a display - Run java -jar target/calculator-*.jar (or release/scripts/run.{py,sh,ps1}). Verify: window titled 'Calculator' at 320x420 minimum; 1+2= -> 3; 10/4= -> 2.5; 2/3= -> 0.6666666667; 1/0= -> inline error message that clears on next input; a digit after a result restarts while an operator continues; keyboard mapping works regardless of focus (printable keys append, ENTER = equals, ESCAPE = clear, BACKSPACE edits an in-progress expression).
8. [automated] Tag v0.2.0 and push (additive only) - git tag -a v0.2.0 -m 'e2e calculator 0.2.0' <commit-from-the-docs-step> && git push origin v0.2.0. If the tag already exists, leave it untouched and investigate — never move, retag or force-push. Automated, idempotently, in release/scripts/release.{py,sh,ps1}.
9. [automated] Create the GitHub release and attach the JAR - gh release create v0.2.0 target/calculator-*.jar --title 'e2e calculator 0.2.0' --notes-file docs/releases/0-2-0.md. Running release/scripts/release.{py,sh,ps1} performs the clean build, manifest check, tagging and this publish step end-to-end and idempotently; if the gh CLI is unavailable it prints the exact manual command instead.
10. [manual] Post-publish verification and close-out - From a clean machine, download the asset from the GitHub release page and confirm java -jar launches the window without errors; confirm the release notes and asset render correctly; confirm the bundled tickets are in Done and the audit/CHANGELOG record matches what shipped.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Builds the calculator JAR (mvn -B clean package), verifies its Main-Class manifest entry, creates and pushes tag v0.2.0 only if missing, and creates the GitHub release with the JAR attached (via the gh CLI when present, else prints the exact command) — run once from the repo root after the release tree and version decision are confirmed; idempotent and never deletes remote state.
- **release** `release/scripts/release.sh` (sh) -- Builds the calculator JAR (mvn -B clean package), verifies its Main-Class manifest entry (requires unzip), creates and pushes tag v0.2.0 only if missing, and creates the GitHub release with the JAR attached via the gh CLI when present — run once from the repo root after the release tree and version decision are confirmed; idempotent and never deletes remote state.
- **release** `release/scripts/release.ps1` (powershell) -- Builds the calculator JAR (mvn -B clean package), verifies its Main-Class manifest entry using .NET zip APIs, creates and pushes tag v0.2.0 only if missing, and creates the GitHub release with the JAR attached via the gh CLI when present — run once from the repo root after the release tree and version decision are confirmed; idempotent and never deletes remote state.
- **run** `release/scripts/run.py` (python) -- Launches the built calculator JAR with java -jar, building it first via mvn -B package if no JAR exists — use for local runs and the pre/post-release smoke tests; requires a Java 21 runtime and a graphical display.
- **run** `release/scripts/run.sh` (sh) -- Launches the built calculator JAR with java -jar, building it first via mvn -B package if no JAR exists — use for local runs and the pre/post-release smoke tests; requires a Java 21 runtime and a graphical display.
- **run** `release/scripts/run.ps1` (powershell) -- Launches the built calculator JAR with java -jar, building it first via mvn -B package if no JAR exists — use for local runs and the pre/post-release smoke tests; requires a Java 21 runtime and a graphical display.

## Rollback

0.2.0 is the first release of a standalone desktop JAR with no server-side state, no database and no downstream consumers, so rollback is low-risk. (1) Stop distribution: in GitHub, edit the v0.2.0 release and mark it as a pre-release (or convert it to a draft) so it no longer appears as 'latest' — do not delete the tag, the release or any history; destructive remote actions are outside the releaser's authority and require explicit human approval. (2) Remove or correct any announcements/links pointing at the asset. (3) Users who already downloaded the JAR simply delete their local copy; nothing is installed and nothing persists. (4) Land any fix as a normal commit on main and ship it as v0.2.1 (bumping the Maven version too, if the runbook's version-alignment step aligned them) rather than moving or rewriting the v0.2.0 tag. (5) Only if the release must be fully withdrawn may a human with admin rights remove the GitHub release and tag, after explicitly approving that destructive action.

