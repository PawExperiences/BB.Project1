# e2e space invaders cc 0.5.0 (0.5.0)

# e2e space invaders cc 0.5.0

The first release of **e2e space invaders (claude code cli)** -- a complete, dependency-free Space Invaders clone that runs by opening `index.html` straight from disk: no server, no build step, no npm install. Seven cards (PRs #349-#355) built the game loop, player controls, the invader/collision core, three escalating levels, and a four-phase boss finale on top of nothing but plain ES modules and Canvas 2D.

## Highlights
- Fixed-timestep 60Hz game loop with a Title / Playing / Game Over scene machine and an on-canvas HUD (score, lives, hi-score).
- Responsive player ship with clamped movement, single-shot bullets, and a lives system.
- Classic 11x5 invader formation with AABB collision detection, kill effects, and scoring.
- **Level 1**: the marching grid that speeds up as it thins out.
- **Level 2**: invaders shoot back, plus a tiered-score bonus UFO.
- **Level 3**: four destructible shield bunkers and a mid-fight formation split.
- **Level 4**: a two-phase boss with a spread attack, sudden death, and a win/lose flow.

## Known issue -- please read before playing
Clearing Level 2 does not currently advance to Level 3: a wiring gap in `level2.js` means it never signals "level cleared" back to the game loop, so **Level 3 and the boss fight are not reachable in this build**. This is a known, tracked issue (see `README.md`'s "Known pre-existing gap" note) and is being scheduled as a fix for a follow-up patch release.

## Getting the game
Download the `e2e-space-invaders-cc-0.5.0.zip` asset below, unzip it, and open `index.html` in any modern browser -- no install required.

## Highlights

- Fixed-timestep 60Hz game loop with a Title / Playing / Game Over scene machine and an on-canvas HUD (score, lives, hi-score).
- Player ship with clamped movement, single-shot bullets, and a lives system (input.js / player.js).
- Classic 11x5 invader formation with AABB collision detection, kill effects, and scoring (invaders.js / collision.js).
- Three escalating levels: marching-grid Level 1, return-fire-and-bonus-UFO Level 2, and shield-bunkers-with-formation-split Level 3.
- Two-phase boss finale (Level 4) with a spread attack, sudden death, and a win/lose/restart flow.
- KNOWN ISSUE: Level 2 does not currently hand off to Level 3 (level2.js never reports 'cleared'), so Level 3 and the boss are unreachable in this build -- see Known Issues.

## Changelog

# Changelog

## [0.5.0] - 2026-08-15

First public release of **e2e space invaders (claude code cli)** -- a dependency-free, file://-only Space Invaders clone (plain HTML + ES modules, no bundler, no npm packages, no build step). Built from 7 feature cards merged as PRs #349-#355.

### Added
- **Game loop and canvas framework** (`index.html`, `game.js`, `gameConfig.js` -- PR #349): 768x896 dark-background canvas; fixed-timestep 60Hz update/render loop with a clamped accumulator (bounded catch-up after a backgrounded tab, no spiral-of-death); Title / Playing / GameOver scene machine driven entirely by ENTER with no page reload; on-canvas HUD backed by an exported `{ score, lives, hiScore }` state object.
- **Keyboard input and the player ship** (`input.js`, `player.js` -- PR #350): held-key tracking via keydown/keyup that ignores OS key-repeat; ship movement at 200 px/s (Arrows or A/D, case-insensitive) clamped to the canvas edges; single-bullet-in-flight upward fire at 500 px/s; lives counter seeded from `STARTING_LIVES` (3).
- **Sprite rendering and collision detection** (`invaders.js`, `collision.js` -- PR #351): 11x5 (55-invader) rigid formation with edge-bounce-and-drop movement, all rectangles drawn with `fillRect`; AABB collision pass (player-bullet-vs-invader, invader-bullet-vs-player) resolved before each frame's render; kill explosion FX and a +10 score increment per kill.
- **Level 1: the classic grid** (`level1.js` -- PR #352): linear step-interval speed ramp (~800ms at 55 alive down to ~100ms at 1 alive), recalculated on every kill; life loss + full formation restart if invaders reach the player's row; HUD level indicator; wired as `game.js`'s level 1, falling through to Game Over for level 2 until the next card replaced that branch.
- **Level 2: they shoot back** (`level2.js` -- PR #353): auto-advance from Level 1 with no level-select screen and lives carried over unchanged; formation 1.5x faster (every Level 1 step interval x0.67); a single global randomized (800-2000ms) invader-fire timer that only lets the lowest surviving invader in a column shoot, bullets falling at 300 px/s; a bonus UFO every 20s of level time, alternating entry side, scoring a deterministic tier `[50,100,150,300][cumulativeShotsFired % 4]`; player respawn at the fixed bottom-center position with 2s of flashing invulnerability on hit.
- **Level 3: shields and formations** (`level3.js` -- PR #354): four destructible 4x4 shield bunkers (~8px cells, 64 cells total) at ~80% canvas height, eroding per-cell from either side's projectiles or invader contact; once >=28 of the 55 starting invaders are destroyed, the formation splits into independently sweeping left (orig. columns 1-6) and right (orig. columns 7-11) groups moving outward.
- **Boss level: multi-phase finale** (`boss.js` -- PR #355): 160x80 boss with 10 HP and a top-of-canvas health bar; horizontal drift at 90 px/s reversing at the canvas edges, fixed vertical position; three-bullet spread attack (straight down, +/-20deg) at 260 px/s, firing every 1500ms above 5 HP (Phase 1) and every 700ms at or below 5 HP (Phase 2); any boss bullet touching the player is sudden death; 0 HP triggers a win screen; both outcomes restart at Level 1 with score reset to 0.

### Changed
- `game.js`'s level dispatcher was audited and extended (PR #355) into one ordered chain -- `level1Active -> level2Active -> level3Active -> bossActive` -- each level signaling completion via its own `update()` return value (`'cleared'` / `'playerHit'` / `'victory'`), so Level 4 could be registered alongside Levels 1-3 using the same pattern.

### Known Issues
- **Level 2 -> Level 3 progression is broken.** `level2.js`'s `update()` never returns `'cleared'`, so `game.js` can never flip from `level2Active` to `level3Active`. **In this build, Level 3 and the Level 4 boss fight cannot be reached by playing through from Level 1** -- the game currently plateaus at Level 2 regardless of score or invaders destroyed. This is self-documented in the shipped `README.md` ("Known pre-existing gap...") and was explicitly left unfixed by PR #355 as "outside this card's scope." See the release runbook's go/no-go step before shipping.

### Notes for reviewers
- The git diffstat supplied for generating this release (12 files changed, 2 insertions(+), 2317 deletions(-), every game file shown as pure deletion) is inconsistent with the actual repository contents at HEAD, which contain the full ~1,800-line implementation described above. This changelog was instead verified directly against the project's stored build artifact for this release (outbound git/network access to the live repo was unavailable in this environment). Recommend a human re-run `git diff <initial-commit>..db2b172 --stat` to confirm the diffstat-generation step in the release pipeline is healthy; if it's inverted for this release it may be inverted for others.

## Release steps

1. [automated] Confirm the release scope is fully merged and the tree is clean - On `main`, run `git status --porcelain` (expect empty output) and `git log --oneline -9` to confirm all 7 feature PRs (#349-#355, commits cd07beb..db2b172) plus the two prior `chore: reset` commits are present and `main`'s HEAD is `db2b172`. No local uncommitted changes are carried into the release.
2. [automated] Verify the CI build workflow matches the shipped toolchain - Confirm `.github/workflows/build.yml` (BuildBoard-owned, re-scaffolded from the `toolchain-node-nobuild` resource) still runs the configured command `echo 'no build step: open index.html'` inside `node:20-bookworm` and publishes `artifact_path: .`. This release does not change the command, image, or artifact path (still a plain, dependency-free static site opened via file://), so no edit to the toolchain resource is required for 0.5.0 -- but re-verify on every release per house rule so CI can never silently drift from what is actually shipped. If a future release changes the run command or output location, that change must go through the toolchain resource config (never a hand-edit of build.yml) so the workflow re-scaffolds correctly.
3. [manual] GO/NO-GO: review the confirmed Level 2 -> Level 3 progression bug - Human decision required. level2.js's update() never returns 'cleared', so game.js can never advance past Level 2 in normal play -- Level 3 and the boss (Level 4) are unreachable in this build (self-documented in README.md). Decide: (a) ship 0.5.0 as-is with this called out in the release notes and a fast-follow patch planned, or (b) hold the release and route a fix for level2.js back through Grooming/In Progress first. The remaining steps in this runbook assume (a) unless told otherwise.
4. [automated] Confirm the release commit's build is green - `gh run list --workflow build.yml --branch main --limit 1 --json headSha,conclusion` and confirm headSha is db2b172 (or its merge commit) with conclusion success, before tagging.
5. [automated] Tag the release - From a clean `main` checkout at commit db2b172, create annotated tag `0.5.0` (the delivery resource's tag_template is `{version}`, no 'v' prefix). Performed by release/scripts/release.{py,sh,ps1}.
6. [automated] Push the tag to origin - `git push origin 0.5.0` -- additive only, no force-push, no branch deletion. Performed by the same release script.
7. [automated] Package the release artifact - Zip the 12 shipped files (index.html, game.js, gameConfig.js, input.js, player.js, invaders.js, collision.js, level1.js, level2.js, level3.js, boss.js, README.md) into e2e-space-invaders-cc-0.5.0.zip, since artifact_path is '.' (the whole no-build source tree). Performed by the release script.
8. [automated] Publish the GitHub Release - `gh release create 0.5.0 e2e-space-invaders-cc-0.5.0.zip --title "e2e space invaders cc 0.5.0" --notes-file release/RELEASE_NOTES.md` against https://github.com/PawExperiences/BB.Project1 (the bound delivery-github-release resource). Idempotent: if the release already exists, the script uploads/overwrites only the asset via `--clobber`.
9. [automated] Verify the published release - `gh release view 0.5.0` to confirm the tag, title, and notes are live; download the uploaded zip and diff its file listing against the expected 12 files to confirm the asset is complete and not truncated.
10. [manual] File a follow-up card for the Level 2 progression gap - Open a card back on the board (Inbox or Grooming) titled e.g. 'Fix level2.js: report level-clear so Level 3/Boss are reachable', referencing this release and README.md's 'Known pre-existing gap' note, so it is tracked for a 0.5.1 patch rather than only living in this runbook.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Tags the repo 0.5.0, pushes the tag, zips the 12 shipped files into e2e-space-invaders-cc-0.5.0.zip, and publishes/updates the GitHub Release with that asset and release/RELEASE_NOTES.md as the body. Run once from the repo root after the CI-green check and the Level 2/3 go/no-go decision; safe to re-run (idempotent: skips an existing tag, re-uploads the asset with --clobber if the release already exists). Requires: git, the gh CLI authenticated (GITHUB_TOKEN env var or `gh auth login`), python3.
- **release** `release/scripts/release.sh` (sh) -- Tags the repo 0.5.0, pushes the tag, zips the 12 shipped files into e2e-space-invaders-cc-0.5.0.zip, and publishes/updates the GitHub Release with that asset and release/RELEASE_NOTES.md as the body. Run once from anywhere in the repo after the CI-green check and the Level 2/3 go/no-go decision; safe to re-run (idempotent). Requires: git, the gh CLI authenticated, POSIX zip.
- **release** `release/scripts/release.ps1` (powershell) -- Tags the repo 0.5.0, pushes the tag, zips the 12 shipped files into e2e-space-invaders-cc-0.5.0.zip, and publishes/updates the GitHub Release with that asset and release/RELEASE_NOTES.md as the body. Run once from anywhere in the repo after the CI-green check and the Level 2/3 go/no-go decision; safe to re-run (idempotent). Requires: git, gh.exe on PATH and authenticated.
- **run** `release/scripts/run.py` (python) -- Opens index.html directly from disk (file://) in the default browser, matching this project's no-server, no-build-step design. Run any time you want to play or smoke-test the shipped build locally.
- **run** `release/scripts/run.sh` (sh) -- Opens index.html directly from disk (file://) in the default browser, matching this project's no-server, no-build-step design. Run any time you want to play or smoke-test the shipped build locally.
- **run** `release/scripts/run.ps1` (powershell) -- Opens index.html directly from disk (file://) in the default browser, matching this project's no-server, no-build-step design. Run any time you want to play or smoke-test the shipped build locally.

## Rollback

This is the project's first release, so there is no prior version to fall back to, and the game has no server or runtime environment to roll back -- it is a static, client-side bundle distributed as a downloadable GitHub Release asset plus the raw repo files, so there is nothing to redeploy or restart. If 0.5.0 needs to be pulled after publishing: do NOT delete the git tag or the GitHub Release (destructive remote actions are forbidden). Instead: (1) run `gh release edit 0.5.0 --prerelease` to unmark it as the latest/production release without removing it; (2) edit the release body (`gh release edit 0.5.0 --notes-file <updated-notes>`) to prepend a visible warning, e.g. pointing at the Level 2 progression gap or any newly discovered issue; (3) fix forward with a 0.5.1 patch release once level2.js is corrected, then run `gh release edit 0.5.1 --latest` to point 'latest' at it. Anyone who already downloaded the 0.5.0 zip is unaffected either way since it is a static snapshot with no auto-update mechanism.

