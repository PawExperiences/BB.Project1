# e2e space invaders cc 0.5.0 (0.5.0)

## e2e space invaders cc 0.5.0

A complete, playable Space Invaders clone written in plain ES modules - no bundler, no package manager, no runtime dependency, nothing to install. This is the first tagged release of the project and it covers the whole game: an entry point and a clamped fixed-timestep frame loop, a keyboard-driven ship with a one-shot cannon, the classic 11x5 invader grid with AABB collision and explosions, three escalating levels (speed-up pacing, return fire and a bonus UFO, destructible bunkers and a formation split) and a multi-phase boss finale with a real win screen. Grab the source zip, unzip it, and open `index.html` - or run `release/scripts/run.sh` (`run.py` / `run.ps1` on other platforms) to serve it over `http://localhost:8080/index.html`.

### Highlights

- **Four levels and a boss.** Level 1's grid tightens from ~800 ms to ~100 ms per step as you clear it; Level 2 shoots back and sends a bonus UFO every 20 s; Level 3 adds four destructible bunkers and splits the formation in two at 28 kills; Level 4 is a 10 HP boss with a health bar, a three-bullet spread and a phase change at 5 HP.
- **A loop that behaves.** Fixed 1/60 s simulation steps with rendering in a separate phase, and every frame's delta clamped to 250 ms - come back to a backgrounded tab and the game resumes instead of teleporting.
- **Zero dependencies, zero build.** Relative-path ES module imports only. No npm install, no bundler, no fetch of local files, no image assets - every sprite, explosion, bunker and boss is drawn with canvas 2D primitives.
- **One HUD, one registry.** `game.js` exports a single mutable HUD state object (`score`, `lives`, `hiScore`) and `levels.js` owns level dispatch, so levels plug in via `registerLevel()` without level-specific branches piling up in the loop.
- **Sudden death, then a win screen.** One boss projectile ends the run whatever your lives count; 0 boss HP routes to the shared `winGame()` path with your final score and a restart option.

### Playing it

1. Download `space-invaders-0.5.0.zip` below and unzip it.
2. Run `sh release/scripts/run.sh` (or `python3 release/scripts/run.py`, or `powershell -File release/scripts/run.ps1`) and let it open `http://localhost:8080/index.html`.
3. Press **ENTER** to start. **Left/Right** or **A/D** to move, **Space** to fire (one bullet in flight at a time).

If your browser blocks ES module imports from `file://` origins (Chrome and Edge report a CORS failure for origin `null`), the `run` scripts above are the reliable path - they serve the same folder over `http://` with Python's standard library and nothing else.

### What's in the box

`index.html`, `game.js`, `gameConfig.js`, `input.js`, `player.js`, `invaders.js`, `collision.js`, `levels.js`, `level1.js`, `level2.js`, `level3.js`, `boss.js`, `README.md`, and the helper scripts under `release/scripts/`.

### Notes for maintainers

This tag replaces the previous e2e project's tree: `pom.xml`, `CMakeLists.txt` and the Java/C++ sources are gone at this ref, so any CI pointed at a Maven or CMake build must be re-pointed at the ES-module toolchain before it will go green. See the release runbook step "Confirm the CI build workflow matches this release's toolchain".


## Highlights

- Four playable stages: the classic grid, invaders that shoot back with a bonus UFO, destructible bunkers plus a 28-kill formation split, and a 10 HP multi-phase boss finale.
- Fixed-timestep loop at 60 steps/s with rendering in a separate phase and every frame's delta clamped to 250 ms, so a backgrounded tab resumes instead of teleporting the ship.
- Zero dependencies and zero build: relative-path ES modules, no npm, no bundler, no image assets - every sprite, explosion, bunker and boss is drawn with canvas 2D primitives.
- Level 1 pacing scales linearly with the alive count (~800 ms per step at 55 invaders down to ~100 ms at 1), and Level 2 multiplies the whole curve by 0.67.
- Shared plumbing that later levels reuse instead of duplicating: one exported HUD state object (`score`, `lives`, `hiScore`), one `levels.js` registry, one AABB collision pass in `collision.js`.
- A real ending: the boss fight is sudden death, and 0 boss HP routes to the shared win screen with the final score and a restart option.

## Breaking changes

- Repository contents replaced: `pom.xml`, `CMakeLists.txt`, `src/main/java/com/buildboard/calculator/**`, `src/*.cpp` and `tests/sieve_test.cpp` from the earlier e2e projects are not present at this ref, so any checkout, script or pipeline that expects a Maven or CMake build of `main` fails at this tag.
- CI toolchain must be re-pointed: `.github/workflows/build.yml` is re-scaffolded by BuildBoard from the build resource's `command` + `artifact_path`. Those values must move from the previous Java/C++ toolchain to the ES-module toolchain (a Node syntax check, static-file artifact) or the build fails on a step that has nothing to do with this release.
- `release/scripts/release.*` and `release/scripts/run.*` are rewritten for this project. The earlier versions in this repo targeted the JAR/binary artifacts of the previous e2e projects; anyone invoking them from an old checkout gets different behaviour.

## Changelog

# Changelog

All notable changes to this project are recorded in this file. This is the first tagged release, so `0.5.0` describes the whole game.

## [0.5.0] - e2e space invaders cc

First tagged release (no previous tag). Bundles PRs #386-#392 (`4defccf`..`661e2ad`).

### Added
- **Entry point** - `index.html` at the repository root: dark page background, exactly one `<canvas>` at `width="768"` / `height="896"`, and `game.js` loaded via `<script type="module" src="game.js">`. No CDN, no bundler output, no npm package. (#386)
- **Frame loop and scenes** - `game.js` runs a fixed-timestep loop at 60 update steps/s with `update` and `render` as distinct phases, and the Title / Playing / Game Over / Win scenes. ENTER drives every transition entirely in-page: no `location.reload()`, no assignment to `location`, no navigation. (#386)
- **Shared HUD state** - `game.js` exports a single mutable HUD state object (`score`, `lives`, `hiScore`). Starting a game resets `score` to 0 and `lives` to 3; `hiScore` is the best score of the page session and survives restarts. (#386)
- **Tunables** - `gameConfig.js` named exports: `CANVAS_WIDTH` 768, `CANVAS_HEIGHT` 896, `PLAYER_SPEED` 200, `BULLET_SPEED` 500, starting lives 3, plus grid dimensions, step distance/interval, row drop, explosion duration, points per invader and invulnerability seconds used by every level. (#386, #388)
- **Keyboard input** - `input.js` exporting `initInput()` and `isKeyHeld(key)`. Held-key state is polled per frame rather than driven by OS key repeat; `ArrowLeft`/`ArrowRight`, `a`/`A`/`d`/`D` and `Space`/`" "` normalise to stable identifiers; arrows and space no longer scroll the page. (#387)
- **Player ship** - `player.js` exporting `Player` with `update(dt)` / `draw(ctx)`: delta-time movement at 200 px/s clamped to the playfield, drawn procedurally from canvas arcs and rectangles (no sprites), a strictly one-bullet-at-a-time cannon at 500 px/s, and a lives counter with `loseLife()`, `grantInvulnerability(seconds)`, `resetPosition()` and a full reset path. (#387)
- **Invader formation** - `invaders.js` exporting `InvaderFormation` (`reset`, `step`, `aliveCount`, `lowestBottom`, `draw`): 55 invaders in the classic 11x5 grid, advancing in discrete steps driven by a `dt` accumulator, dropping a row and reversing when the *living* bounding box would leave the playfield. Kills leave holes; the grid never re-flows. (#388)
- **Collision and explosions** - `collision.js` exporting `overlaps(a, b)` and `collide(state)`: one axis-aligned bounding-box pass per frame for player-bullet-vs-invader (kill, explosion, score) and hostile-bullet-vs-player (life loss then brief invulnerability), plus `spawnExplosion` / `updateExplosions` / `drawExplosions` / `clearExplosions` built from canvas primitives only. (#388)
- **Level registry** - `levels.js` exporting `registerLevel`, `isLevelRegistered` and `createLevel`; a request for an unregistered level falls through to the Game Over scene instead of crashing or blanking the canvas. (#389)
- **Level 1: the classic grid** - `level1.js`, registered as level 1. Step interval `100 + (aliveCount - 1) * (700 / 54)` ms - about 800 ms at 55 alive, about 100 ms at 1 - so survivors visibly speed up. The formation reaching the player's row costs a life and restarts the level from a fresh grid; the last life routes to `endGame()`. Clearing all 55 calls `advanceLevel()`. The HUD gains the current level number. (#389)
- **Level 2: they shoot back** - `level2.js`, extending `Level1`: every step interval multiplied by 0.67, a single global enemy-fire timer at a uniform 800-2000 ms that spawns one bullet from the lowest living invader of an occupied column, hostile bullets at 300 px/s, a bonus UFO every 20 s that alternates entry side and crosses at 120 px/s scoring `[50,100,150,300][shotCount % 4]` (session-cumulative shot count, never random), and death -> bottom-centre respawn with 2 s of flashing invulnerability. Lives carry over from Level 1; clearing the grid advances to level 3. (#390)
- **Level 3: shields and formations** - `level3.js`, extending `Level2`: four destructible bunkers evenly spaced across the canvas with their tops at ~80% of canvas height, each a 4x4 grid of ~8 px cells (64 cells at level start), eroded permanently by player bullets, hostile bullets and descending invaders - never rebuilt, not even on respawn. At 28 kills the survivors split into independent left (original columns 1-6) and right (columns 7-11) groups that start moving apart and sweep-and-drop on their own schedules; clearing both advances to level 4. (#391)
- **Boss level: multi-phase finale** - `boss.js`, self-registering as level 4: a 160x80 boss drawn purely with canvas 2D primitives, 10 HP with a health bar across the top of the canvas, 90 px/s horizontal drift with edge reversal and no vertical movement, and a three-bullet spread from its centre at 0 deg / +20 deg / -20 deg at 260 px/s - every 1500 ms while HP >= 6, every 700 ms from HP <= 5. Sudden death: any boss projectile touching the player ends the run regardless of remaining lives. 0 HP routes to `winGame()`. (#392)
- **README** - the file layout plus a numbered manual verification path per level, runnable end to end from a browser. (#386-#392)
- **Release tooling** - `release/scripts/release.{py,sh,ps1}` and `release/scripts/run.{py,sh,ps1}`.

### Changed
- **Repository toolchain** - the tree at this tag is a browser-only ES-module game with no build step, no package manager and no runtime dependency. The Maven project (`pom.xml`, `src/main/java/com/buildboard/calculator/**`) and the CMake project (`CMakeLists.txt`, `src/*.cpp`, `tests/sieve_test.cpp`) from earlier e2e runs are not present at this ref.
- **Frame order** - one frame in `game.js` executes as entity updates -> `collide(state)` -> `updateExplosions(dt)` -> render. Nothing reachable from the render path kills an invader, removes a bullet, or changes score or lives.

### Fixed
- **Backgrounded-tab catch-up burst** - each frame's raw delta is clamped to 250 ms before it enters the accumulator, capping a single frame at 15 update steps, so returning to a backgrounded tab no longer teleports the ship and the invaders.
- **Two end screens** - `boss.js` no longer renders its own win screen; it delegates to `winGame()` / `renderWin()` in `game.js`, so only one end screen can ever appear.
- **Enemy-fire branch with no shooters** - `collide(state)` is a no-op and does not throw when the hostile-bullet list is absent or empty, so Level 1 (which has no enemy fire) runs clean.


## Release steps

1. [automated] Verify the release bundle is on main and the shipped tree is complete - `git fetch origin --tags && git checkout main && git pull --ff-only`, then confirm the seven bundled commits are ancestors of HEAD: `git log --oneline 4defccf~1..661e2ad`. Then confirm the working tree actually holds the game: `ls index.html README.md game.js gameConfig.js input.js player.js invaders.js collision.js levels.js level1.js level2.js level3.js boss.js`. This check is REQUIRED and not a formality: the diffstat supplied for this release is dominated by deletions of the previous e2e projects (`pom.xml`, `CMakeLists.txt`, `src/**`) and shows the game modules as removed rather than added, which is inconsistent with the seven `feat:` commits above it. If any of those files is missing from the checkout, stop and raise it before tagging - do not publish a tag over an empty tree.
2. [automated] Syntax-check every ES module - There is no compile step, so this is the whole static gate: `node --version` (expect >= 22, where ESM syntax detection is on by default), then `for f in game.js gameConfig.js input.js player.js invaders.js collision.js levels.js level1.js level2.js level3.js boss.js; do node --check "$f" || exit 1; done`. On Node 20.x add the detection flag: `node --experimental-detect-module --check "$f"`. Also assert the no-dependency and in-page-transition rules hold: `grep -rn "location.reload\|location.href\|fetch(\|XMLHttpRequest\|cdn\." -- *.js index.html` must return nothing, and there must be no `package.json`, `node_modules/` or lockfile at the root.
3. [manual] Confirm the CI build workflow matches this release's toolchain (REQUIRED) - `.github/workflows/build.yml` is owned by BuildBoard and re-scaffolded from the build resource's `command` + `artifact_path` on every build - never hand-edit it, and never delete it. Read the version on `main` (`gh api repos/PawExperiences/BB.Project1/contents/.github/workflows/build.yml --jq '.content' | base64 -d`) and check what it runs. This repository's recent history scaffolded workflows for other toolchains (`93e88bf`, `3472e4f`, `df703a9` 'ci: update BuildBoard build workflow') and this release changes the toolchain completely: from Maven/CMake compilation to a browser-only ES-module app with **no build step**. If the workflow still invokes `mvn`, `cmake`, `ctest` or publishes `target/*.jar` or a compiled binary, you MUST update the build resource config before shipping - set `command` to the Node syntax check from step 2 and `artifact_path` to the static game files (`index.html`, `*.js`, `README.md`) - and let BuildBoard re-scaffold the workflow. Then confirm the regenerated `build.yml` on `main` matches. Skipping this leaves CI green-or-red on a toolchain we no longer ship.
4. [automated] Confirm CI is green on the exact release commit - `gh run list --branch main --limit 5` and `gh run watch <run-id> --exit-status`, then verify the run's head SHA equals `git rev-parse HEAD`. A green run against an older SHA does not count. If step 3 changed the build resource config, re-run the build first so CI executes the new command, and confirm the published artifact under `artifact_path` contains `index.html` and all eleven modules.
5. [manual] Manual browser smoke test against the README verification path - Human judgement - this is the only real functional gate, as there is no automated test suite. Run `sh release/scripts/run.sh` (or `python3 release/scripts/run.py`) and open `http://localhost:8080/index.html` with the devtools console visible. Walk the README path: Title draws 'SPACE INVADERS' and 'Press ENTER to start'; ENTER starts Level 1 with 55 invaders in 11x5 and on-canvas score/lives/level; the formation speeds up as you clear it and drops+reverses at each edge; clearing it enters Level 2 with lives carried over, enemy fire from lowest invaders only, and a UFO at ~20 s alternating sides; Level 3 shows four bunkers that erode from your own shots and split the formation at 28 kills; the boss drifts at a constant height, its health bar depletes, the fire rate visibly doubles at 5 HP, one hit ends the run, and 0 HP shows the win screen with the final score. Also verify Game Over -> ENTER -> Title. Then repeat the first 30 seconds by double-clicking `index.html` directly (a `file://` URL) - that path is an explicit acceptance criterion on every bundled card. If your browser refuses the module imports there (Chrome/Edge: CORS, origin `null`), do not silently ship: record it as an AC failure for the board and get a human decision on whether `file://` support is satisfied by another browser or needs a follow-up card. Console must be free of errors throughout.
6. [automated] Prepare the release documentation on a branch and open the PR - Follow the repo's existing convention (`53ba208`, `323d505`): `git checkout -b release-notes/0-5-0`, write `CHANGELOG.md` (the 0.5.0 section above - the file does not currently exist at this ref), `docs/releases/0-5-0.md` (the release body above), the README updates listed in docsUpdates, and `release/scripts/{release,run}.{py,sh,ps1}`. `chmod +x release/scripts/*.sh release/scripts/*.py`. Commit as `chore(release): notes, changelog + helper scripts for e2e space invaders cc 0.5.0`, `git push -u origin release-notes/0-5-0`, then `gh pr create --base main --title 'chore(release): e2e space invaders cc 0.5.0' --body-file docs/releases/0-5-0.md`.
7. [manual] Review and merge the release PR - Standard gate: a human reads the changelog against the six bundled cards and the diff, confirms the notes claim nothing the smoke test did not show, and merges (`gh pr merge --merge`). Merge only - no squash-and-force, no branch deletion via the API. After merging, `git checkout main && git pull --ff-only` so the tag in step 9 lands on the merge commit.
8. [manual] Final go/no-go to publish - Explicit human approval before anything becomes public and permanent: smoke test passed (including a decision on the `file://` path), CI green on the release commit with the corrected toolchain, release PR merged, notes read correctly. Confirm the tag name convention too - the scripts default to `v0.5.0`; if the board expects an unprefixed `0.5.0`, decide now, because an existing tag will never be moved or deleted afterwards.
9. [automated] Create and push the annotated tag v0.5.0 - `python3 release/scripts/release.py --dry-run` first to see exactly what will happen, then `python3 release/scripts/release.py` (or `sh release/scripts/release.sh`, or `powershell -File release/scripts/release.ps1`). It refuses to run on a dirty tree, creates `v0.5.0` annotated on HEAD, and pushes it to `origin`. If the tag already exists on a different commit the script stops and asks a human rather than moving it - there is no previous tag for this project (`PREVIOUS: None`), so the range this release covers is the full history up to HEAD.
10. [automated] Package the distributable zip - The same script produces `dist/space-invaders-0.5.0.zip` with `git archive --format=zip --prefix=space-invaders-0.5.0/ -o dist/space-invaders-0.5.0.zip v0.5.0`, so the archive is exactly the tagged tree - no local strays. Verify with `unzip -l dist/space-invaders-0.5.0.zip` that it contains `index.html`, all eleven modules and `README.md`. The archive also carries repository bookkeeping (`.buildboard/merge-test-*.md`), which is harmless; if you want it excluded, add `export-ignore` attributes in a follow-up rather than editing the zip.
11. [automated] Publish the GitHub release and upload the artifact - Also done by the same script run: `gh release create v0.5.0 --title 'e2e space invaders cc 0.5.0' --notes-file docs/releases/0-5-0.md` followed by `gh release upload v0.5.0 dist/space-invaders-0.5.0.zip`. Both are skipped when the release or the asset already exists, and the script never passes `--clobber` or deletes an asset. If `gh` is not installed or not authenticated the script stops after tagging and packaging and prints the two commands to run by hand.
12. [manual] Post-publish verification from the published artifact - Prove the thing users get actually works: `gh release download v0.5.0 --pattern 'space-invaders-0.5.0.zip' --dir /tmp/si-verify`, unzip into a clean directory, run `sh release/scripts/run.sh` from inside it, and play the first minute of Level 1 (movement both key sets, edge clamp, single-bullet firing, a kill with an explosion and a score increase). Confirm the release page on GitHub renders the notes and lists the asset.
13. [manual] Announce and close out the board - Post the release link where the project is tracked, then move the six bundled tasks (Game loop and canvas framework, Keyboard input and the player ship, Sprite rendering and collision detection, Level 1, Level 2, Level 3, Boss level) to Done with the release link attached. Record any open item raised in step 5 (notably the `file://` ES-module behaviour) as a new Inbox card rather than fixing it here.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Performs the automated part of shipping 0.5.0: creates the annotated tag v0.5.0 on HEAD, pushes it to origin, packages the tagged tree into dist/space-invaders-0.5.0.zip with `git archive`, and (when the GitHub CLI is available) publishes the GitHub release from docs/releases/0-5-0.md and uploads the zip. Run it from the repository root after the release-notes PR is merged and CI on the release commit is green (runbook steps 9-11); it is idempotent, skips work already done, and never deletes, moves or force-pushes anything.
- **release** `release/scripts/release.sh` (sh) -- POSIX sh twin of release.py: creates the annotated tag v0.5.0 on HEAD, pushes it to origin, packages the tagged tree into dist/space-invaders-0.5.0.zip with `git archive`, and publishes the GitHub release from docs/releases/0-5-0.md plus the zip asset when `gh` is available. Run it from the repository root after the release-notes PR is merged and CI is green (runbook steps 9-11); re-running is safe because every step is skipped once done, and it never deletes or force-pushes anything.
- **release** `release/scripts/release.ps1` (powershell) -- Windows PowerShell twin of release.sh: creates the annotated tag v0.5.0 on HEAD, pushes it to origin, packages the tagged tree into dist/space-invaders-0.5.0.zip with `git archive`, and publishes the GitHub release from docs/releases/0-5-0.md plus the zip asset when `gh` is available. Run it from the repository root (`powershell -File release/scripts/release.ps1`) after the release-notes PR is merged and CI is green (runbook steps 9-11); it skips anything already done and never deletes or force-pushes remote state.
- **run** `release/scripts/run.py` (python) -- Starts a read-only static file server (Python's stdlib http.server) rooted at the repository root and opens http://localhost:8080/index.html, which is how you play or smoke-test the game when your browser refuses ES module imports from a file:// origin. Run it for the manual verification step of the runbook, or in an unzipped copy of the release artifact; if the port is already serving it prints the URL and exits instead of starting a second server, and it writes nothing to disk.
- **run** `release/scripts/run.sh` (sh) -- POSIX sh twin of run.py: serves the repository root over http://localhost:8080/index.html with Python's stdlib http.server and opens a browser, which is how you play or smoke-test the game when the browser blocks ES module imports from file://. Run it for the manual verification step of the runbook or inside an unzipped release artifact; it refuses to start a second server if the port is already in use and writes nothing.
- **run** `release/scripts/run.ps1` (powershell) -- Windows PowerShell twin of run.sh: serves the repository root over http://localhost:8080/index.html using Python's stdlib http.server and opens the default browser, the reliable way to play or smoke-test the game when the browser blocks ES modules on file:// origins. Run it (`powershell -File release/scripts/run.ps1`) for the runbook's manual verification step or inside an unzipped release artifact; it exits without starting a second server when the port is already in use and writes nothing.

## Rollback

**Nothing in this release persists state, so rollback is purely about the published artefacts and the branch - and it is revert-forward only. Never delete or force-push remote state.**

1. **Scope check.** The game keeps no data outside the page session: `hiScore` lives in the exported HUD state object in memory and is gone on reload; there is no storage, no server, no schema. There is nothing to restore for players - a user rolls back by re-opening an older copy of the folder.
2. **Stop the bleeding (no deletion).** If a defect is found after publishing, edit the GitHub release rather than removing it: `gh release edit v0.5.0 --prerelease --draft=false` and prepend a bold warning to the body. Do **not** run `gh release delete`, do not delete or move the `v0.5.0` tag, and do not force-push - the tag is a permanent, immutable record.
3. **Revert the code on a branch.** `git checkout -b revert/0-5-0 main`, then revert the merge commits newest-first: `git revert -m 1 661e2ad a94271b 6c32f6b 4b1dc28 d3bea1d 222f1bf 4defccf` (drop `-m 1` for any that turn out to be non-merge commits). Push the branch and open a PR - the same review gate as any other change.
4. **Or revert one card.** The levels are additive and independently reverted: reverting only `661e2ad` (boss) leaves levels 1-3 playable, and clearing Level 3 then falls through the registry's unregistered-level branch to Game Over instead of crashing (`levels.js` `isLevelRegistered`). Reverting `a94271b` (Level 3) likewise leaves 1-2 playable. Reverting `4defccf` (loop + `index.html`) is not partially revertible - it takes the whole game with it.
5. **Ship the correction as 0.5.1.** Once the revert PR is merged, re-run `release/scripts/release.*` with `--version 0.5.1`. The scripts are idempotent and refuse to move an existing tag, so a re-run against `v0.5.0` stops rather than rewriting history.
6. **CI.** If the build resource config was changed for this release and must be undone, change it back in BuildBoard and let it re-scaffold `.github/workflows/build.yml`. Do not hand-edit or delete the workflow file.
7. **Local artefact.** `dist/space-invaders-0.5.0.zip` is a local build product; delete it locally if you rebuild, and re-create it from the tag with `git archive` at any time.

