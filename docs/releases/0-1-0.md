# e2e link checker 0.1.0 (0.1.0)

## e2e link checker v0.1.0

First release of `linkcheck`: a small, dependency-free, offline CLI that scans a single Markdown file and reports links whose targets are syntactically broken -- no network calls, no external services, just fast lexical checks you can run locally or wire into CI.

### Highlights
- Install with `pip install .`, run with `linkcheck <file.md>`.
- Flags four kinds of broken targets: empty, unsupported URI scheme, embedded whitespace, and unresolvable site-absolute paths (`/...`).
- stdout stays machine-parseable (`<line>:<target>: <reason>` per broken link); a human-readable summary of files/links/broken-links goes to stderr.
- Clear exit-status contract for scripting: `0` clean, `1` broken links found, `2` usage error.
- Covered by a subprocess-driven regression suite against fixture Markdown files (mixed and fully-clean).

### Getting started
```
pip install .
linkcheck README.md
```

No configuration and no third-party runtime dependencies -- just the Python standard library.


## Highlights

- First release of the `linkcheck` CLI: `pip install .` then `linkcheck <file.md>` to scan a single Markdown file for syntactically broken links, fully offline.
- Detects four classes of broken targets -- empty, unsupported scheme, embedded whitespace, unsupported absolute path -- reported as `<line>:<target>: <reason>`.
- New exit-status contract for scripting/CI: 0 clean, 1 broken links found, 2 usage error, plus a human-readable files/links/broken-links summary on stderr.
- Zero third-party runtime dependencies -- pure standard library, PEP 517 packaged.
- Regression-tested via a subprocess-driven pytest suite against mixed and fully-clean fixture documents.

## Changelog

## [0.1.0] - 2026-08-12

### Added
- New `linkcheck` Python package and installable CLI (`pip install .` / `pip install -e .`), PEP 517 packaged via `pyproject.toml`, exposing a `linkcheck` console script (`linkcheck.cli:main`). No third-party runtime dependencies. (#298)
- Static, offline link checker for a single Markdown file: extracts every inline link `[text](target)` (excluding image syntax `![alt](target)`) and every reference-style link definition `[id]: target`, each with its 1-based line number. (#298)
- Broken-target detection, checked in priority order: empty/whitespace-only target (`empty target`), non-http/https/mailto scheme (`unsupported scheme`), whitespace inside the target (`contains a space`), and scheme-less absolute path starting with `/` (`absolute path not supported`). Reported to stdout as `<line>:<target>: <reason>`. (#298)
- Exit-status contract: `0` clean, `1` one or more broken links, `2` usage/IO error (missing args or an unreadable file path). (#298, #299)
- Human-readable run summary (files scanned, links found, broken links) written to **stderr**, keeping stdout machine-parseable for CI/piping. (#299)
- `README.md`: install/usage instructions, the four broken-link rules, the output line format, and the exit-status contract. (#298, #299)
- Regression test suite (`tests/test_linkcheck.py`) driving the CLI as a subprocess against `tests/fixtures/sample.md` (mixed valid/broken links; asserts the exact set of reported line numbers and exit status 1) and `tests/fixtures/clean.md` (only valid links; asserts no stdout output and exit status 0). (#300)

### Changed
- The usage/IO-error exit status, described in the initial implementation as "non-zero other than 1 (e.g. 2)", is now pinned exactly to `2` and documented as part of the formal exit-status contract. (#299)


## Release steps

1. [manual] Confirm scope is merged and CI is green - On GitHub, confirm PRs #298 (9b0342d), #299 (3e6dbb7) and #300 (4f51bfd) are merged into main and that main's latest CI run is green. No other commits should be included in this release.
2. [automated] Run the full test suite locally - From the repo root: `python -m pytest`. Must exit 0.
3. [automated] Build the distribution artifacts - `python -m pip install --upgrade build && python -m build` from the repo root; produces `dist/linkcheck-0.1.0-py3-none-any.whl` and `dist/linkcheck-0.1.0.tar.gz`. (Also handled by release.py/.sh/.ps1 below.)
4. [automated] Smoke-test the built wheel in a clean environment - In a throwaway venv: `python -m venv /tmp/lc-smoke && /tmp/lc-smoke/bin/pip install dist/linkcheck-0.1.0-py3-none-any.whl`, then `/tmp/lc-smoke/bin/linkcheck tests/fixtures/clean.md` (expect exit 0, no stdout) and `/tmp/lc-smoke/bin/linkcheck tests/fixtures/sample.md` (expect exit 1, broken-link lines on stdout). Confirms the packaged console-script entry point works, not just the source tree.
5. [manual] Confirm the CI build workflow matches this release's toolchain - `.github/workflows/build.yml` is owned by BuildBoard and re-scaffolded from the project's build resource command + artifact_path. This release introduces the first real build: a PEP 517 Python build via `python -m build` producing `dist/`. In BuildBoard project settings, confirm the build resource's command builds/tests this package (e.g. `python -m pytest && python -m build`) and artifact_path is `dist/`. If either differs, update the build resource config -- not build.yml directly -- so it re-scaffolds correctly before tagging.
6. [manual] Add CHANGELOG.md and merge to main - Open a PR adding CHANGELOG.md at the repo root with the [0.1.0] entry from the changelog field above (no prior file exists -- this is the first entry). Get it reviewed per the project's standard gate and merged to main before tagging.
7. [automated] Tag, build, and publish the release - From a clean main checkout, run release/scripts/release.py (or release.sh / release.ps1, which delegate to it). It re-runs tests, rebuilds artifacts, creates annotated tag v0.1.0, pushes it to origin, and -- if the gh CLI is authenticated -- publishes a GitHub Release attaching the wheel and sdist. Idempotent: re-running after a partial failure skips steps already completed (existing tag/release).
8. [manual] Verify the published GitHub Release - Open the v0.1.0 release page on GitHub; confirm the wheel and sdist are attached, the release body matches releaseNotes, and the tag points at commit 4f51bfd. Download the wheel from the release and pip install it into a scratch venv as a final sanity check.
9. [manual] Announce the release - Post the release link and highlights in the project's usual release-announcement channel per the team's convention (not specified in this runbook's inputs).

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Runs pytest, builds the sdist/wheel with python -m build, creates and pushes annotated git tag v0.1.0, and (if the gh CLI is authenticated) publishes a GitHub Release with the built artifacts attached. Run once from a clean, up-to-date main checkout after the CHANGELOG.md PR has merged. This is the single source of truth for release logic; release.sh and release.ps1 are thin wrappers around it.
- **release** `release/scripts/release.sh` (sh) -- POSIX sh wrapper that delegates to release.py -- this is a Python project, so python3 is already required to build/test it, and duplicating the full release logic in three languages would just triple the bug surface. Run once from a clean, up-to-date main checkout after the CHANGELOG.md PR has merged; forwards all arguments and env vars to release.py.
- **release** `release/scripts/release.ps1` (powershell) -- PowerShell 5.1-compatible wrapper that delegates to release.py -- this is a Python project, so python is already required to build/test it, and duplicating the full release logic in three languages would just triple the bug surface. Run once from a clean, up-to-date main checkout after the CHANGELOG.md PR has merged; forwards the version argument to release.py.

## Rollback

0.1.0 is the first published release, so there is no prior version to roll back to. If a critical defect surfaces after release: (1) Do NOT delete the v0.1.0 git tag or the GitHub Release -- deleting remote state is forbidden. (2) Edit the existing GitHub Release description in place to prepend a visible warning (e.g. "Known issue: <describe>; fixed in v0.1.1+") linking the tracking ticket. (3) There is no PyPI publish in this release, so `pip uninstall linkcheck` is sufficient for anyone who installed from the wheel/sdist directly; if a future release adds PyPI publishing, yank (do not delete) the affected version there instead. (4) Fix the defect, bump to 0.1.1, and run this runbook again -- never force-push or rewrite the v0.1.0 tag's commit. (5) Once 0.1.1 ships, edit the v0.1.0 release notes in place to point users at 0.1.1.

