# e2e space invaders 0.1.0 (0.1.0)

## e2e Space Invaders v0.1.0

This is the first playable release of **e2e Space Invaders** — a complete, four-level browser Space Invaders game built entirely with vanilla ES modules and the HTML5 Canvas API. No bundler, no npm dependencies, no server required; open `index.html` from a `file://` URL and play.

### Highlights

- **Full game loop** with a fixed 60-step/s timestep, delta cap, and a three-scene state machine (Title → Playing → Game Over → Title).
- **Keyboard-driven player ship**: smooth delta-time movement, single-bullet firing, procedural canvas rendering.
- **Classic invader grid**: 11×5 formation with step-and-drop sweep, AABB collision, per-kill explosion flashes, and a live score counter.
- **Four escalating levels**: Level 1 (classic grid, speed scales with kill count) → Level 2 (invaders shoot back, bonus UFO, player invulnerability) → Level 3 (destructible bunkers, mid-wave formation split) → Level 4 Boss (multi-phase spread fire, health bar, sudden-death, win screen).
- **Persistent session state**: score and shot count carry across levels; lives carry from Level 1 into Level 2.
- **Zero external dependencies**: works offline from `file://`, pure ES modules, no build step needed for play.

## Highlights

- First playable release — complete four-level Space Invaders game in vanilla JS + Canvas API, no dependencies.
- Fixed-timestep game loop (60 steps/s) with delta cap prevents catch-up bursts after backgrounding.
- Progressive difficulty: speed-scaled invader sweep (Level 1) → enemy fire + UFO bonus (Level 2) → destructible bunkers + formation split (Level 3) → multi-phase boss (Level 4).
- Full AABB collision system (`collision.js`) shared across all levels — no duplicate collision code.
- Sudden-death boss fight with two fire phases, canvas health bar, and a proper win screen.
- Zero-dependency, file://-compatible ES module architecture — open index.html and play immediately.

## Changelog

# Changelog

## [0.1.0] - Initial Release

### Added
- **Game loop and canvas framework** (`index.html`, `game.js`, `gameConfig.js`, `README.md`): Fixed-timestep game loop at 60 updates/s with delta cap, three-scene state machine (Title → Playing → Game Over), canvas HUD (score, lives), shared config constants (CANVAS_WIDTH=768, CANVAS_HEIGHT=896, PLAYER_SPEED=200, BULLET_SPEED=500, STARTING_LIVES=3), and named `hudState` export.
- **Keyboard input and player ship** (`input.js`, `player.js`): Key-held tracking without browser repeat artifacts, player movement (ArrowLeft/A, ArrowRight/D) clamped to canvas bounds, single-bullet firing (Space), procedural ship and bullet rendering, lives counter initialised from config.
- **Sprite rendering and collision detection** (`invaders.js`, `collision.js`): 11×5 invader formation (55 invaders, 32×24 px cells, 8 px gaps), step-and-drop sweep, per-kill explosion flash, exported `score` counter, AABB `runCollisions()` covering player-bullet-vs-invader and invader-bullet-vs-player pairs.
- **Level 1 – The Classic Grid** (`level1.js`): Lifecycle object `{ init, update, render }`, step-interval scaling from ~800 ms (55 alive) to ~100 ms (1 alive), edge-detect-drop, lose condition (formation reaches player row), win condition (all 55 cleared → Level 2), HUD level label.
- **Level 2 – They Shoot Back** (`level2.js`): Formation at 1.5× Level 1 speed (intervals × 0.67), random invader fire (800–2000 ms, lowest-in-column), invader bullets at 300 px/s, bonus UFO every 20 s (alternating sides, 120 px/s, 50/100/150/300 pt tiers keyed by session shot count), 2-second post-respawn invulnerability with ship flash, lives carry over from Level 1.
- **Level 3 – Shields and Split Formations** (`level3.js`): Four destructible 4×4-cell bunkers at ~80% canvas height, per-cell erosion by player bullets, invader bullets, and descending invaders; formation split at ≥28 kills into independent left (cols 1–6) and right (cols 7–11) halves with opposite initial directions; win → Boss transition.
- **Boss Level – Multi-Phase Finale** (`boss.js`): 160×80 px canvas-primitive boss, 90 px/s horizontal drift, 10 HP health bar, Phase 1 three-bullet spread every 1500 ms (HP 6–10), Phase 2 every 700 ms (HP ≤5), collision via `collision.js`, sudden-death rule (one hit → Level 1 restart), win screen with final score and Play Again.
- **CI workflow** (`.github/workflows/build.yml`): BuildBoard-scaffolded build workflow for the project.

### Changed
- `README.md` expanded from stub to full documentation (planned file layout, manual verification steps, Level 2–4 verification sections).

### Fixed
- N/A (initial release).

## Release steps

1. [manual] Confirm all bundled tasks are in the Done lane - Verify that all seven tasks (Game loop and canvas framework, Keyboard input and the player ship, Sprite rendering and collision detection, Level 1, Level 2, Level 3, Boss level) are in the Done lane on the BuildBoard kanban. Do not proceed if any task is still in Review or earlier.
2. [manual] Pull latest main / default branch and verify clean working tree - Run:
  git checkout main && git pull origin main && git status
Expected: 'nothing to commit, working tree clean'. Confirm HEAD is commit 290673f (Boss level: multi-phase finale).
3. [manual] Confirm CI build workflow (.github/workflows/build.yml) is current - Open .github/workflows/build.yml and confirm it reflects the correct toolchain for this release. This project ships plain ES modules with no build step (no Node compile, no bundler). The workflow should check out the repo and verify the presence of the expected artefact files (index.html, game.js, gameConfig.js, input.js, player.js, invaders.js, collision.js, level1.js, level2.js, level3.js, boss.js, README.md). If the workflow's build command or artifact_path was updated in this release cycle, update the build resource config in BuildBoard so the workflow re-scaffolds correctly before tagging. The workflow must pass on the target commit before the tag is pushed.
4. [manual] Run manual smoke-test via file:// URL - Open index.html directly in a browser (File → Open File, or drag-and-drop). Verify:
  1. Title scene shows 'SPACE INVADERS' and 'Press ENTER to start' centred on the 768×896 canvas.
  2. ENTER transitions to Playing scene (no page reload).
  3. Invader grid (11×5) renders with correct gaps.
  4. Arrow/A/D moves the player ship; Space fires one bullet at a time.
  5. Shooting all invaders in Level 1 progresses to Level 2.
  6. Invaders fire back in Level 2; UFO appears after 20 s.
  7. Level 3 bunkers are visible; formation splits at ~28 kills.
  8. Boss level starts after Level 3 cleared; health bar depletes; win screen appears on boss death.
  9. Game Over triggers correctly on life loss; ENTER returns to Title.
  10. No console errors at any point.
Document pass/fail per criterion.
5. [automated] Verify expected files are present in repo root - Run the release/scripts/release.py (or .sh / .ps1) pre-flight check, OR run:
  python -c "
import os, sys
expected = ['index.html','game.js','gameConfig.js','input.js','player.js','invaders.js','collision.js','level1.js','level2.js','level3.js','boss.js','README.md']
missing = [f for f in expected if not os.path.isfile(f)]
if missing: print('MISSING:', missing); sys.exit(1)
print('All files present.')
"
Must exit 0 with 'All files present.' before proceeding.
6. [manual] Create and push the v0.1.0 annotated git tag - Run:
  git tag -a v0.1.0 -m "e2e Space Invaders 0.1.0 — initial release"
  git push origin v0.1.0
Do NOT delete or move any existing tags. Confirm the tag appears on the remote: https://github.com/PawExperiences/BB.Project1/tags
7. [manual] Verify CI build passes on the v0.1.0 tag - Navigate to https://github.com/PawExperiences/BB.Project1/actions and confirm the workflow triggered by the v0.1.0 tag push completes with a green checkmark. If it fails, do not publish the release — investigate and fix on a hotfix branch, then re-tag as v0.1.1.
8. [automated] Package release artefact (zip of game files) - Run release/scripts/release.py (or .sh / .ps1) to create the distributable zip. The script zips exactly: index.html, game.js, gameConfig.js, input.js, player.js, invaders.js, collision.js, level1.js, level2.js, level3.js, boss.js, README.md into e2e-space-invaders-0.1.0.zip.
9. [manual] Create and publish GitHub Release - Go to https://github.com/PawExperiences/BB.Project1/releases/new
  - Tag: v0.1.0 (select existing tag pushed in step 6)
  - Title: e2e Space Invaders 0.1.0
  - Body: paste the release notes from the releaseNotes field above
  - Attach: e2e-space-invaders-0.1.0.zip
  - Click 'Publish release'.
Do NOT mark as pre-release unless smoke-test failed.
10. [manual] Post-release verification - Download e2e-space-invaders-0.1.0.zip from the published GitHub Release, unzip to a fresh directory, and open index.html via file:// to confirm the shipped artefact is playable end-to-end. Verify the GitHub Release page shows the correct tag (v0.1.0), correct title, and the attached zip.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Pre-flight check + artefact packager: verifies all required game files are present in the repo root, then creates e2e-space-invaders-0.1.0.zip containing exactly those files. Run this BEFORE pushing the git tag and uploading to GitHub Releases.
- **release** `release/scripts/release.sh` (sh) -- Pre-flight check + artefact packager for e2e Space Invaders 0.1.0. Verifies all required game files exist in the repo root, then creates e2e-space-invaders-0.1.0.zip. Run from the repository root before pushing the v0.1.0 tag.
- **release** `release/scripts/release.ps1` (powershell) -- Pre-flight check + artefact packager for e2e Space Invaders 0.1.0. Verifies all required game files exist in the repo root, then creates e2e-space-invaders-0.1.0.zip. Run from the repository root before pushing the v0.1.0 tag.
- **run** `release/scripts/run.py` (python) -- Serves the game locally over HTTP on port 8080 so it can also be tested via http:// (in addition to file://). Opens the browser automatically. Run from the repository root at any time to play or verify the build.
- **run** `release/scripts/run.sh` (sh) -- Serves the game locally over HTTP on port 8080 using Python's built-in server. Run from the repository root to play or verify the build over HTTP. The game also works via file:// without this script.
- **run** `release/scripts/run.ps1` (powershell) -- Serves the game locally over HTTP on port 8080 using Python's built-in server. Run from the repository root to play or verify the build over HTTP. The game also works via file:// without this script.

## Rollback

1. This is the initial release (v0.1.0); there is no previous production version to roll back to.
2. If a critical defect is found post-publish: unpublish or retract the GitHub Release (mark as pre-release / draft) to prevent new users from downloading the broken artefact.
3. Delete the local checkout and re-clone from the commit immediately before tag `v0.1.0` (commit `5fe68c8 chore: reset repo`) if a hotfix branch is required — do NOT force-push or delete the tag from the remote.
4. Open a new ticket in the Inbox lane describing the defect; cut a patch release (v0.1.1) from a hotfix branch rather than altering the v0.1.0 tag.
5. The game has no server-side state, database, or deployed backend — there is nothing else to roll back.

