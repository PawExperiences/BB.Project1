# e2e space invaders 0.1.0 (0.1.0)

## e2e Space Invaders v0.1.0

This is the **first playable release** of *e2e Space Invaders* — a browser-based recreation of the arcade classic built entirely with vanilla ES modules and the Canvas 2D API. No npm, no bundler, no server: open `index.html` from a `file://` URL in any modern browser and play.

### Highlights
- **Full game scaffold** — fixed-timestep 60 fps loop, scene state machine (Title / Playing / Game Over), canvas HUD (score, hi-score, lives, level).
- **Responsive player ship** — delta-time movement, left/right clamping, classic single-bullet constraint.
- **Classic invader grid** — 11×5 formation with step-and-drop edge detection, AABB collision, kill explosions, and score tracking.
- **Level 1** — progressive speed scaling (800 ms → 100 ms as invaders die), lose/win conditions, HUD level indicator.
- **Level 2** — invaders shoot back (bottom-row fire), 1.4× faster grid, UFO bonus (50/100/150/300 pts by shot count), 2-second hit-invulnerability window with player blink.
- **Zero external dependencies** — pure HTML + ES modules, works offline from the filesystem.

## Highlights

- Complete Space Invaders game playable directly from index.html via file:// — no install, no server, no bundler.
- Fixed-timestep 60 fps game loop with scene state machine (Title → Playing → Game Over) and canvas HUD.
- Classic 11×5 invader grid with step-and-drop movement, AABB collision detection, kill explosions, and score tracking.
- Level 1: progressive speed scaling from 800 ms down to 100 ms per step as invaders are destroyed.
- Level 2: invader return fire from bottom-row shooters, 1.4× faster grid, UFO bonus scoring, and 2-second hit-invulnerability respawn window.
- 100% vanilla ES modules — zero npm packages, zero external dependencies, fully offline-capable.

## Changelog

# Changelog

## [0.1.0] - Initial Release

### Added
- `index.html` — HTML entry point hosting a 768×896 `<canvas id="gameCanvas">` centred on a black background; loads `game.js` as an ES module; works from `file://` with no server or bundler.
- `gameConfig.js` — Shared configuration constants: `CANVAS_WIDTH=768`, `CANVAS_HEIGHT=896`, `PLAYER_SPEED=200`, `BULLET_SPEED=500`, `STARTING_LIVES=3`, `POINTS_PER_KILL`, `UFO_SPEED`, and related level-tuning values.
- `game.js` — Main game module: fixed-timestep loop at 60 steps/s (`requestAnimationFrame`), delta cap at 5 steps, scene state machine (`TITLE` → `PLAYING` → `GAME_OVER`), canvas HUD renderer, exported `hudState` (`score`, `lives`, `hiScore`, `level`) and `SCENES` constant, `startLevel(n)` dispatcher.
- `input.js` — Keyboard input module: `initInput()` attaches `keydown`/`keyup` listeners; `isKeyHeld(key)` returns stable held-key state with no key-repeat flicker.
- `player.js` — `Player` class: delta-time movement at 200 px/s, left/right clamping, single-bullet constraint at 500 px/s, procedural canvas drawing (arcs + rectangles), `lives` property, hit-invulnerability blinking window for Level 2.
- `invaders.js` — Invader grid: 11×5 formation, step-and-drop movement, edge detection, destruction tracking, explosion effect (~150 ms flash), invader return-fire (bottom-row eligible shooters), UFO sprite with shot-count-based scoring (`playerShotCount % 4` → 50/100/150/300 pts).
- `collision.js` — AABB collision detection: bullet-vs-invader, invader-bullet-vs-player; collision pass runs before render; accepts empty invader-bullet arrays safely.
- `level1.js` — Level 1 logic: 55-invader formation, speed scaling (`100 + (aliveCount-1)*(700/54)` ms interval), 24 px drop on edge, lose condition (invaders reach player top edge), win condition calls `startLevel(2)`, HUD level indicator.
- `level2.js` — Level 2 logic: seamless transition from Level 1, lives carry over, 1.4× faster base movement, invader return fire with randomised intervals, UFO mechanic, hit invulnerability window (~2 s blinking), advances to Level 3 on clear.
- `README.md` — Project overview, canonical file-layout table, `file://` run instructions, manual verification steps for Title → Playing → Game Over → Title flow and level mechanics.
- CI build workflow (`.github/workflows/build.yml`) scaffolded by BuildBoard.

## Release steps

1. [manual] Verify CI build workflow currency - Open `.github/workflows/build.yml` and confirm it matches the current toolchain for this release. This project is plain static HTML + ES modules with no build step. The workflow should either be a no-op build (artifact = repository files) or a simple zip/copy of the source files. If the build command, toolchain, or artifact path drifted from what BuildBoard last scaffolded, update the build resource config in BuildBoard so the workflow re-scaffolds to match. This MUST be done before tagging.
2. [manual] Confirm all bundled tasks are in the Done lane - On the BuildBoard kanban, verify all five tasks are in the Done lane: 'Game loop and canvas framework', 'Keyboard input and the player ship', 'Sprite rendering and collision detection', 'Level 1: the classic grid', 'Level 2: they shoot back'. If any are not Done, resolve them before proceeding.
3. [automated] Pull latest main and verify clean working tree - Run: `git checkout main && git pull origin main && git status`. Confirm working tree is clean and HEAD is at commit 5a5d7d0 (or newer if hotfixes were applied). No uncommitted changes should be present.
4. [manual] Run manual smoke test in browser - Open `index.html` directly from the filesystem (`file:///path/to/repo/index.html`) in Chrome, Firefox, or Safari. Walk through: Title screen → Enter → Playing (HUD visible, 55 invaders) → shoot invaders → Level 2 transition → UFO → Game Over → Enter → Title. Confirm zero console errors throughout.
5. [manual] Draft and review CHANGELOG and release notes - Review the changelog and release notes sections of this runbook. Confirm they accurately reflect all five bundled tasks and the git diff. Make any wording corrections directly in the release artefact before publishing.
6. [automated] Create and push the release tag - Run the release script: `python release/scripts/release.py` (or `sh release/scripts/release.sh` / `pwsh release/scripts/release.ps1`). The script creates an annotated git tag `v0.1.0` on HEAD and pushes it to origin. Verify the tag appears on GitHub at https://github.com/PawExperiences/BB.Project1/tags.
7. [manual] Create GitHub Release from tag - Navigate to https://github.com/PawExperiences/BB.Project1/releases/new. Select tag `v0.1.0`. Set release title to 'e2e Space Invaders v0.1.0'. Paste the releaseNotes markdown from this runbook into the description field. Attach the release artifact ZIP (produced by the release script, `release/e2e-space-invaders-0.1.0.zip`). Publish the release.
8. [automated] Upload release artifact ZIP - The release script packages all game source files (`index.html`, `game.js`, `gameConfig.js`, `input.js`, `player.js`, `invaders.js`, `collision.js`, `level1.js`, `level2.js`, `README.md`) into `release/e2e-space-invaders-0.1.0.zip`. Attach this ZIP to the GitHub Release (step above). The ZIP must be self-contained and openable by unzipping then opening `index.html` from `file://`.
9. [manual] Verify GitHub Release is publicly visible - Open https://github.com/PawExperiences/BB.Project1/releases/tag/v0.1.0 in a browser (incognito window recommended). Confirm the release title, notes, and ZIP artifact are all visible without authentication.
10. [manual] Post-release smoke test from artifact - Download the published ZIP from the GitHub Release page. Unzip it. Open `index.html` from the unzipped folder via `file://`. Confirm the game runs identically to the pre-release smoke test. This validates the artifact is complete and self-contained.
11. [manual] Update BuildBoard release record - In BuildBoard, mark the release 'e2e Space Invaders 0.1.0' as shipped and record the GitHub Release URL. All five bundled tasks should already be in Done; confirm no tickets were accidentally left in Review or Testing.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Creates an annotated git tag v0.1.0 on the current HEAD, pushes it to origin, and packages all game source files into release/e2e-space-invaders-0.1.0.zip. Run once from the repo root immediately before publishing the GitHub Release.
- **release** `release/scripts/release.sh` (sh) -- Creates an annotated git tag v0.1.0 on the current HEAD, pushes it to origin, and packages all game source files into release/e2e-space-invaders-0.1.0.zip. Run once from the repo root immediately before publishing the GitHub Release.
- **release** `release/scripts/release.ps1` (powershell) -- Creates an annotated git tag v0.1.0 on the current HEAD, pushes it to origin, and packages all game source files into release/e2e-space-invaders-0.1.0.zip. Run once from the repo root immediately before publishing the GitHub Release.
- **run** `release/scripts/run.py` (python) -- Starts a minimal local HTTP server on port 8080 serving the repository root and opens index.html in the default browser. Use this as an alternative to the file:// method when the browser blocks ES module imports from file:// (e.g. Chrome with --allow-file-access-from-files not set). Run any time after checkout.
- **run** `release/scripts/run.sh` (sh) -- Starts a minimal local HTTP server on port 8080 serving the repository root and prints the URL. Use this as an alternative to the file:// method when the browser blocks ES module imports from file://. Run any time after checkout.
- **run** `release/scripts/run.ps1` (powershell) -- Starts a minimal local HTTP server on port 8080 serving the repository root and opens index.html in the default browser. Use this as an alternative to the file:// method when the browser blocks ES module imports from file://. Run any time after checkout.

## Rollback

1. Because this is the initial release (no previous tag), a rollback means removing the v0.1.0 GitHub Release (mark it as draft or delete it via the GitHub UI — do NOT force-push or delete the tag via git; use the GitHub Releases UI only).
2. Communicate to users that the release has been retracted and that the game is temporarily unavailable.
3. If a critical bug is found, create a patch branch from the `v0.1.0` tag (`git checkout -b hotfix/0.1.1 v0.1.0`), apply the minimal fix, and release `v0.1.1` following this same runbook.
4. No database migrations exist in this release, so there is no data-layer rollback required.

