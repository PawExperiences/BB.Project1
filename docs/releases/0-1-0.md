# e2e space invaders 0.1.0 (0.1.0)

## e2e Space Invaders v0.1.0

This is the **initial release** of *e2e Space Invaders* — a complete, four-level, browser-native Space Invaders clone built as vanilla ES modules with zero external dependencies or build tools. The game runs directly from `index.html` over a `file://` URL in any modern browser.

### Highlights

- **Full game loop**: fixed 60 Hz timestep with delta-cap, three-scene state machine (Title / Playing / Game Over), and a canvas HUD showing score, lives, and hi-score.
- **Responsive controls**: frame-rate-independent keyboard input (arrow keys + WASD + Space), procedurally-drawn player ship, single-bullet constraint.
- **Classic invader grid**: 11×5 formation with AABB collision, score tracking, and a brief explosion effect on kill.
- **Four levels**: Level 1 (accelerating march), Level 2 (return fire + UFO bonus with deterministic score tiers, player respawn/invulnerability), Level 3 (destructible shield bunkers + mid-level formation split), and a Boss Level (10 HP, two firing phases, three-bullet spread, sudden death).
- **Green CI**: GitHub Actions workflow validates `index.html` presence with a plain shell check — no npm, no bundler, no server required.

## Highlights

- Zero-dependency vanilla ES modules — opens directly from file:// in Chrome/Firefox, no server or build step needed.
- Four complete levels: accelerating invader march → return fire + UFO bonus → destructible shields + formation split → multi-phase boss fight.
- Fixed-timestep game loop (60 Hz, delta-cap) with scene state machine (Title / Playing / Game Over) and canvas HUD.
- Deterministic UFO score tiers keyed on cumulative shot count (50/100/150/300) — no RNG in scoring.
- Destructible 4×4 shield bunkers that erode cell-by-cell from player bullets, invader bullets, and descending invaders.
- Green GitHub Actions CI: plain shell validation step — no npm, no node, no bundler drift.

## Changelog

# Changelog

## [0.1.0] - Initial Release

### Added
- **CI / Workflow**: Fixed `.github/workflows/build.yml` to use a plain shell `test -f index.html` validation step; removed all npm/node/bundler invocations that are inappropriate for a no-build-tool stack.
- **Game Loop & Canvas Framework** (`index.html`, `game.js`, `gameConfig.js`): Established the foundational scaffolding — 768×896 canvas, fixed-timestep game loop at 60 Hz with delta-cap, three-scene state machine (Title → Playing → Game Over), canvas HUD (score, lives, hi-score), and exported `hudState` object.
- **Keyboard Input & Player Ship** (`input.js`, `player.js`): Thin key-state tracker (`initInput`, `isKeyHeld`); `Player` class with delta-time movement (200 px/s), canvas-edge clamping, single-bullet constraint (500 px/s upward), procedural ship rendering, and `lives` counter sourced from `gameConfig.js`.
- **Sprite Rendering & Collision Detection** (`invaders.js`, `collision.js`): 11×5 invader grid (55 invaders, 32×24 px each, 16 px horizontal / 12 px vertical gaps), unit formation movement with edge-detect drop, AABB collision (player bullet vs. invader → score increment + explosion effect; invader bullet vs. player → console stub), score tracking.
- **Level 1 — The Classic Grid** (`level1.js`): Formation march with linearly-scaling step interval (800 ms → 100 ms as invaders are destroyed), win/lose wiring (`advanceLevel` / `loseLife`), HUD level field update.
- **Level 2 — They Shoot Back** (`level2.js`, `shared/invaders.js`): Shared step-interval utility; Level 2 runs at 1.5× Level 1 speed (×0.67 multiplier); random invader shooting (800–2000 ms, lowest surviving invader per column, 300 px/s bullet); player hit/respawn with 2-second 10 Hz blink invulnerability; UFO bonus every 20 s alternating sides at 120 px/s with `cumulativeShotCount % 4` score tiers (50/100/150/300).
- **Level 3 — Shields & Formations** (`level3.js`): Four destructible 4×4 shield bunkers at ~80% canvas height; standard 11×5 grid; formation split at ≥28 kills (columns 1–6 left, 7–11 right) into two independent sweep halves; win transitions to Boss Level.
- **Boss Level — Multi-Phase Finale** (`boss.js`): 160×80 px procedurally-drawn boss, 10 HP with full-width health bar; three-bullet spread (straight, ±20°) at 260 px/s; Phase 1 fires every 1500 ms (HP 10–6), Phase 2 every 700 ms (HP ≤5); sudden-death on boss hit; win screen on HP=0; uses imported `collision.js`.
- **README.md**: Full file layout, manual verification checklists for every component and level.

## Release steps

1. [manual] Verify repository state and commit log - Confirm the HEAD of the main branch is commit `975d47d` (Boss level). Run: `git log --oneline -8` and cross-reference against the seven expected commits listed in this runbook. If HEAD differs, STOP and resolve before proceeding.
2. [manual] Confirm CI workflow is green - Open https://github.com/PawExperiences/BB.Project1/actions and verify the most recent workflow run on main/master completed with status 'success'. The `.github/workflows/build.yml` validation step must execute `test -f index.html` — confirm no npm/node/bundler invocations remain. If the run is red, fix the workflow before tagging.
3. [manual] Confirm CI build workflow matches current toolchain - Open `.github/workflows/build.yml` and verify: (a) the checkout step uses `actions/checkout@v3` or later; (b) the validation step is exactly `test -f index.html && echo "index.html found" || (echo "index.html missing" && exit 1)`; (c) there are NO `npm install`, `npm test`, `npm run`, `node`, or bundler commands. This project ships zero build tooling — the workflow must stay in sync. If any drift is found, open a PR to correct `.github/workflows/build.yml` before tagging.
4. [manual] Run manual smoke-test checklist - Follow every step in the manualSteps list above. Open `index.html` via `file://` in Chrome and Firefox. Verify all four levels, the boss fight, HUD values, collision behaviour, and ENTER transitions. Document pass/fail for each item. Do not proceed if any item fails.
5. [manual] Update CHANGELOG and version references - Ensure `README.md` (and any other doc files) reflects version `0.1.0`. Commit message: `docs: prepare v0.1.0 release notes`. Push to main branch. No `package.json` version field exists in this project; if one is introduced in future, update it here.
6. [automated] Create and push annotated release tag v0.1.0 - Run the release script: `python release/scripts/release.py` (or `.sh`/`.ps1` variant). The script creates an annotated git tag `v0.1.0` on the current HEAD and pushes it to origin. Command equivalent: `git tag -a v0.1.0 -m "Release v0.1.0 — e2e Space Invaders initial release" && git push origin v0.1.0`. DO NOT force-push or delete any existing tags.
7. [manual] Create GitHub Release from tag - Navigate to https://github.com/PawExperiences/BB.Project1/releases/new. Select tag `v0.1.0`. Set release title to `e2e Space Invaders v0.1.0`. Paste the contents of `releaseNotes` from this runbook into the release body. Mark as 'Latest release'. Attach the artifact (zip of project files) produced by the release script. Publish.
8. [automated] Attach release artifact (project zip) - The release script packages `index.html`, `game.js`, `gameConfig.js`, `input.js`, `player.js`, `invaders.js`, `collision.js`, `level1.js`, `level2.js`, `level3.js`, `boss.js`, `shared/invaders.js`, and `README.md` into `e2e-space-invaders-v0.1.0.zip`. This zip is the distributable artifact — users can unzip and open `index.html` directly. Upload to the GitHub Release in the previous step.
9. [manual] Post-release verification - Download the release artifact from the GitHub Releases page. Unzip to a local folder. Open `index.html` via `file://` URL. Run a abbreviated smoke test: Title scene visible, ENTER starts game, player moves, invaders appear, at least one level transitions correctly. Confirm no 404s or console errors.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Creates annotated git tag v0.1.0 on HEAD, pushes it to origin, and packages the distributable project files into e2e-space-invaders-v0.1.0.zip in the repo root. Run once from the repo root after all commits are on main and CI is green.
- **release** `release/scripts/release.sh` (sh) -- Creates annotated git tag v0.1.0 on HEAD, pushes it to origin, and packages the distributable project files into e2e-space-invaders-v0.1.0.zip in the repo root. Run once from the repository root after all commits are on main and CI is green.
- **release** `release/scripts/release.ps1` (powershell) -- Creates annotated git tag v0.1.0 on HEAD, pushes it to origin, and packages the distributable project files into e2e-space-invaders-v0.1.0.zip in the repo root. Run once from the repository root after all commits are on main and CI is green.
- **run** `release/scripts/run.py` (python) -- Starts a local HTTP server on port 8080 serving the repository root, then opens index.html in the default browser. Use this when file:// restrictions prevent ES module imports (e.g. Firefox strict mode); for a fully offline play the file:// URL works directly without this script.
- **run** `release/scripts/run.sh` (sh) -- Starts a local HTTP server on port 8080 serving the repository root, then opens index.html in the default browser. Use when file:// ES module loading is restricted; for a fully offline play the file:// URL works directly without this script.
- **run** `release/scripts/run.ps1` (powershell) -- Starts a local HTTP server on port 8080 serving the repository root, then opens index.html in the default browser. Use when file:// ES module loading is restricted; for a fully offline play the file:// URL works directly without this script.

## Rollback

1. Because this is the initial release (no previous tag exists) a conventional rollback to a prior version is not applicable.
2. If the release tag `v0.1.0` must be retired: open a PR that reverts all feature commits back to the `8266d81 chore: reset repo` baseline commit; do NOT delete the tag or any remote branches — create a new `v0.1.0-reverted` tag on the revert commit instead.
3. If only a specific level is broken: identify the commit that introduced it (see commit log above), open a targeted revert PR for that commit only, and cut a `v0.1.1` patch release.
4. End-users: simply close the browser tab; no server-side state, no database, no persistent data to roll back.

