# e2e unit converter 0.1.0 (0.1.0)

## units 0.1.0

Initial release of `units`, a small typed Python library for converting between common length and mass units. It exposes a single function, `convert(value, frm, to) -> float`, backed by two lookup tables of exact, standard conversion factors — no rounding, no per-unit-pair boilerplate to maintain.

### Highlights
- One public API: `convert(value, frm, to)` covers both length and mass conversions.
- Precise, unrounded results computed via base-unit lookup tables (`src/units/tables.py`: `LENGTH_FACTORS`, `MASS_FACTORS`).
- Clear, typed errors: `ValueError` (naming both units) for cross-dimension conversions, `KeyError` (naming the unit) for unsupported symbols.
- Automated test suite (`tests/test_convert.py`) covering both conversion directions and both error paths.
- Pure Python, zero runtime dependencies, Python 3.12+, packaged and built with `uv`.

See `README.md` in this tag for the authoritative list of supported unit symbols and three worked examples — the exact symbol spellings were still being reconciled across this release's source tasks, so treat the README shipped with this tag as ground truth over any individual ticket description.

## Highlights

- First public release of the `units` Python library for length and mass unit conversion.
- Single, typed public API: `convert(value, frm, to) -> float`.
- Base-unit lookup tables (`src/units/tables.py`) give exact, unrounded results with no per-unit-pair conversion functions to maintain.
- Explicit error handling: `ValueError` for cross-dimension conversions and `KeyError` for unsupported units, each naming the offending unit(s).
- Ships with an automated pytest suite and a README documenting every supported unit plus worked examples.
- Zero runtime dependencies; Python 3.12+, packaged and built with `uv`.

## Changelog

# Changelog

## [0.1.0] - 2026-08-11 (first release)

### Added
- New `units` Python package (`pyproject.toml`: `[project] name = "units"`, `version = "0.1.0"`, `requires-python = ">=3.12"`, plus a `[build-system]` table), installable/buildable with `uv`.
- `src/units/tables.py`: typed `LENGTH_FACTORS: dict[str, float]` and `MASS_FACTORS: dict[str, float]` — the single source of truth for conversion factors, each expressed relative to the dimension's base unit (metres for length, grams for mass; the base unit maps to `1.0`).
- `src/units/__init__.py`: public `convert(value: float, frm: str, to: str) -> float` API. Converts within a dimension via the base unit with no rounding; raises `ValueError` naming both units on a cross-dimension request (e.g. a length unit to a mass unit); raises `KeyError` naming the offending symbol for any unit absent from both tables. `LENGTH_FACTORS`/`MASS_FACTORS` are imported from `.tables` (not redefined) and re-exported as public API.
- `tests/test_convert.py`: automated coverage for a within-dimension conversion in both directions, a cross-dimension `ValueError`, and an unknown-unit `KeyError`. `python -m pytest` passes.
- `README.md`: supported units listed by dimension, plus three worked conversion examples.

### Flagged for release sign-off (not resolved by this runbook)
- Task #280 ("Length and mass conversions") acceptance criteria specify unit symbols `inch`/`mile`, require `convert(1, "yd", "m")` to raise `KeyError` (yd unsupported), and list exactly 11 README units (no `mg`). Task #279 ("The conversion tables") — which #280's `__init__.py` is required to import its factors from — instead defines `in`/`mi`, *includes* `yd` as a supported length unit, and adds `mg` as a mass unit (13 units total). These cannot both be true of the same shipped `convert()`. See the release step "Resolve the unit-symbol discrepancy" below.

## Release steps

1. [automated] Confirm the release branch and commit range - On `main`: `git fetch origin && git log --oneline -5` and confirm the tip contains `9ddff55 feat: Tests and the supported-unit list (#281)`, `ffbdd2a feat: Length and mass conversions (#280)`, `4cd4985 feat: The conversion tables (#279)`, on top of `622d162 chore: reset for the next e2e project`. Confirm `git status --porcelain` is empty.
2. [automated] Install dependencies and run the test suite - From the repo root: `uv sync` then `uv run pytest -q`. Must exit 0, covering tests/test_convert.py's within-dimension (both directions), cross-dimension ValueError, and unknown-unit KeyError cases.
3. [automated] Run lint and format checks - `uv run ruff check .` and `uv run ruff format --check .`; both must pass (required by task #279's acceptance criteria).
4. [automated] Build distribution artifacts - `uv build` from the repo root; produces `dist/units-0.1.0-py3-none-any.whl` and `dist/units-0.1.0.tar.gz` (exact filenames depend on the configured build backend in pyproject.toml).
5. [manual] Resolve the unit-symbol discrepancy before shipping the public API - Task #280's acceptance criteria specify symbols 'inch'/'mile' and require convert(1,'yd','m') to raise KeyError (11 README units, no 'mg'). Task #279's acceptance criteria define src/units/tables.py with 'in'/'mi', INCLUDE 'yd' as supported, and add 'mg' (13 units). Since #279 requires __init__.py to import its factors from tables.py rather than defining its own dict literals, tables.py's symbol set is the more likely source of truth, but this is not confirmed. Run e.g. `python -c "from units import convert; print(convert(1,'in','cm')); print(convert(1,'inch','cm'))"` (and similarly for mile/mi and yd) against the built package to see which symbols actually work, and confirm README.md matches. If mismatched, route back to grooming rather than resolving it here.
6. [manual] Confirm the CI build workflow matches this release's toolchain - `.github/workflows/build.yml` is owned and re-scaffolded by BuildBoard from the build resource's configured command + artifact_path. Confirm the configured build runs `uv sync` + `uv run pytest` against Python 3.12, and that artifact_path points at `dist/` (matching `uv build`'s output). This release did not change the build command, toolchain, or artifact layout, so no build-resource update should be needed — but confirm this explicitly. If it has drifted, update the build resource config (never hand-edit the workflow file) so it re-scaffolds correctly.
7. [automated] Tag the release - `git tag -a v0.1.0 -m 'units 0.1.0'` then `git push origin v0.1.0`. Additive only — never force-push or delete an existing tag.
8. [automated] Create the GitHub release - `gh release create v0.1.0 dist/* --title 'units 0.1.0' --notes-file release/notes/RELEASE_NOTES.md` (or paste this runbook's `releaseNotes` as the release body if the notes file isn't present).
9. [manual] Smoke-test the published artifact - In a clean virtualenv: `pip install dist/units-0.1.0-py3-none-any.whl` (or `uv pip install <wheel>`), then run `python -c "from units import convert; print(convert(1,'km','m'))"` plus the symbols resolved in the discrepancy step, confirming they match README.md's documented examples.
10. [manual] Announce the release - Share the GitHub release URL and release notes with the project's collaborators; confirm task cards #279, #280, #281 are in Done on the board.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Runs the full automated release for units 0.1.0: checks the tree is clean, runs `uv sync`, `pytest`, `ruff check`/`ruff format --check`, builds with `uv build`, tags `v0.1.0`, pushes the tag, and creates the GitHub release with the built artifacts via `gh`. Idempotent (skips tag/push/release creation if already done). Run this from the repo root once all manual sign-off steps above are complete.
- **release** `release/scripts/release.sh` (sh) -- POSIX sh equivalent of release.py: checks the tree is clean, runs `uv sync`, `pytest`, `ruff check`/`ruff format --check`, builds with `uv build`, tags `v0.1.0`, pushes the tag, and creates the GitHub release with the built artifacts via `gh`. Idempotent. Run this from anywhere; it locates the repo root relative to its own path.
- **release** `release/scripts/release.ps1` (powershell) -- Windows PowerShell 5.1-compatible equivalent of release.py/release.sh: checks the tree is clean, runs `uv sync`, `pytest`, `ruff check`/`ruff format --check`, builds with `uv build`, tags `v0.1.0`, pushes the tag, and creates the GitHub release with the built artifacts via `gh`. Idempotent. Run this from anywhere; it locates the repo root relative to its own path.

## Rollback

This is the project's first release, so there is no prior version to fall back to. If v0.1.0 is found to be broken after publishing: (1) never delete or force-push over the v0.1.0 tag or GitHub release — removing remote history/releases is prohibited; (2) edit the GitHub release description to add a visible 'known issue' notice and mark it as a pre-release so it is not picked up as the recommended download; (3) cut a forward-fix patch release (e.g. v0.1.1) with the correction, using the same release/scripts/release.* script with VERSION updated; (4) point consumers at v0.1.1+ as the recommended version. Because convert() has no prior external consumers, there is no data/schema migration or consumer-side rollback burden — the safest 'rollback' is simply to stop recommending v0.1.0 and ship the fix quickly.

