# e2e space invaders 0.1.0 (0.1.0)

## e2e Space Invaders v0.1.0

This is the first playable release of **e2e Space Invaders** — a pure-browser, zero-dependency Space Invaders clone built with vanilla ES modules and the HTML5 Canvas API. Open `index.html` directly from your filesystem (no server, no bundler, no npm) and play through four handcrafted levels to a multi-phase boss finale.

### Highlights

- **Full four-level arc** — classic invader grid (Level 1) → enemies that shoot back with UFO bonuses (Level 2) → destructible shields and a dramatic formation split (Level 3) → a two-phase boss with sudden-death stakes (Level 4).
- **Fixed-timestep game loop** with delta capping so background-tabbing never causes burst updates.
- **Procedural rendering** throughout — every entity (player ship, invaders, boss, HUD, health bar, explosions) is drawn with canvas 2D primitives; no image assets required.
- **Progressive difficulty** — invader step interval scales linearly with remaining count; Level 2 runs 33% faster; Level 3 splits the formation at 50% casualties; the boss doubles its fire rate at half HP.
- **Zero external dependencies** — pure ES modules, works at `file://`, CI-verified on every push.

## Highlights

- First fully playable end-to-end build: Title → Level 1 → Level 2 → Level 3 → Boss → Win screen, no page reloads
- Fixed-timestep game loop with delta capping prevents tab-background burst (MAX_ACCUMULATED_DELTA guard)
- Destructible shield bunkers in Level 3 — cell-level erosion by bullets and descending invaders
- Formation split mechanic at 50% casualties: two halves sweep and accelerate independently
- Two-phase boss with spread shots, per-hit health bar update, and sudden-death rule
- Zero dependencies — pure ES modules, opens from file:// URL, CI build green

## Changelog

# Changelog

## [0.1.0] - Initial Release

### Added
- **Game Loop & Canvas Framework** (`index.html`, `game.js`, `gameConfig.js`): 768×896 canvas on a dark background, fixed-timestep game loop (1/60 s steps, delta capped to `MAX_ACCUMULATED_DELTA`), scene state machine (Title → Playing → Game Over), HUD rendering (score, lives, hi-score), ENTER-key scene transitions, no page reloads.
- **Shared Config** (`gameConfig.js`): Named exports `CANVAS_WIDTH=768`, `CANVAS_HEIGHT=896`, `PLAYER_SPEED=200`, `BULLET_SPEED=500`, `STARTING_LIVES=3`, `TARGET_FPS=60`, `MAX_ACCUMULATED_DELTA`.
- **Shared Constants** (`constants.js`): Grid geometry and sprite ID constants consumed by level modules.
- **Keyboard Input** (`input.js`): `initInput()` / `isKeyHeld(code)` API; physical press/release tracking via `KeyboardEvent.code`; no key-repeat pollution.
- **Player Ship** (`player.js`): Delta-time movement (ArrowLeft/KeyA, ArrowRight/KeyD), edge clamping, single-bullet firing (Space), upward bullet travel, procedural canvas-primitive rendering.
- **Invader Formation & Collision** (`invaders.js`, `collision.js`): 11×5 invader grid, sideways sweep with drop-and-reverse on edge contact, AABB bullet-vs-invader and invader-bullet-vs-player collision passes, running `score` export, timed explosion effect renderer.
- **Level 1** (`level1.js`): Classic 11×5 grid, linearly scaling step interval (800 ms → 100 ms as invaders are killed), win/lose Promise resolution, "Level 1" HUD label.
- **Level 2** (`level2.js`): 1.5× faster formation (0.67× interval multiplier), enemy shooting from lowest invader per column (800–2000 ms random timer, 300 px/s bullets), UFO bonus every 20 s (alternating sides, 120 px/s, score tier by `shotCount % 4`), player hit/respawn with 2 s flashing invulnerability, SceneManager transitions to Level 3 or Game Over.
- **Level 3** (`level3.js`): Same 11×5 grid with standard speed-up; four destructible shield bunkers (4×4 cell grids at 80% canvas height); formation split at ≥50% casualties into two independently sweeping/accelerating halves; level-completion signal to boss.
- **Boss Level** (`boss.js`): 160×80 px procedural boss, horizontal drift at 90 px/s, 10 HP health bar, two-phase spread shot (1500 ms / 700 ms at ≤5 HP, 260 px/s, ±20° spread), sudden-death on player hit, win screen with score and restart.
- **CI Workflow Fix** (`.github/workflows/build.yml`): Corrected `index.html` existence check; all original checkout + validate steps preserved and passing.
- **README** (`README.md`): Full planned file layout, manual verification checklist, tab-background burst prevention notes.

## Release steps

1. [manual] Confirm all bundled tasks are in the Done lane on the kanban board - Visually verify that the following tickets are in Done: 'Game loop and canvas framework', 'Keyboard input and the player ship', 'Sprite rendering and collision detection', 'Level 1: the classic grid', 'Level 2: they shoot back', 'Level 3: shields and formations', 'Boss level: multi-phase finale', 'CI build failed'. Do not proceed if any ticket is not Done.
2. [manual] Verify the main branch is green in CI - Navigate to https://github.com/PawExperiences/BB.Project1/actions and confirm the latest workflow run on the `main` branch for `.github/workflows/build.yml` shows a green (passing) status. If it is red, do not proceed — investigate and fix the workflow first (the 'CI build failed' task should have resolved this).
3. [manual] Confirm CI build workflow matches the current toolchain - Open `.github/workflows/build.yml` in the repository. Verify that:
- The `actions/checkout` step uses a current, pinned version (e.g. `actions/checkout@v4`).
- The validation step checks for the existence of `index.html` at the repository root using a correct shell command (e.g. `test -f index.html` with `shell: bash`).
- No build command, artifact path, or toolchain reference has drifted from what this release actually ships (pure static ES modules, no bundler, no npm — the 'build command' is the existence check itself).
If any drift is found, open a PR to correct the workflow configuration before tagging. This step is REQUIRED per release policy.
4. [manual] Run the full manual verification checklist from README.md - Open `index.html` via a `file://` URL in both Chrome and Firefox. Work through every item in the README.md manual verification checklist:
1. Title screen renders correctly, ENTER transitions to Playing.
2. HUD visible, game area dark.
3. Level 1 clears, auto-advances to Level 2.
4. Level 2 enemy fire, UFO, and invulnerability all function.
5. Level 3 bunkers erode, formation splits at ~50% casualties.
6. Boss drifts, health bar updates, phase transition at HP=5, sudden-death fires.
7. Win screen shows score and restarts cleanly.
8. Game Over scene resets correctly.
9. Tab-background burst does not occur.
Document pass/fail for each item. Do not proceed if any item fails.
5. [automated] Create and push the v0.1.0 release tag - Run the release script (see scripts section) which executes:
```
git checkout main
git pull origin main
git tag -a v0.1.0 -m "Release v0.1.0 — e2e Space Invaders initial release"
git push origin v0.1.0
```
The script is idempotent: if the tag already exists locally it will not re-create it; if it already exists on remote the push will be a no-op (GitHub will reject a duplicate tag push gracefully).
6. [automated] Package the release artifact (zip of static files) - The release script produces `release/e2e-space-invaders-0.1.0.zip` containing:
`index.html`, `game.js`, `gameConfig.js`, `constants.js`, `input.js`, `player.js`, `invaders.js`, `collision.js`, `level1.js`, `level2.js`, `level3.js`, `boss.js`, `README.md`
The zip is created from the root of the checked-out `main` branch. No `node_modules`, no `.git`, no `.github` directory is included.
7. [automated] Create the GitHub Release and upload the artifact - Using the GitHub CLI (`gh`), the release script runs:
```
gh release create v0.1.0 \
  --title "e2e Space Invaders 0.1.0" \
  --notes-file release/RELEASE_NOTES.md \
  release/e2e-space-invaders-0.1.0.zip
```
This creates the GitHub Release at tag `v0.1.0` and uploads the zip artifact. Requires `GH_TOKEN` environment variable to be set with a PAT that has `contents: write` scope. The `release/RELEASE_NOTES.md` file is the `releaseNotes` field above, written to disk by the script before this step.
8. [manual] Verify the GitHub Release page - Navigate to https://github.com/PawExperiences/BB.Project1/releases/tag/v0.1.0 and confirm:
- Release title is 'e2e Space Invaders 0.1.0'.
- Release notes body is correct.
- The zip artifact `e2e-space-invaders-0.1.0.zip` is listed and downloadable.
- Download the zip, unzip it, open `index.html` from the extracted folder via `file://`, and confirm the game loads correctly.
9. [manual] Post-release smoke test - Download the published artifact from the GitHub Release page (not from the repo directly). Extract and open `index.html` in an incognito/private browser window with no prior state. Play through to Level 1 completion to confirm the shipped artifact is self-contained and functional.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Tags the current HEAD of main as v0.1.0, packages the static game files into a zip artifact, writes the release notes to disk, and creates the GitHub Release with the artifact uploaded — run once on a clean main branch with GH_TOKEN set.
- **release** `release/scripts/release.sh` (sh) -- Tags the current HEAD of main as v0.1.0, packages the static game files into a zip artifact, writes the release notes to disk, and creates the GitHub Release with the artifact uploaded — run once on a clean main branch with GH_TOKEN set.
- **release** `release/scripts/release.ps1` (powershell) -- Tags the current HEAD of main as v0.1.0, packages the static game files into a zip artifact, writes the release notes to disk, and creates the GitHub Release with the artifact uploaded — run once on a clean main branch with GH_TOKEN set.
- **run** `release/scripts/run.py` (python) -- Starts a local HTTP server (Python's built-in http.server) serving the repository root on port 8080, then opens index.html in the default browser — use this when your browser blocks ES module imports under a file:// URL.
- **run** `release/scripts/run.sh` (sh) -- Starts a local HTTP server (Python's built-in http.server) serving the repository root on port 8080, then prints the URL — use this when your browser blocks ES module imports under a file:// URL.
- **run** `release/scripts/run.ps1` (powershell) -- Starts a local HTTP server (Python's built-in http.server) serving the repository root on port 8080, then opens index.html in the default browser — use this when your browser blocks ES module imports under a file:// URL.

## Rollback

1. Identify the previous release tag. For v0.1.0 this is the initial release so there is no prior tag to roll back to; the rollback target is an empty/reset repository state.
2. If a hotfix forward is not feasible: communicate downtime to users; v0.1.0 is a client-side static release with no backend or database, so 'rollback' means replacing the served files.
3. To revert the GitHub Release artifact: do NOT delete the release. Instead, open a new patch release (v0.1.1) that reverts the offending commits via `git revert` and creates a new artifact.
4. If the CI workflow itself is broken: revert `.github/workflows/build.yml` to the last known-good state via a new PR (additive revert commit — no force-push).
5. Since there are no server deployments, databases, or migrations in this release, no data rollback is required.
6. Communicate status via the project's issue tracker.

