# e2e link checker 0.1.0 (0.1.0)

## e2e link checker 0.1.0

First release of `linkcheck`, a small, dependency-free, offline Markdown link checker you can drop into any repo or CI pipeline. Point it at a single Markdown file and it flags structurally broken link targets — empty targets, unsupported URI schemes, whitespace in the target, and root-relative (`/...`) paths — by fixed lexical rules only, with no network calls and no assumption of a site root. It reports one diagnostic per broken link and communicates pass/fail through its exit code, so it drops straight into a CI gate.

### Highlights
- Install with `pip install .`; run as `linkcheck <file>`.
- Detects broken inline links (`[text](target)`) and reference-style definitions (`[id]: target`), with 1-based line numbers.
- Deterministic `<line>:<target>: <reason>` output on stdout, one line per broken link, in document order — safe to pipe into other tooling.
- Stable exit-code contract for CI: `0` clean, `1` broken links found, `2` usage error.
- A single stderr-only summary line (files scanned / links found / broken links) that never pollutes stdout.
- Test suite pinned against fixture documents covering both the broken and the fully-clean case; no live network access anywhere in the tool or its tests.

See the [Changelog](#) for the full list of additions and `README.md` in this release for the complete usage, classification-rule, and exit-code reference.

## Highlights

- New `linkcheck` CLI: `pip install .` then `linkcheck <file>` to scan a Markdown file for broken link targets, offline and dependency-free.
- Fixed, deterministic classification rules: empty target, non-http(s)/mailto scheme, whitespace in target, or root-relative `/` path.
- One diagnostic line per broken link on stdout (`<line>:<target>: <reason>`), in ascending line-number order.
- Stable exit-code contract for CI gating: 0 clean / 1 broken links found / 2 usage error.
- Stderr-only summary line (files/links/broken counts) keeps stdout machine-parseable.
- Test suite over fixed fixture documents (`tests/fixtures/sample.md`, `tests/fixtures/clean.md`) pins the exact line-number and exit-code behaviour.

## Changelog

# Changelog

All notable changes to `linkcheck` are documented in this file.

## [0.1.0] - 2026-08-15
### Added
- `linkcheck` Python package skeleton (`pyproject.toml`, `src/linkcheck/__init__.py`, `src/linkcheck/cli.py`) declaring project name `linkcheck`, version `0.1.0`, and a `[project.scripts]` entry point so `linkcheck` is on PATH after `pip install .`.
- CLI usage `linkcheck <file>`: scans a single Markdown file for inline links (`[text](target)`) and reference-style link definitions (`[id]: target`), tracking the 1-based source line number of each occurrence.
- Fixed lexical broken-link rules (no network access, no site root): a target is BROKEN if it is empty, uses a scheme other than `http`, `https`, or `mailto`, contains whitespace, or starts with `/`.
- Deterministic stdout diagnostics, one line per broken link in ascending line-number order, in the exact format `<line>:<target>: <reason>`.
- Stable exit-status contract: `0` clean scan, `1` one or more broken links found, `2` usage error (bad/missing argument or unreadable file) — usage errors do not print any broken-link lines.
- A single stderr-only summary line per scan reporting files scanned, links found, and broken-link count, keeping stdout reserved for machine-parseable diagnostics.
- `README.md` documenting installation, `linkcheck <file>` usage, the classification rules, the output line format, and the 0/1/2 exit-code contract.
- Test suite `tests/test_linkcheck.py` against fixtures `tests/fixtures/sample.md` (one instance each of a good http(s) link, a good `mailto:` link, a good reference definition, an empty target, an `ftp://` target, a target with a space, and a non-existent `/absolute` path) and `tests/fixtures/clean.md` (all well-formed, resolvable links), asserting exact reported line numbers and exit codes 1 / 0 respectively. No test performs live network requests.

### Changed
- N/A — initial release.

### Fixed
- N/A — initial release.

## Release steps

1. [manual] Verify HEAD actually contains the shipped package (resolve the diffstat anomaly) - The diffstat supplied for this release shows exactly 318 deleted lines across README.md, pyproject.toml, src/linkcheck/__init__.py, src/linkcheck/cli.py, tests/fixtures/clean.md, tests/fixtures/sample.md, and tests/test_linkcheck.py, with zero additions — and no other files. That total exactly matches the size of everything the three 'feat' commits (d13d096, 3768875, a997366) should have ADDED, which is inconsistent with them being net-new scaffolding. The most likely explanation is that the diff range was computed back-to-front against the base commit 7f0d94e ('chore: reset for the next e2e project'), which is benign. But before tagging, a human must confirm on the actual release-branch HEAD (not from this stat alone) that `git show HEAD:src/linkcheck/cli.py`, `git show HEAD:pyproject.toml`, and `git show HEAD:README.md` all return real content, and that `tests/` exists. If HEAD is genuinely empty of these files, STOP — do not proceed to tagging; escalate to the upstream agent/human that scoped this release, since a v0.1.0 tag on an empty tree would ship nothing.
2. [manual] Confirm the CI build workflow matches this release's toolchain - `.github/workflows/build.yml` is owned and re-scaffolded by BuildBoard from the build resource's `command` + `artifact_path`, not hand-edited. Confirm the build resource's `command` runs this Python toolchain end to end (e.g. `pip install . && python -m pytest`) and that `artifact_path` reflects what this release actually produces (no packaged binary is required by this release's ACs — only `pip install .` and console-script availability — so `artifact_path` may legitimately be left unset, but confirm that intentionally). Since PREVIOUS is None, this may be the first time these settings are configured for `linkcheck`; if so, set them now so the workflow regenerates correctly, then re-check the regenerated `.github/workflows/build.yml` reflects it before relying on CI as a release gate.
3. [automated] Run the test suite and a clean-install check as a release gate - From the repo root on the exact commit being released: `python -m pytest` (must pass with no failures per the 'Tests over a fixture document' AC), then in a throwaway virtualenv `pip install .` followed by `linkcheck --help`-equivalent smoke check (e.g. `linkcheck tests/fixtures/clean.md`, expect exit 0) to confirm the console-script entry point resolves on PATH. These are the first two actions `release/scripts/release.py` (and its `.sh`/`.ps1` siblings) perform automatically.
4. [manual] Sign off on release notes and stage RELEASE_NOTES.md - A human reviews the drafted changelog and release notes in this runbook for accuracy against the bundled tasks and the verified HEAD contents (step 1). Once approved, save the `releaseNotes` markdown to `RELEASE_NOTES.md` at the repo root — `release/scripts/release.*` reads this file (override via `RELEASE_NOTES_FILE` env var) to populate the GitHub release body.
5. [automated] Tag, push, and publish the GitHub release - From the repo root, with `gh` authenticated (`gh auth status`) and `RELEASE_NOTES.md` present: run `python release/scripts/release.py` (or `sh release/scripts/release.sh` / `pwsh release/scripts/release.ps1`) with `RELEASE_VERSION=0.1.0` (default) and optionally `RELEASE_TITLE="e2e link checker 0.1.0"`. It re-runs the pytest + install gate, creates annotated tag `v0.1.0` on HEAD if it doesn't already exist, runs `git push origin v0.1.0`, and runs `gh release create v0.1.0 --title "e2e link checker 0.1.0" --notes-file RELEASE_NOTES.md` if the release doesn't already exist. GitHub auto-attaches source-code zip/tar.gz archives to the release; no separate artifact build step is required by this release's scope.
6. [automated] Smoke-test the published tag - In a clean virtualenv: `pip install "git+https://github.com/PawExperiences/BB.Project1.git@v0.1.0"`, then run `python release/scripts/run.py tests/fixtures/sample.md` (expect exit code 1, broken-link lines printed) and `python release/scripts/run.py tests/fixtures/clean.md` (expect exit code 0, no broken-link lines) — or the `.sh`/`.ps1` equivalents — to confirm the tagged artifact behaves per the exit-code contract end to end.
7. [manual] Verify the GitHub Release page - Open the `v0.1.0` release on GitHub and confirm: the release notes render as intended, the tag points at the commit verified in step 1, and the auto-generated 'Source code (zip)' / 'Source code (tar.gz)' assets are present and downloadable.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Idempotent: runs `pytest` and a clean `pip install .` as a gate, then tags the current commit `vRELEASE_VERSION` (skips if the tag already exists), pushes the tag to origin, and publishes a GitHub release from RELEASE_NOTES_FILE via `gh release create` (skips if the release already exists). Run from the repo root after the pre-tag checks in the runbook (HEAD verified, CI toolchain confirmed, notes signed off) are done. Requires `git`, `gh` (authenticated), and Python on PATH.
- **release** `release/scripts/release.sh` (sh) -- POSIX-sh equivalent of release.py: idempotent test + clean-install gate, then tags HEAD as vRELEASE_VERSION, pushes the tag, and publishes a GitHub release from RELEASE_NOTES_FILE. Run from the repo root once the pre-tag checks in the runbook are done. Requires git, gh (authenticated), and python3 on PATH.
- **release** `release/scripts/release.ps1` (powershell) -- PowerShell equivalent of release.py: idempotent test + clean-install gate, then tags HEAD as vRELEASE_VERSION, pushes the tag, and publishes a GitHub release from RELEASE_NOTES_FILE. Run from the repo root once the pre-tag checks in the runbook are done. Requires git, gh (authenticated), and python on PATH.
- **run** `release/scripts/run.py` (python) -- Idempotent: checks whether the `linkcheck` console script is already on PATH and installs the package with `pip install .` only if it's missing, then runs `linkcheck` against each Markdown file path passed as an argument, forwarding its exit code (2 aborts immediately, 1 from any file makes the overall exit 1). Use this to start/exercise the built CLI, e.g. after installing a released tag, or locally against tests/fixtures/*.md.
- **run** `release/scripts/run.sh` (sh) -- POSIX-sh equivalent of run.py: idempotent install-if-missing check for the `linkcheck` console script, then runs it against each Markdown file argument, forwarding its exit code. Use this to start/exercise the built CLI, e.g. after installing a released tag, or locally against tests/fixtures/*.md.
- **run** `release/scripts/run.ps1` (powershell) -- PowerShell equivalent of run.py: idempotent install-if-missing check for the `linkcheck` console script, then runs it against each Markdown file argument, forwarding its exit code. Use this to start/exercise the built CLI, e.g. after installing a released tag, or locally against tests/fixtures/*.md.

## Rollback

This is linkcheck's very first release, so there is no prior version to fall back users to. If a defect surfaces after publishing v0.1.0: (1) do NOT delete the `v0.1.0` git tag or the GitHub Release — removing remote history or releases is out of bounds. (2) Edit the existing GitHub Release in place to prepend a clear warning banner (e.g. "Known issue: <summary> — do not install this version, see #<issue>") so anyone landing on the release page sees it immediately; this is an in-place edit, not a deletion. (3) Open a tracking issue describing the defect and link it from the release banner. (4) Fix on a new branch, bump to 0.1.1, and cut a new release through this same runbook; the 0.1.1 release notes should state that it supersedes 0.1.0. (5) If any consumer's tooling pins `v0.1.0` directly (e.g. `pip install git+...@v0.1.0`), point them at `>=0.1.1` once available via the release-page warning. There is no database or schema state to roll back.

