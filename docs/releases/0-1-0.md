# e2e provider kimi 0.1.0 (0.1.0)

# romans 0.1.0 - e2e provider kimi

**romans** is a small, typed Python library that converts integers to canonical Roman numerals and parses them back - losslessly - over the full supported range 1-3999. This is its first release. Both conversion directions are driven by a single numeral table (the 7 single symbols plus all six subtractive pairs), the parser is strict (it accepts exactly the canonical forms `to_roman` produces), and the error contract is documented in the README and covered by tests. The package builds with `uv` into a wheel that carries the PEP 561 `py.typed` marker, so type checkers see the fully annotated API.

## Highlights

- `to_roman(n)` / `from_roman(s)`: canonical Roman numerals for every integer in 1-3999, in both directions, from one shared table (`romans/table.py`).
- Strict parsing: `from_roman` rejects invalid characters and non-canonical forms (`IIII`, `IL`, `VV`, `IC`, lowercase input, empty string) with a `ValueError` naming the offending character.
- Documented error contract: `to_roman` names the bounds 1 and 3999 for out-of-range input and the valid integer range for non-integer input.
- Exhaustive acceptance check: `tests/test_roundtrip.py` proves `from_roman(to_roman(n)) == n` for all 3999 values.
- Ships as a `uv`-built wheel + sdist with the PEP 561 typing marker; lint-clean under `ruff`.

## Verify this release

```sh
uv run pytest        # exhaustive round-trip over 1..3999 + table tests
uv run ruff check    # lint
uv build             # wheel + sdist under dist/
```

## Artifacts

- Wheel: `dist/romans-0.1.0-*.whl` (contains `romans/__init__.py`, `romans/table.py`, `romans/py.typed`)
- Source distribution: `dist/romans-0.1.0.tar.gz`

> Note: 0.1.0 is the first tag cut from this repository after it was reset from earlier, unrelated e2e projects; there is no previous release to diff against.

## Highlights

- First release of `romans`: a typed Python library converting integers 1-3999 to canonical Roman numerals and back, losslessly.
- One shared numeral table (`romans/table.py`, 13 pairs including all six subtractive pairs) drives both conversion directions.
- Strict, documented error contract: `to_roman` names the bounds 1/3999, and `from_roman` rejects invalid characters and non-canonical forms (IIII, IL, VV, IC, ...) by name.
- Acceptance is exhaustive: `tests/test_roundtrip.py` proves `from_roman(to_roman(n)) == n` for all 3999 values under `uv run pytest`.
- Ships as a `uv`-built wheel + sdist with the PEP 561 `py.typed` marker; lint-clean under `ruff`.

## Changelog

# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [0.1.0] - Initial release

First release of `romans`, a typed Python library converting integers to canonical Roman numerals and back, losslessly, over the range 1-3999.

### Added
- Public API `to_roman(n: int) -> str` and `from_roman(s: str) -> int` in `src/romans/__init__.py`: canonical Roman numerals for 1-3999, including the subtractive pairs IV, IX, XL, XC, CD, CM (#405).
- Strict, documented error contract: `to_roman` raises `ValueError` naming the lower bound (1) or upper bound (3999) for out-of-range input and the valid integer range for non-integer input (float, str, None, bool); `from_roman` is case-sensitive and raises `ValueError` naming the offending character for invalid characters, non-canonical sequences (`IIII`, `IL`, `VV`, `IC`, ...) and empty input (#405).
- Numeral table extracted into `src/romans/table.py`: exactly 13 (value, numeral) pairs - the 7 single symbols plus the 6 subtractive pairs (CM, CD, XC, XL, IX, IV) - in strictly descending order, the single source of truth shared by both converters (#406).
- Exhaustive round-trip test `tests/test_roundtrip.py`: asserts `from_roman(to_roman(n)) == n` for every integer n in 1..3999, importing only the public API (#407).
- `tests/test_table.py` pinning the table's exact contents and ordering.
- Packaging per the project Blueprint: `pyproject.toml` (`[project]` name `romans`, version `0.1.0`, src-layout package discovery, pytest + ruff as dev tooling) and the PEP 561 marker `src/romans/py.typed`; `uv build` produces a wheel containing `romans/__init__.py`, `romans/table.py` and `romans/py.typed` (#405).
- `README.md` documenting the library's purpose, usage examples for both functions, the valid range 1-3999, the error contract, and the acceptance check with the exact command `uv run pytest tests/test_roundtrip.py` (#408).

### Changed
- Nothing user-visible. The numeral-table extraction (#406) is a pure refactor: conversion results are byte-for-byte identical before and after.

### Fixed
- None (initial release).

### Removed
- Repository reset commits (`chore: reset repo`, `chore: reset for the next e2e project`) cleared the sources of earlier, unrelated e2e exercises (JavaScript space-invaders game, Java/Swing calculator, C++ prime tester) that previously lived in this repository. They are not part of the `romans` library and are listed only because 0.1.0 is the first tagged release and the full history precedes it.

## Release steps

1. [manual] Confirm the CI build workflow matches this release's toolchain - Open .github/workflows/build.yml and verify it builds what this release ships: uv-based Python toolchain - install via uv, `uv run pytest`, `uv run ruff check`, build command `uv build`, artifact upload from `dist/`. BuildBoard OWNS this file and re-scaffolds it from the build resource's `command` + `artifact_path` on every build. The git history shows this repo was reset from earlier non-Python e2e projects (C++/CMake prime tester, Java/Maven calculator, JS/npm space invaders), so drift is a real risk: if the build resource config still names an old command (e.g. `cmake --build`, `mvn package`) or artifact path, updating the build resource config to command `uv build` and artifact_path `dist/` is REQUIRED so the workflow re-scaffolds before the release is tagged. Do not edit the workflow file by hand.
2. [automated] Run the full verification suite - From the repository root: `uv run pytest` must exit 0 (tests/test_roundtrip.py exercises all 3999 values; tests/test_table.py pins the table) and `uv run ruff check` must report no errors.
3. [automated] Build the distribution - `uv build` from the repository root. Expect a wheel `dist/romans-0.1.0-*.whl` and an sdist `dist/romans-0.1.0.tar.gz`. Verify the wheel contains `romans/__init__.py`, `romans/table.py` and `romans/py.typed` (the release script does this automatically).
4. [automated] Smoke-test the built wheel in a clean environment - Install the built wheel into a fresh venv and exercise the public API, e.g.: `python -m venv /tmp/romans-smoke && /tmp/romans-smoke/bin/pip install dist/romans-0.1.0-*.whl && /tmp/romans-smoke/bin/python -c "from romans import to_roman, from_roman; assert to_roman(1994)=='MCMXCIV'; assert to_roman(3999)=='MMMCMXCIX'; assert from_roman('CM')==900; assert all(from_roman(to_roman(n))==n for n in range(1,4000)); print('smoke OK')"` (adapt the venv bin path on Windows).
5. [manual] Land the release-notes commit on the default branch - Review and merge the chore(release) PR that adds CHANGELOG.md (0.1.0 section), docs/releases/0-1-0.md (the releaseNotes body) and release/scripts/release.{py,sh,ps1}. The tag must be created only after this commit lands, so the tagged tree contains its own release documentation.
6. [automated] Tag v0.1.0 and create the GitHub release with artifacts - From the repository root run `python release/scripts/release.py` (or `sh release/scripts/release.sh` / `powershell -File release/scripts/release.ps1`). It runs `uv build`, verifies the wheel contents, creates and pushes annotated tag v0.1.0, and creates GitHub release 'e2e provider kimi 0.1.0' with docs/releases/0-1-0.md as the body and the dist artifacts attached (via the `gh` CLI; it prints the exact manual command if `gh` is absent). Fully idempotent - existing tags/releases are detected and skipped, so it is safe to re-run.
7. [manual] Decide on PyPI publication - The Blueprint targets a PyPI-publishable library, but no publishing destination or credentials are configured in this release's scope. A human decides: if publishing, confirm the project name `romans` is available/owned on PyPI, configure PYPI_TOKEN or trusted publishing, and run `uv publish`. If this release is GitHub-only, skip explicitly and record the decision.
8. [manual] Post-release sanity check - Open the GitHub release page for v0.1.0: confirm the notes render, the tag points at the intended default-branch commit, and both artifacts download. Install the wheel from the release asset into a clean venv and re-run the smoke import (`from romans import to_roman, from_roman; to_roman(2024)`).

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Performs the automated release of romans 0.1.0: runs `uv build`, verifies the wheel contains romans/__init__.py, romans/table.py and romans/py.typed, creates and pushes annotated tag v0.1.0, then creates the GitHub release with the dist artifacts attached (via the gh CLI, or prints the exact manual command). Idempotent and safe to re-run; run from the repository root AFTER the release-notes commit has landed on the default branch.
- **release** `release/scripts/release.sh` (sh) -- POSIX-sh equivalent of the release helper: builds with `uv build`, verifies the wheel contains the three romans package members, creates and pushes annotated tag v0.1.0, then creates the GitHub release with the dist artifacts via gh (or prints the exact manual command). Idempotent; run from the repository root AFTER the release-notes commit has landed: `sh release/scripts/release.sh`.
- **release** `release/scripts/release.ps1` (powershell) -- PowerShell equivalent of the release helper: builds with `uv build`, verifies the wheel contains the three romans package members, creates and pushes annotated tag v0.1.0, then creates the GitHub release with the dist artifacts via gh (or prints the exact manual command). Idempotent; run from the repository root AFTER the release-notes commit has landed: `powershell -File release/scripts/release.ps1`.

## Rollback

This release is purely additive: an annotated tag v0.1.0, a GitHub release with wheel + sdist artifacts, and release-notes files on the default branch. No database schema, no deployed service, no prior version. To roll back: (1) a human deletes the GitHub release 'e2e provider kimi 0.1.0' in the GitHub UI first, then deletes the tag v0.1.0 (releases must be removed before their tag; BuildBoard agents are forbidden from deleting remote state, so this is always a human action - never a force-push or history rewrite); (2) if the wheel was published to PyPI, yank release 0.1.0 in the PyPI UI - PyPI does not allow re-uploading the same version, so any corrected re-release must ship as 0.1.1; (3) no code rollback is required: 0.1.0 is the first release, the pre-release state is simply the untagged default branch, and all four feature PRs remain valid work. After a rollback, release/scripts/release.py can be re-run safely because every step is idempotent (existing tags/releases are detected and skipped).

