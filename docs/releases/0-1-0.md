# e2e word count 0.1.0 (0.1.0)

# e2e word count v0.1.0

The first release of **e2e word count** — a small, dependency-free `wc`-style command-line tool written in Go. Point it at one or more files (or pipe data into it) and get back line, word, and byte counts, with a combined total across multiple inputs.

## Highlights
- New `wordcount` CLI: prints `<lines>\t<words>\t<bytes>\t<name>` per input (`-` for stdin), plus a `total` row when more than one input is given.
- Counting logic lives in its own `count.go` module (`Counts` struct + `Count(io.Reader) (Counts, error)`), decoupled from CLI/argument handling in `main.go`.
- Table-driven tests lock down four edge cases: empty input, no trailing newline, repeated/consecutive spaces, and multi-byte UTF-8 text where byte and rune counts differ.
- README.md documents build/run steps and a verified output table for those same four inputs.
- No runtime dependencies — Go standard library only; `go.mod` targets Go 1.22.

## Getting started
```
git clone https://github.com/PawExperiences/BB.Project1.git
cd BB.Project1
go build ./...
./wordcount README.md
```

## Compatibility
Initial release — no prior version to compare against, no breaking changes.


## Highlights

- First release of `wordcount`, a dependency-free wc-style CLI in Go (line/word/byte counts, stdin + multi-file + total support).
- Counting logic isolated into `count.go` (`Counts` struct, `Count(io.Reader) (Counts, error)`), decoupled from CLI/argument handling in `main.go`.
- Table-driven tests cover empty input, missing trailing newline, repeated whitespace, and multi-byte UTF-8 edge cases.
- README documents build/run steps plus a verified output table for those same four inputs.
- No runtime dependencies — pure Go standard library, targeting Go 1.22.

## Changelog

## [0.1.0] - 2026-08-11

### Added
- `wordcount`: a dependency-free `wc`-style CLI in Go (`go.mod`, `main.go`). Reads one or more files named on the command line, or stdin when no arguments are given, and prints `<lines>\t<words>\t<bytes>\t<name>` per input (using `-` as the name for stdin), plus a combined `<total-lines>\t<total-words>\t<total-bytes>\ttotal` line when more than one input is given. Unreadable files log `wordcount: <name>: ...` to stderr, are skipped, and the process exits 1; a fully successful run exits 0. (#257)
- `count.go`: an exported `Counts` struct (line/word/byte counts) and `func Count(r io.Reader) (Counts, error)`, extracting counting into a self-contained, testable unit. Words follow `bufio.ScanWords` semantics, lines are counted as `\n` bytes (`wc -l` semantics), and bytes are raw byte counts (not runes). (#258)
- `count_test.go`: table-driven tests over the four highest-risk edge cases — empty input, a single line with no trailing newline, a line with repeated/consecutive spaces, and input containing a multi-byte UTF-8 character where byte count and rune count differ.
- README.md usage/output table showing the exact, verified CLI output for those same four inputs, alongside build/run instructions.

### Changed
- Counting logic moved out of `main.go` into `count.go`; `main.go` now only parses arguments/flags, opens the input source(s), calls into `count.go`, and formats/prints results. Observable CLI output is unchanged by this refactor. (#258)

### Fixed
- N/A — first release.


## Release steps

1. [automated] Sync main and verify a clean working tree - Run `git fetch origin && git checkout main && git pull --ff-only origin main && git status --porcelain`. The status output must be empty before continuing; abort and investigate if it is not.
2. [manual] Verify the 'Tests and the README table' task's changes are actually merged - Manually confirm main contains count_test.go (table-driven tests for empty input, no trailing newline, repeated spaces, and multi-byte UTF-8) and that README.md has the corresponding usage/output table. The commit log/diffstat provided for this release only lists #257 (wc in Go) and #258 (Counting, in its own file) — no commit for the tests task is visible. Do not proceed until this is confirmed; if the task is not actually merged, halt and flag it back upstream rather than tagging an incomplete release.
3. [automated] Run format, build, and test gates - Run `gofmt -l .` (must print nothing), `go build ./...`, and `go test ./...`. All three must complete with exit code 0.
4. [manual] Confirm CI build workflow currency - Confirm the project's build resource command is `go build ./...` (or `go build -o wordcount .`) on Go 1.22, matching go.mod's `go 1.22` directive, and that artifact_path points at the compiled `wordcount` binary — so `.github/workflows/build.yml` (BuildBoard-owned, re-scaffolded from that config) reflects what this release actually builds. If the build command, toolchain, or artifact path changed for this release, update the build resource config now so the workflow re-scaffolds; do not hand-edit build.yml.
5. [automated] Generate RELEASE_NOTES.md from this runbook - Write this runbook's `releaseNotes` field verbatim to RELEASE_NOTES.md at the repo root. The release script's `gh release create --notes-file RELEASE_NOTES.md` step consumes this file.
6. [automated] Run the release script - Run `sh release/scripts/release.sh` (or release.py / release.ps1 on other platforms). It re-checks gofmt/build/test, cross-compiles linux/darwin/windows (amd64+arm64) binaries into dist/, creates and pushes the annotated tag `v0.1.0` (additive only, never force), and — if an authenticated `gh` CLI is present — publishes the GitHub release with the dist/ artifacts attached.
7. [manual] Publish the GitHub release manually if gh CLI is unavailable - If release.sh reported that `gh` was not found or not authenticated, go to https://github.com/PawExperiences/BB.Project1/releases/new, select tag v0.1.0, title it 'e2e word count v0.1.0', paste RELEASE_NOTES.md as the body, and upload the binaries from dist/.
8. [manual] Smoke-test the published binary - Download one of the published dist/ artifacts (or use the local build), run it against the four README example inputs and a small multi-file case, and confirm the output matches the README's documented table exactly, including the total line and exit codes 0 (success) / 1 (an input failed to open).
9. [manual] Announce and close out - Link the published release from the BuildBoard release ticket for 'e2e word count 0.1.0', and move the release ticket to Done.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Idempotent: runs gofmt/go build/go test gates, cross-compiles linux/darwin/windows (amd64+arm64) binaries into dist/, creates+pushes the v0.1.0 tag if missing, and publishes the GitHub release with artifacts via an authenticated gh CLI if present. Run this once main has the verified release commit checked out, right before publishing.
- **release** `release/scripts/release.sh` (sh) -- POSIX sh equivalent of release.py: idempotently runs gofmt/go build/go test, cross-compiles linux/darwin/windows (amd64+arm64) binaries into dist/, creates+pushes the v0.1.0 tag if missing, and publishes the GitHub release with artifacts via gh if present. Run this once main has the verified release commit checked out, right before publishing.
- **release** `release/scripts/release.ps1` (powershell) -- Windows PowerShell 5.1 equivalent of release.py/release.sh: idempotently runs gofmt/go build/go test, cross-compiles linux/darwin/windows (amd64+arm64) binaries into dist/, creates+pushes the v0.1.0 tag if missing, and publishes the GitHub release with artifacts via gh if present. Run this once main has the verified release commit checked out, right before publishing.
- **run** `release/scripts/run.py` (python) -- Builds the wordcount binary if it does not already exist in the current directory, then runs it forwarding all command-line arguments (files, or none for stdin). Use this any time you want to try the CLI locally without remembering the go build invocation.
- **run** `release/scripts/run.sh` (sh) -- POSIX sh equivalent of run.py: builds the wordcount binary if it does not already exist in the current directory, then runs it forwarding all command-line arguments (files, or none for stdin). Use this any time you want to try the CLI locally without remembering the go build invocation.
- **run** `release/scripts/run.ps1` (powershell) -- Windows PowerShell 5.1 equivalent of run.py/run.sh: builds the wordcount(.exe) binary if it does not already exist in the current directory, then runs it forwarding all command-line arguments (files, or none for stdin). Use this any time you want to try the CLI locally without remembering the go build invocation.

## Rollback

This is the initial v0.1.0 release, so there is no prior published version to revert to. Remote deletions are forbidden: never delete or force-push over the v0.1.0 tag or the GitHub release. If a defect is found after publishing: (1) edit the existing GitHub release in place to add a warning note at the top of the body pointing users to the fix (do not delete it); (2) optionally edit the release via the GitHub UI to mark it as a pre-release, reducing its visibility as 'latest', again in place, not by deleting it; (3) cut a new patch release (v0.1.1) with the fix by following this same runbook; (4) in the interim, instruct users to build from the pre-release commit (before f1e7f48) or hold off upgrading until v0.1.1 ships. There are no data or schema migrations in this release, so no data rollback is required.

