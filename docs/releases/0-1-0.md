# e2e space invaders 0.1.0 (0.1.0)

## e2e Space Invaders v0.1.0

This is the **first playable release** of *e2e Space Invaders* — a complete, browser-native Space Invaders clone that runs directly from the filesystem (`file://`) with no server, no bundler, and no npm dependencies.

The game is built entirely with vanilla ES modules and the Canvas 2D API. Open `index.html` in any modern browser and play immediately.

### Highlights

- **Full game loop** with fixed-timestep updates (1/60 s), delta capping, and a three-scene state machine (Title → Playing → Game Over).
- **Playable player ship** — arrow/WASD movement, single-bullet mechanic, procedural canvas rendering.
- **55-invader formation** with AABB collision, white flicker explosions, and score tracking.
- **Four progressive levels**: classic grid (Level 1) → enemies shoot back with UFO bonus (Level 2) → destructible bunkers and formation split (Level 3) → two-phase boss fight.
- **Boss finale** with a 10-HP health bar, two distinct firing phases, instant-death projectiles, and a Win Screen on victory.
- **Zero external dependencies** — plain HTML + ES modules, `file://`-compatible.

## Highlights

- First fully playable release — open index.html from the filesystem, no server required
- Four-level progression: classic grid → enemy fire + UFO → bunkers + split formation → two-phase boss
- Fixed-timestep game loop (1/60 s tick, 250 ms delta cap) with three-scene state machine
- Destructible shield bunkers with per-cell erosion in Level 3
- Boss fight with visible health bar, phase-change visual cue, and Win Screen on victory
- Zero external dependencies — pure ES modules, Canvas 2D API, no bundler or npm

## Changelog

# Changelog

## [0.1.0] - Initial Release

### Added
- **Game loop and canvas framework** (`index.html`, `game.js`, `gameConfig.js`): Fixed-timestep game loop (1/60 s tick, 250 ms delta cap), three-scene state machine (Title → Playing → Game Over), canvas HUD (score, lives, hi-score), and shared constants (`CANVAS_WIDTH=768`, `CANVAS_HEIGHT=896`, `PLAYER_SPEED=200`, `BULLET_SPEED=500`, `STARTING_LIVES=3`). Works from a `file://` URL with no server dependency.
- **Keyboard input and player ship** (`input.js`, `player.js`): Held-key tracking via `KeyboardEvent.code`; `Player` class with delta-time movement (200 px/s), edge clamping, single-bullet mechanic (500 px/s), and procedural canvas drawing.
- **Sprite rendering and collision detection** (`invaders.js`, `collision.js`, `explosion.js`, `score.js`): 11×5 green invader formation, AABB collision (player bullet↔invader, invader bullet↔player), white flicker explosion pool (3–5 frames), 10-point-per-kill score exported from `score.js`.
- **Level 1 – The Classic Grid** (`level1.js`): Formation steps 8 px per tick; speed scales linearly from ~800 ms (55 alive) to ~100 ms (1 alive); edge-drop of one cell height on boundary touch; life-loss/reset on formation reaching player row; level-clear transition to Level 2; HUD displays `LEVEL 1`.
- **Level 2 – They Shoot Back** (`level2.js`): Formation runs at 0.67× Level 1 intervals; global enemy-fire timer (800–2000 ms random, bottom-column invader selected at random); invader bullets travel down at 300 px/s; 2-second invulnerability with visual flash on player hit; UFO bonus target every 20 s alternating sides at 120 px/s with score tier (50/100/150/300 pts) based on `totalShotsFired % 4`.
- **Level 3 – Shields and Formations** (`level3.js`): Four destructible shield bunkers (4×4 grid of 8 px green cells at 80% canvas height); cell-level erosion from both player and invader bullets; formation split at ≥ 28 kills into independent left/right sub-formations; each sub-formation sweeps and fires independently; level complete when all invaders in both halves are eliminated.
- **Boss Level – Multi-Phase Finale** (`boss.js`): Stationary boss rendered with canvas primitives, 10 HP visible health bar; Phase 1 (HP 6–10): fires single projectile every 2 s; Phase 2 (HP 1–5): fires every 1 s with visual phase-change cue; boss projectile hits player → instant death, score reset, restart at Level 1; boss defeated → Win Screen with final score and any-key restart.
- **CI workflow** (`.github/workflows/build.yml`): BuildBoard-scaffolded build workflow initialised for the project.
- **README.md**: Module file listing, single-sentence purpose per file, manual verification checklists for every level and mechanic.

## Release steps

1. [manual] Verify CI build workflow currency - Inspect `.github/workflows/build.yml` (owned and scaffolded by BuildBoard). Confirm the `command` field matches the project's actual build command (static file project — no compile step; command should be a lint/check or a no-op pass), and that `artifact_path` points to the correct output (e.g. the root directory or a zip of the JS files). If the toolchain or artifact path changed in this release, update the BuildBoard build resource config so the workflow re-scaffolds correctly. No CI drift is acceptable at release time.
2. [manual] Confirm all bundled tasks are in Done lane - On the BuildBoard kanban board for 'e2e space invaders', verify all seven tasks are in the Done lane: 'Game loop and canvas framework', 'Keyboard input and the player ship', 'Sprite rendering and collision detection', 'Level 1: the classic grid', 'Level 2: they shoot back', 'Level 3: shields and formations', 'Boss level: multi-phase finale'. Do not proceed if any task is not Done.
3. [automated] Verify the working tree on main - Run: `git fetch origin && git checkout main && git pull origin main && git log --oneline -10`. Confirm the HEAD commit is `3b619d1` (feat: Boss level: multi-phase finale). Confirm the following files exist: index.html, game.js, gameConfig.js, input.js, player.js, invaders.js, collision.js, explosion.js, score.js, level1.js, level2.js, level3.js, boss.js, README.md. Command: `ls -1 index.html game.js gameConfig.js input.js player.js invaders.js collision.js explosion.js score.js level1.js level2.js level3.js boss.js README.md`
4. [manual] Run manual smoke test (file:// open) - Open index.html directly in Chrome and Firefox using a file:// URL (e.g. `file:///path/to/repo/index.html`). Walk through the manual verification checklist in README.md covering: Title scene render, ENTER to Playing, player movement and clamping, single-bullet mechanic, invader formation, collision and explosion, Level 1 speed scaling, Level 2 enemy fire and UFO, Level 3 bunkers and split, boss fight both phases, instant-death, Win Screen, and delta-cap. Zero console errors expected.
5. [manual] Confirm CI passes on main - Navigate to https://github.com/PawExperiences/BB.Project1/actions and confirm the most recent workflow run on the `main` branch (`.github/workflows/build.yml`) completed with a green status. If it is failing, do not tag the release — resolve the CI failure first.
6. [automated] Create and push the v0.1.0 annotated tag - Run the release script: `python release/scripts/release.py` (or `.sh` / `.ps1` equivalent). The script creates an annotated git tag `v0.1.0` on the current HEAD of main with the release message 'e2e Space Invaders 0.1.0 — initial release', then pushes the tag to origin. Exact commands executed by the script:
  git tag -a v0.1.0 -m 'e2e Space Invaders 0.1.0 — initial release'
  git push origin v0.1.0
7. [manual] Publish GitHub Release - On https://github.com/PawExperiences/BB.Project1/releases/new: select tag `v0.1.0`, set title to 'e2e Space Invaders v0.1.0', paste the release notes from the `releaseNotes` field of this runbook into the description body, and click 'Publish release'. No binary artifact upload is required (the game is pure source; the repo itself is the distributable). Mark as 'latest release'.
8. [manual] Post-release verification - Confirm the tag is visible at https://github.com/PawExperiences/BB.Project1/tags. Confirm the release is visible at https://github.com/PawExperiences/BB.Project1/releases. Download or browse the source zip from the release page, unzip it, open index.html from a file:// URL in a fresh browser profile, and confirm the game starts correctly with no console errors.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Creates annotated git tag v0.1.0 on the current HEAD of main and pushes it to origin. Run once on the release branch after all checks pass and CI is green.
- **release** `release/scripts/release.sh` (sh) -- Creates annotated git tag v0.1.0 on the current HEAD of main and pushes it to origin. Run once on the release branch after all checks pass and CI is green.
- **release** `release/scripts/release.ps1` (powershell) -- Creates annotated git tag v0.1.0 on the current HEAD of main and pushes it to origin. Run once on the release branch after all checks pass and CI is green.
- **run** `release/scripts/run.py` (python) -- Starts a minimal local HTTP server on port 8080 serving the repo root so the game can be tested over http:// (the game also works from file://, but this is useful for browsers that restrict ES modules on file://). Run during development or QA; not needed for production use.
- **run** `release/scripts/run.sh` (sh) -- Starts a minimal local HTTP server on port 8080 serving the repo root so the game can be tested over http://. Run during development or QA; not needed for production use (file:// also works).
- **run** `release/scripts/run.ps1` (powershell) -- Starts a minimal local HTTP server on port 8080 serving the repo root so the game can be tested over http://. Run during development or QA; not needed for production use (file:// also works).

## Rollback

This is the initial release (v0.1.0); there is no previous production version to roll back to. If the release is found to be broken after tagging:
1. Do NOT delete the v0.1.0 tag or release from GitHub (protected remote state).
2. Create a new patch branch `hotfix/0.1.1` from the v0.1.0 tag.
3. Apply fixes on that branch, verify locally by opening index.html from a file:// URL, then open a pull request targeting `main`.
4. After merge, tag the fix as v0.1.1 and publish a new GitHub release.
5. Update the GitHub release description for v0.1.0 to note it was superseded by v0.1.1.

