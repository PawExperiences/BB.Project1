# e2e space invaders 0.1.0 (0.1.0)

## Space Invaders 0.1.0

The first release of **Space Invaders (JavaScript)**: a complete, playable arcade campaign built entirely as hand-written ES modules — no dependencies, no bundler, no server. Download the attached zip (or clone the tag), open `index.html` in any modern browser straight from the filesystem, and press ENTER. You get the full classic loop: a 60 Hz fixed-timestep engine with Title / Playing / Game Over scenes, the 11x5 marching invader formation, and a four-level campaign that escalates from the classic grid to invaders that shoot back (with a bonus UFO), destructible shield bunkers with a mid-wave formation split, and a two-phase boss finale with sudden death and a win screen.

### Highlights
- Zero-dependency, zero-build: pure ES modules that run from `file://`.
- Deterministic 60 Hz fixed-timestep loop with clamped catch-up and an on-canvas HUD (score, lives, hi-score, level).
- Classic 11x5 invader formation with AABB collision, explosions and the one-bullet-in-flight rule.
- Four levels: classic grid → they shoot back + UFO bonus → shield bunkers + formation split → multi-phase boss.
- Controls: Arrow keys or A/D to move, Space to fire, ENTER to advance scenes.

### Running the game
Open `index.html` directly in a browser (a `file://` URL works), or use the helper: `python release/scripts/run.py` (also `.sh` / `.ps1`).

See `CHANGELOG.md` for the complete list of changes.


## Highlights

- Complete, playable Space Invaders in hand-written ES modules — zero dependencies, zero build step; open index.html from file:// and play.
- Deterministic 60 Hz fixed-timestep loop with clamped catch-up, a three-scene state machine (Title / Playing / Game Over) and an on-canvas HUD (score, lives, hi-score, level).
- The classic 11x5 marching invader formation with shared AABB collision, concurrent explosions, +10 scoring and the one-bullet-in-flight rule.
- A four-level campaign: the classic grid, invaders that shoot back with a shot-count-driven bonus UFO, destructible shield bunkers with a mid-wave formation split, and a 10 HP two-phase boss finale with sudden death and a win screen.
- Controls: Arrow keys or A/D to move, Space to fire, ENTER to advance scenes — lives carry across levels and the session shot count drives UFO rewards.

## Changelog

# Changelog — Space Invaders 0.1.0

First release of the project: a complete, four-level Space Invaders built as hand-written ES modules with zero dependencies and no build step. Open `index.html` from the filesystem (`file://`) and press ENTER.

## Added

### Game framework (#366, commit 6c8adf1)
- `index.html`: dark page with a single 768x896 canvas, all styling inline, loads `game.js` as an ES module; runs from `file://` with no server, bundler, fetch or CDN.
- `gameConfig.js`: shared named constants `CANVAS_WIDTH` (768), `CANVAS_HEIGHT` (896), `PLAYER_SPEED` (200), `BULLET_SPEED` (500), `STARTING_LIVES` (3).
- `game.js`: fixed-timestep loop (`update(dt)` at a constant 1/60 s, 60 steps/s, render as a separate per-frame phase, frame delta clamped at 250 ms against background-tab catch-up bursts).
- Scene state machine with exactly three scenes (Title / Playing / Game Over), every transition triggered by ENTER, no page reloads; `hiScore` updated to `max(hiScore, score)` on entering Game Over.
- On-canvas HUD plus the named export `hudState` (`{ score, lives, hiScore }`) as the integration point for later cards.

### Input and player ship (#367, commit 1fbc5f2)
- `input.js`: `initInput()` / `isKeyHeld(key)` held-key tracking derived purely from keydown/keyup transitions; keyboard auto-repeat neutralized via an `event.repeat` guard.
- `player.js`: procedurally drawn ship (canvas arcs/rects, no image assets); ArrowLeft/ArrowRight or A/D movement at 200 px/s dt-scaled; hard clamping to `x = 0`..`CANVAS_WIDTH`.
- Firing: Space spawns one upward bullet at 500 px/s under the classic one-bullet-in-flight rule; the in-flight bullet (or `null`) is exposed as `player.bullet`.
- Lives counter initialised from `STARTING_LIVES` with a documented decrement interface, rendered on the canvas.

### Invaders and collision (#368, commit 78a8f91)
- `invaders.js`: the classic 11-column x 5-row formation (55 identical `fillRect` invaders) marching sideways as a unit; at either canvas edge the formation reverses and drops one step.
- `collision.js`: shared AABB overlap test; per-frame collision pass runs after world updates and before any drawing; player-bullet-vs-invader kills remove both, spawn a ~0.2-0.4 s explosion (several may run concurrently) and add +10 score; the invader-bullet-vs-player check consumes the game state's hostile-bullet list (empty until Level 2).

### Levels and finale (#369, #370, Level 3, #371; commits 7766c63, b7a04f3, 7afebba)
- `level1.js` + level registry/loader: classic 55-invader grid; step interval scales linearly from ~800 ms (55 alive) to ~100 ms (1 alive); edge contact drops the formation by exactly one invader cell height and reverses direction; an invader reaching the player row costs one life and restarts the level (score/lives otherwise carry over); clearing all 55 advances the level counter; HUD shows `LEVEL n`.
- `level2.js`: same grid at 1.5x speed (every march interval x 0.67); enemy fire on a single global timer every 800-2000 ms from the lowest living invader of a random occupied column, bullets travel down at 300 px/s; bonus UFO every 20 s, alternating entry sides, crossing at 120 px/s, awarding 50/100/150/300 points by session shot count mod 4 (cumulative across levels); player death respawns the ship bottom-centre with 2 s of visibly flashing invulnerability; last life lost enters the existing Game Over scene.
- `level3.js`: four destructible shield bunkers (4x4 grids of ~8 px cells) evenly spaced at ~80% canvas height; each projectile (either side) destroys exactly the cell it hits and is consumed; destroyed cells stay destroyed and let projectiles pass; descending invaders carve through bunkers unharmed; at the 28th kill the formation splits by original column into independent left (columns 1-6, moves left first) and right (columns 7-11, moves right first) groups that sweep, descend and fire independently at equal speed.
- `boss.js` (Level 4): 160x80 px boss drawn with canvas primitives, 10 HP with a proportional health bar across the top of the canvas; horizontal drift at 90 px/s with edge reversal, never descends; three-bullet centre spread (straight down, +/-20 degrees) at 260 px/s, fired every 1500 ms above 5 HP and every 700 ms from 5 HP; all hit-testing imports the shared helpers from `collision.js`; one boss-bullet hit is sudden death (restart is a fresh run at Level 1); reducing the boss to 0 HP shows a win screen with the final score and a restart option.

### Build / CI
- BuildBoard-managed CI build workflow `.github/workflows/build.yml` scaffolded (commit 917c6a5); the project builds green with all game files present.

## Changed
- Nothing — initial release.

## Fixed
- Nothing — initial release.


## Release steps

1. [manual] Pre-flight: confirm scope merged and CI green - Verify on the main branch of https://github.com/PawExperiences/BB.Project1: PRs #366 (framework), #367 (input/player), #368 (invaders/collision), #369 (Level 1), #370 (Level 2), #371 (boss) and the Level 3 card are merged; the latest run of the BuildBoard build workflow on main is green. The commit log supplied with this release shows no distinct Level 3 commit — explicitly confirm level3.js is present on main (ls level3.js) before proceeding; if it is absent, STOP and escalate, the bundle is incomplete.
2. [manual] CI workflow currency check (required) - Confirm .github/workflows/build.yml matches the toolchain this release ships. The release is a zero-dependency static site: no package.json, no bundler, no compile step; the artifact is the static file set (index.html + *.js + README.md). Nothing in this range changes the build command, toolchain or artifact path (the bundled tasks explicitly forbid adding bundlers/dependencies), so no build-resource config update is required. If the check finds drift (e.g. the workflow's command or artifact_path no longer reflects the repo), update the build resource config so BuildBoard re-scaffolds the workflow BEFORE tagging. Record the outcome of this check in the release ticket.
3. [manual] Merge the release PR - Open and merge a PR to main adding: the CHANGELOG.md 0.1.0 entry (content from this runbook), docs/releases/0-1-0.md with the release notes, and the helper scripts under release/scripts/ (release.{py,sh,ps1}, run.{py,sh,ps1}). Wait for the BuildBoard build to go green on the merge commit.
4. [automated] Run the automated release script - From a clean, up-to-date checkout of main run: python release/scripts/release.py (or: sh release/scripts/release.sh / powershell -File release\scripts\release.ps1). The script verifies all game files are present (fails if any, e.g. level3.js, is missing), builds dist/space-invaders-0.1.0.zip (or .tar.gz under sh), creates the annotated tag v0.1.0, pushes it to origin, and — if the gh CLI is installed and authenticated — creates the GitHub release 'Space Invaders 0.1.0' with the artifact attached. Every step is idempotent; safe to re-run after a failure.
5. [manual] Finish the GitHub release manually (only if gh was unavailable) - At https://github.com/PawExperiences/BB.Project1/releases/new choose tag v0.1.0, set title 'Space Invaders 0.1.0', paste the releaseNotes from this runbook as the body, attach dist/space-invaders-0.1.0.zip (or .tar.gz), and publish as latest.
6. [manual] Smoke-test the shipped artifact - Download the release artifact (or check out tag v0.1.0) and open index.html via file://. Walk Title → Playing → Game Over → Title with ENTER; verify HUD (score, lives, LEVEL n), ship clamping at both edges, the one-bullet rule, formation edge reverse-and-drop, bullet-vs-invader kills (+10, explosion), Level 2 enemy fire and the UFO, Level 3 bunker erosion (both projectile sides) and the formation split at the 28th kill, the boss health bar, the fire-rate change at 5 HP, sudden death, and the win screen with restart — per the README manual verification path.
7. [manual] Announce and close the release - Ensure the GitHub release v0.1.0 is marked as latest, link it from the BuildBoard release ticket, and close the release. No deployment, migration or notification pipeline exists for this static artifact.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Builds the v0.1.0 zip from the game files, creates and pushes the annotated git tag v0.1.0 (skips both if they already exist), and creates the GitHub release with the zip attached when the gh CLI is available — run once from a clean main checkout after the release PR merges and CI is green; safe to re-run.
- **release** `release/scripts/release.sh` (sh) -- Builds the v0.1.0 tarball from the git tag via git archive (so artifact == tagged source), creates and pushes the annotated tag v0.1.0 (skips both if they already exist), and creates the GitHub release with the tarball attached when the gh CLI is available — run once from a clean main checkout after the release PR merges and CI is green; safe to re-run.
- **release** `release/scripts/release.ps1` (powershell) -- Builds the v0.1.0 zip from the game files via Compress-Archive, creates and pushes the annotated git tag v0.1.0 (skips both if they already exist), and creates the GitHub release with the zip attached when the gh CLI is available — run once from a clean main checkout after the release PR merges and CI is green; safe to re-run.
- **run** `release/scripts/run.py` (python) -- Opens the game's index.html in the default browser via a file:// URL (the game is fully static — no server or build step) — run any time you want to play or smoke-test the released 0.1.0 build.
- **run** `release/scripts/run.sh` (sh) -- Opens the game's index.html in the default browser via a file:// URL using xdg-open (Linux), open (macOS) or start (Windows/Git Bash) — run any time you want to play or smoke-test the released 0.1.0 build; no server needed.
- **run** `release/scripts/run.ps1` (powershell) -- Opens the game's index.html in the default browser via a file:// URL using Start-Process (the game is fully static — no server or build step) — run any time you want to play or smoke-test the released 0.1.0 build.

## Rollback

There is no runtime deployment to roll back: the game is a fully static, client-side artifact distributed as a zip plus source, and 0.1.0 is the first release (no previous version to revert to). Rollback therefore means withdrawing the published release, in order of preference: (1) NON-DESTRUCTIVE — edit the GitHub release v0.1.0 and convert it to a draft or pre-release so it disappears from 'latest'; the tag and artifact remain for forensics. (2) FIX-FORWARD — for any defect found after publishing, land the fix on main and ship 0.1.1 with a new tag; this is the supported path. (3) DESTRUCTIVE, human-only — if the release must be fully removed (e.g. a takedown), a human maintainer with admin rights deletes the GitHub release and the v0.1.0 tag; deleting remote state is outside the releaser agent's authority and must never be scripted. No database, schema or persisted user state exists, so nothing else requires rollback.

