# e2e dotnet tool 0.1.0 (0.1.0)

## caltool 0.1.0

The first release of **caltool** — a small, self-contained .NET 8 console tool that prints a single calendar month to the terminal as a text grid, in the spirit of the Unix `cal` command. Run it with no arguments to see the current month, or pass a year and month to see any other one. Output is fixed to invariant-culture English, so it looks identical regardless of the host machine's locale.

### Highlights
- Zero runtime dependencies — `dotnet publish` produces a single runnable executable.
- `caltool` prints the current month; `caltool <year> <month>` (e.g. `caltool 2026 8`) prints any month, year 1-9999, month 1-12.
- Monday-first grid: centered month/year header, `Mo Tu We Th Fr Sa Su` weekday row, right-aligned day numbers, 4-6 week rows depending on the month.
- Invalid input (wrong arg count, non-numeric args, out-of-range month/year) exits with code 2 and a message naming the accepted ranges.
- Grid computation lives in its own `Calendar` class, independently unit-tested for Monday-start, Sunday-start, and leap/non-leap Februarys.

### Getting it
Download the attached artifact, or build from source:
```
dotnet publish caltool.csproj -c Release -o out
```

See `README.md` in the repository for the full build/run/publish instructions and CLI contract.

## Highlights

- First release: a zero-dependency .NET 8 console tool that prints a calendar month as a Monday-first text grid.
- `caltool` prints the current month; `caltool <year> <month>` prints any month from year 1-9999, month 1-12.
- Deterministic, invariant-culture English output — identical regardless of host locale.
- Invalid input exits with code 2 and a message naming the accepted ranges.
- Grid math is isolated in a dedicated `Calendar` class, unit-tested for Monday-start, Sunday-start, and leap/non-leap Februarys.
- README documents build, run, and publish (`dotnet publish -c Release -o out`).

## Changelog

# Changelog

## [0.1.0] - 2026-08-15

### Added
- `caltool`, a self-contained .NET 8 console tool that prints a single calendar month as a text grid, in the spirit of Unix `cal`.
- CLI contract: no arguments prints the current month (system clock); `caltool <year> <month>` prints the given month (year 1-9999, month 1-12). Any invalid invocation (wrong arg count, non-integer args, month outside 1-12, year outside 1-9999) exits with code 2 and a message naming the accepted ranges.
- Deterministic, invariant-culture English output: header line with month name + year centered over 20 characters, Monday-first `Mo Tu We Th Fr Sa Su` weekday line, and right-aligned day-number grid (4-6 week rows depending on the month).
- `Calendar.cs`: a dedicated `Calendar` class that computes the month grid (Monday-first week rows of 7 day-slots, blanks for out-of-month days) as pure structured data, with no console output, formatting, or argument parsing.
- `caltool.csproj`: net8.0, `OutputType=Exe`, `Nullable=enable`.
- `tests/CalendarTests.csproj`: a .NET unit-test project covering four boundary scenarios — a month starting on Monday (January 2024), a month starting on Sunday (September 2024), leap-year February (2024, 29 days), and non-leap-year February (2023, 28 days). `dotnet test` passes with 0 failures.
- `README.md`: build (`dotnet build`), run (`dotnet run`), and publish (`dotnet publish -c Release -o out`) instructions, plus the CLI usage/exit-code contract.

### Changed
- None (first release).

### Fixed
- None (first release).

## Release steps

1. [manual] Verify bundled tasks meet acceptance criteria - Manually confirm all three bundled tasks — 'A calendar month on the command line', 'The month grid, in its own class', 'Tests and the README' — satisfy every listed AC (CLI contract, Monday-first grid, invariant-culture text, exit code 2 with range-naming messages, the four boundary-case tests, README build/run/publish docs). Do not proceed if any AC is unmet.
2. [automated] Sync main and confirm CI is green - git fetch origin && git checkout main && git pull --ff-only origin main. Then confirm the latest CI run on main (commit d9ccc7a or later) succeeded, e.g. gh run list --branch main --limit 1 --json status,conclusion.
3. [automated] Confirm the CI build workflow matches this release's toolchain - Check that .github/workflows/build.yml's build command and artifact path resolve to a net8.0 restore/build/publish of caltool.csproj with artifact path out/ (dotnet publish -c Release -o out) — unchanged from what this release ships. If it does not match, STOP: update the project's build resource (command + artifact_path) in BuildBoard so build.yml re-scaffolds, since BuildBoard owns that file and hand-editing it would be silently overwritten.
4. [automated] Run the local gate: restore, build, test - From the repo root: dotnet restore caltool.csproj && dotnet build caltool.csproj -c Release && dotnet test tests/CalendarTests.csproj. Use explicit project paths (there is no .sln) so a bare 'dotnet test' at root does not ambiguously target caltool.csproj instead of the test project. Must complete with 0 test failures before publishing. Matches release/scripts/release.*.
5. [automated] Publish the release artifact - dotnet publish caltool.csproj -c Release -o out. Part of release/scripts/release.*.
6. [automated] Package the artifact - Zip the contents of out/ into caltool-0.1.0.zip at the repo root. Part of release/scripts/release.*.
7. [automated] Tag the release - git tag -a v0.1.0 -m 'caltool 0.1.0'. Idempotent — skipped if v0.1.0 already exists locally. This is the first tag in the repository (PREVIOUS: None).
8. [automated] Push the tag to origin - git push origin v0.1.0. Additive only — never force-pushes and never deletes or moves an existing ref.
9. [automated] Create the GitHub release - gh release create v0.1.0 caltool-0.1.0.zip --title 'caltool 0.1.0' --notes-file release/RELEASE_NOTES.md (falls back to an inline --notes string if that file is absent). Uploads the zipped out/ artifact. Skipped if a v0.1.0 release already exists.
10. [manual] Verify the published release - Open the GitHub release page, confirm the artifact download link works, unzip it, and run the caltool binary once with no arguments and once with a known <year> <month> pair (e.g. 2026 8) to sanity-check the output against the documented grid format.
11. [manual] Confirm board reflects the shipped release - Confirm the three bundled tasks remain in Done and the board accurately reflects that 0.1.0 shipped. No further board action is required by the releaser role.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Runs the full 0.1.0 release end-to-end: restores/builds/tests caltool, publishes to out/, zips the artifact, creates and pushes the v0.1.0 git tag, and creates the GitHub release with the artifact attached. Run once from any directory after acceptance criteria are verified and CI is green; idempotent, safe to re-run (skips an already-existing tag or release).
- **release** `release/scripts/release.sh` (sh) -- POSIX sh equivalent of release.py: restores/builds/tests caltool, publishes to out/, zips the artifact, creates and pushes the v0.1.0 git tag, and creates the GitHub release with the artifact attached. Run once from any directory after acceptance criteria are verified and CI is green; idempotent, safe to re-run.
- **release** `release/scripts/release.ps1` (powershell) -- ASCII PowerShell equivalent of release.py: restores/builds/tests caltool, publishes to out/, zips the artifact, creates and pushes the v0.1.0 git tag, and creates the GitHub release with the artifact attached. Run once from any directory after acceptance criteria are verified and CI is green; idempotent, safe to re-run.
- **run** `release/scripts/run.py` (python) -- Runs caltool: uses the published binary in out/ if present, otherwise falls back to 'dotnet run'. Pass <year> <month> as arguments to print a specific month, or no arguments to print the current month. Use to smoke-test the build before or after publishing.
- **run** `release/scripts/run.sh` (sh) -- POSIX sh equivalent of run.py: uses the published binary in out/ if present, otherwise falls back to 'dotnet run'. Pass <year> <month> as arguments to print a specific month, or no arguments to print the current month.
- **run** `release/scripts/run.ps1` (powershell) -- ASCII PowerShell equivalent of run.py: uses the published binary in out/ if present, otherwise falls back to 'dotnet run'. Pass <year> <month> as arguments to print a specific month, or no arguments to print the current month.

## Rollback

This is the first tagged release of caltool (PREVIOUS: None) — there is no prior version to fall back to. If a defect surfaces after publishing v0.1.0:
1. Never delete or force-move the v0.1.0 tag or the GitHub release; tags, releases, and history are protected remote state and must not be removed.
2. If the GitHub release is still a draft, leave it as a draft and fix forward rather than deleting it.
3. If already published, edit the release description to add a visible 'Known issue' note describing the defect and its workaround (if any).
4. Fix the defect, then cut a new patch release (0.1.1) with a corrected artifact and tag; never overwrite or remove the 0.1.0 asset or tag.
5. Point consumers at the corrected tag/release via the new release notes and README — do not attempt to remove or hide 0.1.0.
6. caltool has no persistent state, no schema, and no external service dependency, so rollback is purely a matter of directing users to a corrected version; there is nothing to migrate back.

