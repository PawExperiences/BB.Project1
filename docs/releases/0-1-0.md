# e2e space invaders 0.1.0 (0.1.0)

## e2e space invaders v0.1.0

This is the first complete release of **e2e space invaders** — a fully playable, four-level Space Invaders clone built entirely with vanilla ES modules and the HTML5 Canvas API, requiring no build tools, no npm, and no local server. Open `index.html` from the filesystem and play.

### Highlights
- 🚀 Classic 11×5 invader formation with speed-scaling as invaders are destroyed.
- 🎮 Full keyboard control (arrow keys or WASD + Space), single-bullet mechanic, lives and hi-score HUD.
- 💥 AABB collision detection, 20-frame yellow explosion effects, and running score.
- 🛡️ Level 3 introduces four destructible cell-erosion bunkers and a mid-level formation split into independent halves.
- 👾 UFO bonus target in Level 2 with classic score tiers; invulnerability/flash respawn mechanic.
- 🏆 Multi-phase boss finale (Level 4) with health bar, phase-shift visual, sudden-death stakes, and a win screen.

## Highlights

- Four fully playable levels: classic grid → invaders shoot back → shields & split formation → multi-phase boss.
- Zero dependencies — pure ES modules, works from a file:// URL in any modern browser.
- Fixed-timestep game loop with delta-cap prevents catch-up bursts after tab backgrounding.
- Destructible bunkers with per-cell erosion from both player and invader projectiles.
- UFO bonus with classic shot-count-based score tiers (50 / 100 / 150 / 300).
- Boss level: 10-HP two-phase fight, shared collision module, sudden-death rules, and a victory/restart screen.

## Changelog

# Changelog

## [0.1.0] - Initial Release

### Added
- **Game loop and canvas framework** (`index.html`, `game.js`, `gameConfig.js`): Fixed-timestep loop at 60 updates/s with delta-cap, three-scene state machine (Title → Playing → Game Over), HUD (score, lives, hi-score), and shared `hudState` export.
- **Keyboard input and player ship** (`input.js`, `player.js`): Held-key tracker via `keydown`/`keyup` events; `Player` class with delta-time movement (ArrowLeft/KeyA, ArrowRight/KeyD), single-bullet firing (Space), canvas-boundary clamping, and procedural ship/bullet rendering.
- **Sprite rendering and collision detection** (`invaders.js`, `collision.js`, `explosion.js`): 11×5 lime-green invader formation with 1 px/frame horizontal movement and 20 px edge-drop; AABB bullet-vs-invader and stub enemy-bullet-vs-player collision pass; 20-frame yellow explosion pool; score counter (+10 per kill).
- **Level 1 – The Classic Grid** (`level1.js`): Formation step-interval scaling from ~800 ms (55 invaders) to ~100 ms (1 invader); edge-drop by `INVADER_HEIGHT` (32 px); life-loss/restart on invader reach; `transitionTo('level2')` on clearance; HUD level indicator.
- **Level 2 – They Shoot Back** (`level2.js`): 1.5× faster formation (0.67× Level 1 intervals); random invader shooting (800–2000 ms timer, lowest invader per column, 300 px/s bullets); player respawn at bottom-centre with 2 s invulnerability and flashing; UFO every 20 s alternating sides at 120 px/s with score tiers (0→100, 1→50, 2→150, 3→300 by `shotCount % 4`); static Game Over screen on last life; `transitionTo('level3')` on clearance.
- **Level 3 – Shields and Formation Split** (`level3.js`): Four destructible 4×4-cell bunkers at ~80% canvas height, cell-level erosion from any projectile; formation split into independent halves at 50% kills, halves start in opposite directions and bounce independently; Level 2 shooting behaviour retained; `this.done` flag on clearance or player death.
- **Boss Level – Multi-Phase Finale** (`boss.js`): Boss drawn with canvas primitives, 10 HP with health bar; Phase 1 (10–6 HP) baseline fire rate, Phase 2 (5–0 HP) ≥2× rate with visual transition indicator; sudden-death on boss projectile hit (reset to Level 1); win screen with final score and restart prompt; shared collision module reused for all hit-testing.
- **README.md**: Planned file layout, manual verification checklists for all levels and features.
- **CI**: BuildBoard-scaffolded `.github/workflows/build.yml` (subsequently removed/replaced in diff — see CI currency step in runbook).

## Release steps

1. [manual] Verify CI build workflow currency - Open `.github/workflows/build.yml` in the repository. The diffstat shows it was removed (86 deletions) and the BuildBoard scaffold may not have been re-applied. Confirm the file exists and its `build` job's `run:` command, working directory, and `artifact_path` match the project's actual build command (no compile step; artefact is the repository root containing `index.html`). If the file is missing or stale, trigger a BuildBoard re-scaffold of the build resource or manually restore the workflow. Do NOT proceed to tagging until CI is green.
2. [manual] Run full end-to-end manual smoke test - Open `index.html` via a `file://` URL in Chrome, Firefox, and Edge. Walk through all four levels, verify all acceptance criteria listed in manualSteps above. Record any regressions as GitHub issues before proceeding.
3. [manual] Confirm repository is clean and on the correct branch - Ensure you are on the `main` (or release) branch with no uncommitted changes: `git status` must show a clean working tree. Pull latest: `git pull origin main`.
4. [automated] Create and push the release tag v0.1.0 - Run: `git tag -a v0.1.0 -m 'Release v0.1.0 – initial four-level Space Invaders'` then `git push origin v0.1.0`. This is additive only; no existing tags are deleted.
5. [automated] Package the release artefact - Create a zip archive of the shippable files: `index.html`, `game.js`, `gameConfig.js`, `input.js`, `player.js`, `invaders.js`, `collision.js`, `explosion.js`, `level1.js`, `level2.js`, `level3.js`, `boss.js`, and `README.md`. Exclude `.github/`, `test/`, and any dotfiles. Output: `e2e-space-invaders-0.1.0.zip`. The release script below automates this step.
6. [manual] Create the GitHub Release and upload the artefact - On GitHub, navigate to the repository → Releases → 'Draft a new release'. Select tag `v0.1.0`. Set title: `e2e space invaders v0.1.0`. Paste the release notes from `releaseNotes` above into the description. Upload `e2e-space-invaders-0.1.0.zip` as the release asset. Publish the release. (Alternatively, use the GitHub CLI: `gh release create v0.1.0 e2e-space-invaders-0.1.0.zip --title 'e2e space invaders v0.1.0' --notes-file RELEASE_NOTES.md`.)
7. [manual] Post-release verification - Download `e2e-space-invaders-0.1.0.zip` from the GitHub Release page, unzip to a fresh directory, open `index.html` in a browser via `file://`, and confirm the game launches and plays correctly. Verify the release is listed publicly on the repository's Releases page.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Packages the shippable game files into e2e-space-invaders-0.1.0.zip, creates annotated git tag v0.1.0, and pushes the tag to origin. Run once from the repository root after all manual checks pass and the working tree is clean.
- **release** `release/scripts/release.sh` (sh) -- Packages the shippable game files into e2e-space-invaders-0.1.0.zip, creates annotated git tag v0.1.0, and pushes the tag to origin. Run once from the repository root after all manual checks pass and the working tree is clean.
- **release** `release/scripts/release.ps1` (powershell) -- Packages the shippable game files into e2e-space-invaders-0.1.0.zip, creates annotated git tag v0.1.0, and pushes the tag to origin. Run once from the repository root after all manual checks pass and the working tree is clean.
- **run** `release/scripts/run.py` (python) -- Starts a minimal local HTTP server (Python's http.server) in the repository root on port 8080 and prints the URL to open in a browser. Run this when you need to serve the game over http:// (e.g. for DevTools profiling) instead of opening index.html directly via file://.
- **run** `release/scripts/run.sh` (sh) -- Starts a minimal local HTTP server (Python's http.server) in the repository root on port 8080 and prints the URL to open in a browser. Run this when you need to serve the game over http:// (e.g. for DevTools profiling) instead of opening index.html directly via file://.
- **run** `release/scripts/run.ps1` (powershell) -- Starts a minimal local HTTP server (Python's http.server) in the repository root on port 8080 and prints the URL to open in a browser. Run this when you need to serve the game over http:// (e.g. for DevTools profiling) instead of opening index.html directly via file://.

## Rollback

1. This is the initial release (v0.1.0); there is no previous production tag to roll back to.
2. If the release tag or GitHub Release was created in error, do NOT delete the tag or release (protected remote state). Instead, open a GitHub issue flagging the problem and pin a note to the release body marking it as retracted.
3. To stop serving the release to users: unpublish or mark the GitHub Release as a pre-release (draft) via the GitHub UI — this is an in-place edit, not a deletion.
4. Because the project is a static client-side app with no server component, no database, and no migrations, there is no infrastructure to roll back beyond the release artefact itself.

