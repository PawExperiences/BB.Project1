# e2e space invaders 0.1.0 (0.1.0)

## e2e Space Invaders v0.1.0

This is the first release of **e2e Space Invaders**, a browser-native Space Invaders clone built entirely with vanilla ES modules and the Canvas 2D API — no bundler, no npm packages, no server required. Open `index.html` directly in Chrome or Firefox and play.

### Highlights

- **Full game loop** — fixed-timestep 60 Hz update loop with a 250 ms delta cap so backgrounded tabs never cause a burst of updates.
- **Three-scene state machine** — Title, Playing, and Game Over; navigate with the ENTER key only.
- **Playable through three levels** — Level 1 classic grid, Level 2 enemy fire + bonus UFO, Level 3 destructible shields and a formation split at 50% kills.
- **Keyboard-driven player ship** — smooth delta-time movement, boundary clamping, and a single-in-flight bullet rule.
- **AABB collision detection** — dedicated collision pass every frame (before render) covering player bullet vs. invaders and invader projectiles vs. player.
- **Canvas HUD** — score, hi-score, lives, and current level drawn directly on the canvas; no DOM overlays.

## Highlights

- First playable release: full Space Invaders loop across Title, Playing, and Game Over scenes.
- Three levels of escalating difficulty: classic grid (L1), enemy fire and bonus UFO (L2), destructible shields and split formation (L3).
- Fixed-timestep 60 Hz game loop with delta-cap — works correctly after tab focus is restored.
- Zero dependencies, zero build step — open index.html from a file:// URL and play immediately.
- Cell-by-cell destructible shield bunkers with bullet consumption and persistent erosion.
- UFO bonus scoring determined by deterministic shot-count modulo mapping (50/100/150/300 pts).

## Changelog

# Changelog

## [0.1.0] - Initial Release

### Added
- `index.html`: Entry point hosting a 768×896 px `<canvas>` element on a dark (`#000`) background, loading `game.js` as an ES module.
- `gameConfig.js`: Named exports for all shared constants (`CANVAS_WIDTH=768`, `CANVAS_HEIGHT=896`, `PLAYER_SPEED=200`, `BULLET_SPEED=500`, `STARTING_LIVES=3`).
- `game.js`: Fixed-timestep game loop (60 steps/s, 250 ms delta cap), three-scene state machine (Title → Playing → Game Over), canvas HUD renderer, and exported `hudState` object.
- `input.js`: `initInput()` and `isKeyHeld(key)` using a held-key map (no key-repeat dependency).
- `player.js`: `Player` class with delta-time movement, boundary clamping, single-in-flight bullet, procedural canvas rendering, and `lives` property seeded from `gameConfig.js`.
- `invaders.js`: 11×5 invader grid (55 invaders, 24×16 px cells, 32×24 px stride), horizontal march with edge-drop (16 px) and direction reversal, AABB collision detection for player bullet vs. invaders and invader bullets vs. player, explosion flash effect (~300 ms), score increment of 10 pts per kill.
- `collision.js`: Dedicated AABB collision pass integrated into the `update` cycle before `render`.
- Level 1: Formation speed scaling linearly from ~800 ms/step (55 invaders) to ~100 ms/step (1 invader); lose condition (invader reaches player ship top → lose 1 life, level restart); win condition (all 55 killed → advance to Level 2); HUD displays current level number.
- Level 2: Faster invader movement (measurable multiplier increase over Level 1); invader downward projectiles from bottom-most alive invader per column at random intervals; bonus UFO crossing the top of the screen with points awarded by `playerShotCount % 4` → `[50, 100, 150, 300]`; player respawn with ≥1.5 s invulnerability window (flashing indicator) after a hit; game-over flow triggered on last life lost.
- Level 3: Four destructible shield bunkers (4×4 grid of ~8×8 px cells, green, placed at ~80% canvas height); cell-by-cell erosion by player or invader bullets; formation split at 50% kills — remaining invaders divide into left and right halves sweeping independently in opposite directions; transition to Boss level on full clear.
- `README.md`: File-layout map and manual verification steps for all features through Level 3.

## Release steps

1. [manual] Verify all six ES module source files exist at repository root - Confirm the following files are present and non-empty at the repo root: `index.html`, `game.js`, `gameConfig.js`, `input.js`, `player.js`, `invaders.js`, `collision.js`, `README.md`. Run: `git ls-files index.html game.js gameConfig.js input.js player.js invaders.js collision.js README.md` and verify all eight names appear.
2. [manual] Confirm CI build workflow (.github/workflows/build.yml) matches current toolchain - This project has no build step (pure ES modules, Node container for lint/syntax checks only). Open `.github/workflows/build.yml` and verify: (a) the build command matches the project's configured `command` in the build resource (e.g. a syntax-check or no-op pass), (b) the `artifact_path` points to the correct root-level files or is set to `.` for the whole repo, (c) no stale references to a bundler (webpack, vite, rollup, parcel) that was never used. If any drift is found, update the build resource config so BuildBoard re-scaffolds the workflow before tagging.
3. [automated] Run syntax / lint check locally - From the repo root, run the configured build command (Node container, no build step). Example: `node --input-type=module --eval "import('./game.js')"` for a quick ES module parse check, or the project's configured CI command. All six JS modules must parse without errors. Exit code must be 0.
4. [manual] Execute manual smoke-test checklist in browser - Open `index.html` via a `file://` URL in Chrome and Firefox. Step through the manual verification checklist: Title scene text, ENTER transitions, HUD (score/lives/level), Level 1 grid and speed ramp, Level 2 enemy fire and UFO, Level 3 shields and split, Game Over scene, tab-backgrounding delta-cap. Document pass/fail against each acceptance criterion. All criteria must pass before proceeding.
5. [manual] Update CHANGELOG.md and README.md for v0.1.0 - Append the v0.1.0 changelog entry to `CHANGELOG.md` (create the file if absent). Verify `README.md` contains the file-layout table, keyboard controls, and manual verification steps for Levels 1–3. Commit with message: `docs: release notes and changelog for v0.1.0`.
6. [automated] Create and push the v0.1.0 annotated git tag - Run:
```sh
git tag -a v0.1.0 -m "Release v0.1.0 — e2e Space Invaders initial release"
git push origin v0.1.0
```
Verify the tag appears on GitHub under Releases > Tags before proceeding.
7. [automated] Create the GitHub Release and upload artifact - Using the GitHub CLI (or the release/scripts/release.sh helper script):
```sh
gh release create v0.1.0 \
  --title "e2e Space Invaders v0.1.0" \
  --notes-file CHANGELOG.md \
  spaceinvaders-0.1.0.zip
```
The `spaceinvaders-0.1.0.zip` artifact is produced by the release script (zips all source files). Confirm the release is visible at https://github.com/PawExperiences/BB.Project1/releases/tag/v0.1.0.
8. [manual] Verify CI build passes on the tagged commit - Navigate to https://github.com/PawExperiences/BB.Project1/actions and confirm the workflow run triggered by the `v0.1.0` tag completes with a green check. If it fails, do NOT proceed; investigate and fix the build resource config, then re-run.
9. [manual] Post-release smoke test from the published artifact - Download `spaceinvaders-0.1.0.zip` from the GitHub release, unzip it, and open `index.html` via `file://` URL in a fresh browser profile. Confirm the game loads and plays through at least Level 1 with no console errors. This validates the artifact is self-contained.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Packages all game source files into spaceinvaders-{VERSION}.zip, creates an annotated git tag v{VERSION}, pushes the tag to origin, and creates a GitHub Release with the zip attached via the GitHub CLI. Run once per release after all manual steps are complete.
- **release** `release/scripts/release.sh` (sh) -- Packages all game source files into spaceinvaders-{VERSION}.zip, creates an annotated git tag v{VERSION}, pushes the tag to origin, and creates a GitHub Release with the zip attached via the GitHub CLI. Run once per release after all manual steps are complete.
- **release** `release/scripts/release.ps1` (powershell) -- Packages all game source files into spaceinvaders-{VERSION}.zip, creates an annotated git tag v{VERSION}, pushes the tag to origin, and creates a GitHub Release with the zip attached via the GitHub CLI. Run once per release after all manual steps are complete.
- **run** `release/scripts/run.py` (python) -- Starts a local HTTP server (Python http.server) on port 8080 serving the repository root, then opens index.html in the default browser. Run any time you want to serve the game over HTTP instead of via a file:// URL (useful for browsers that restrict ES module imports on file://).
- **run** `release/scripts/run.sh` (sh) -- Starts a local HTTP server (Python http.server) on port 8080 serving the repository root, then prints the URL. Run any time you want to serve the game over HTTP instead of via a file:// URL.
- **run** `release/scripts/run.ps1` (powershell) -- Starts a local HTTP server (Python http.server) on the specified port serving the repository root, then opens index.html in the default browser. Run any time you want to serve the game over HTTP instead of via a file:// URL.

## Rollback

Because this is the initial release (no previous tag), a rollback means reverting to the empty repository state.

1. Identify the last known-good commit SHA before the release tag `v0.1.0` was created (the `e97155e chore: initialize repo` commit).
2. In the GitHub UI, navigate to the repository and open the commit history to verify `e97155e` is intact — do NOT delete the `v0.1.0` tag or any commits.
3. To serve the pre-release state locally, check out `e97155e` in a local clone: `git checkout e97155e` (detached HEAD, safe for inspection).
4. Communicate to users that v0.1.0 has been retracted and no playable build is available; point them to the GitHub release page where the retraction note should be added (edit the release description — do not delete the release).
5. Do NOT force-push, rewrite history, or delete any remote branches, tags, or commits.

