# e2e space invaders 0.1.0 (0.1.0)

## e2e space invaders 0.1.0

This is the **first playable release** of *e2e Space Invaders* — a four-level, browser-native Space Invaders clone that runs directly from the filesystem (`file://`) with zero dependencies, no bundler, and no server.

Starting from an empty repository, this release delivers the complete game: a fixed-timestep canvas game loop, a keyboard-driven player ship, the classic 11×5 invader grid with AABB collision, and four escalating levels — the passive classic grid (Level 1), enemy fire with a bonus UFO (Level 2), destructible shield bunkers and a mid-wave formation split (Level 3), and a two-phase boss finale (Level 4). All game state (score, lives, session shot count) flows forward across levels. Open `index.html` in any modern browser and press Enter to play.

### Highlights
- Zero-dependency, `file://`-compatible ES-module architecture across 11 source files
- Fixed-timestep game loop with 250 ms delta-cap, separate update/render phases
- Four complete levels with increasing difficulty: passive grid → enemy fire + UFO → shields + split formation → two-phase boss
- AABB collision system shared across all levels; single source of truth for score/lives via `hudState`
- Destructible shield bunkers with per-cell erosion (Level 3)
- Multi-phase boss with spread fire pattern, proportional health bar, and sudden-death penalty (Level 4)

## Highlights

- First playable release: complete four-level Space Invaders clone, no server or bundler required
- Fixed-timestep game loop (60 Hz, 250 ms cap) with clean update/render phase separation
- Levels 1–3 escalate difficulty: passive grid → enemy fire & UFO → destructible shields & split formation
- Two-phase boss (Level 4) with spread fire, proportional health bar, and sudden-death mechanic
- Shared AABB collision module and single `hudState` object keep cross-level state consistent
- Fully `file://`-compatible ES modules — open `index.html` and play instantly

## Changelog

## [0.1.0] - Initial Release

### Added
- **CI scaffold** (`index.html`, `main.js`, `style.css`, `README.md`): minimum source files to satisfy the CI pipeline and establish the file-naming contract for all sibling tasks.
- **Game loop & canvas framework** (`game.js`, `gameConfig.js`, `index.html`): fixed-timestep loop at 60 Hz, delta-cap at 250 ms, Title/Playing/Game-Over scene state machine, canvas HUD, exported `hudState` object.
- **Shared constants** (`gameConfig.js`): `CANVAS_WIDTH=768`, `CANVAS_HEIGHT=896`, `PLAYER_SPEED=200`, `BULLET_SPEED=500`, `STARTING_LIVES=3`.
- **Keyboard input** (`input.js`): `initInput()` / `isKeyHeld(key)` with held-state tracking; no key-repeat leakage.
- **Player ship** (`player.js`): procedurally-drawn ship (40×32 px), frame-rate-independent movement & clamping, single-bullet constraint, `lives` property, `loseLife()` method.
- **Invader grid & score** (`invaders.js`): 11×5 = 55-invader formation, lateral sweep with edge-reversal and downward drop, mutable `score` export, 20-frame explosion flash effect.
- **Collision detection** (`collision.js`): AABB `runCollisions(invaders, bullets, player)`, bullet-vs-invader pass, invader-bullet-vs-player stub with `TODO` comment.
- **Level 1** (`level1.js`): classic grid, speed scaling 800 ms → 100 ms linear with alive-count, 48 px drop on wall contact, LEVEL COMPLETE screen, lose-life-on-reach-player-boundary with full reset, LEVEL 1 HUD label.
- **Level 2** (`level2.js`): 1.5× faster grid (×0.67 intervals), invader shooting (single bullet, random 800–2000 ms interval, lowest-per-column selection, 300 px/s downward), UFO every 20 s alternating sides at 120 px/s, score tiers by `sessionShotCount % 4`, 2-second respawn invulnerability with flash, lives carry-over from Level 1, Game Over handoff.
- **Level 3** (`level3.js`): same grid, four destructible shield bunkers (4×4 cells, 8×8 px, #00FF00) at 80 % canvas height, mid-wave formation split at ≥28 kills into independent Left (cols 1–6) and Right (cols 7–11) halves sweeping in opposite directions, cell-level bunker erosion via `rectIntersects`, `isDone()` lifecycle method.
- **Boss level** (`boss.js`): Level 4 auto-dispatched from `game.js`, 160×80 px canvas-primitive boss at Y=40 drifting at 90 px/s, 10 HP, green proportional health bar at canvas top, three-bullet spread (±20°, 260 px/s), Phase 1 ≥6 HP every 1500 ms / Phase 2 ≤5 HP every 700 ms, sudden-death player hit → Level 1 reset, win screen with score and restart prompt.

## Release steps

1. [manual] Confirm CI build is green on the release branch - Navigate to https://github.com/PawExperiences/BB.Project1/actions and verify the latest workflow run on the release commit (f8ddcac) is passing. Do not proceed if any check is red.
2. [manual] Verify CI workflow currency — build.yml matches current toolchain - Open .github/workflows/build.yml in the repository. Confirm the build command and artifact_path match the project's configured build resource (node --check per-file syntax validation, artifact path is the repository root static files). The diffstat shows build.yml had 86 lines removed in this release cycle — confirm the re-scaffolded workflow correctly reflects the current toolchain. If any drift is detected, update the BuildBoard build resource config so the workflow re-scaffolds correctly before tagging.
3. [automated] Run automated syntax check on all JS source files - Execute: node --check game.js gameConfig.js input.js player.js invaders.js collision.js level1.js level2.js level3.js boss.js main.js
All files must exit 0 with no output. Any syntax error must be fixed before tagging.
4. [manual] Manual smoke-test: open index.html via file:// in Chrome and Firefox - Open index.html directly (no server). Verify: no console errors, canvas appears, Title screen renders correctly, ENTER transitions to Playing, HUD shows score=0 lives=3 LEVEL 1. Play through at least the first 10 invader kills to confirm the core loop.
5. [manual] Manual acceptance check: Level 1 win condition and Level 2 auto-transition - Shoot all 55 invaders in Level 1. Confirm 'LEVEL COMPLETE' appears, then Level 2 starts automatically with lives unchanged. Verify faster invader movement in Level 2.
6. [manual] Manual acceptance check: Level 2 invader fire, UFO, and invulnerability - In Level 2: (a) allow an invader bullet to hit the player — confirm life decrements and 2-second flash invulnerability; (b) wait ~20 s for UFO spawn — confirm alternating entry side and score tier; (c) confirm at most one invader bullet on screen at a time.
7. [manual] Manual acceptance check: Level 3 shields and formation split - In Level 3: (a) confirm four green bunker structures at ~80 % canvas height; (b) shoot individual bunker cells and verify cell-level erosion; (c) destroy 28+ invaders and confirm formation splits into two independent halves moving in opposite directions.
8. [manual] Manual acceptance check: Level 4 boss fight and win screen - In Level 4: (a) confirm boss appears at Y=40 drifting at 90 px/s; (b) confirm green health bar shrinks on hit; (c) confirm fire pattern changes at 5 HP (700 ms interval); (d) allow boss bullet to hit player — confirm Level 1 reset with score=0; (e) defeat boss and confirm win screen with 'YOU WIN!', final score, and restart prompt.
9. [manual] Create and push the release tag v0.1.0 - On the verified release commit (f8ddcac), run:
  git tag -a v0.1.0 -m 'Release 0.1.0: e2e space invaders initial playable release'
  git push origin v0.1.0
Do NOT force-push or delete any existing tags.
10. [manual] Publish GitHub Release - Navigate to https://github.com/PawExperiences/BB.Project1/releases/new, select tag v0.1.0, set title 'e2e space invaders 0.1.0', paste the releaseNotes markdown into the release body, and click 'Publish release'. Upload any build artifact (e.g. a zip of the source files) if the CI pipeline produces one.
11. [automated] Upload build artifact to GitHub Release (if CI produces one) - Run release/scripts/release.sh (or .py / .ps1) — see scripts section. The script tags, creates the GitHub Release via the API, zips the static source files, and uploads the zip as a release asset. Requires GITHUB_TOKEN env var with repo write scope.

## Helper scripts (committed under release/scripts/)

- **release** `release/scripts/release.py` (python) -- Creates a GitHub Release for v0.1.0 and uploads a zip of the static game files as a release asset. Run once after the release tag has been pushed, with GITHUB_TOKEN set in the environment.
- **release** `release/scripts/release.sh` (sh) -- Creates a GitHub Release for v0.1.0 and uploads a zip of the static game files as a release asset. Run once after the release tag has been pushed, with GITHUB_TOKEN set in the environment.
- **release** `release/scripts/release.ps1` (powershell) -- Creates a GitHub Release for v0.1.0 and uploads a zip of the static game files as a release asset. Run once after the release tag has been pushed, with GITHUB_TOKEN set in the environment.
- **run** `release/scripts/run.py` (python) -- Serves the game from the repository root on http://localhost:8080 using Python's built-in HTTP server, then opens index.html in the default browser. Run anytime to play or manually verify the release.
- **run** `release/scripts/run.sh` (sh) -- Serves the game from the repository root on http://localhost:8080 using Python's built-in HTTP server and opens index.html in the default browser. Run anytime to play or manually verify the release.
- **run** `release/scripts/run.ps1` (powershell) -- Serves the game from the repository root on http://localhost:8080 using Python's built-in HTTP server and opens index.html in the default browser. Run anytime to play or manually verify the release.

## Rollback

1. Identify the previous Git tag. For this first release there is no prior tag; rollback means reverting to the empty-repository state (commit `8266d81` or the last `chore: reset repo` commit before CI scaffolding).
2. To roll back the deployed GitHub Release: navigate to the release page on GitHub, click 'Edit release', and change the status to 'Draft' or delete the release body — do NOT delete the tag or any commits (protected remote state).
3. If the live URL or CDN is serving the built artifact, re-point it to the previous artifact (none exists for v0.1.0 — inform users the service is temporarily unavailable).
4. Communicate to users via the project's issue tracker that the release has been retracted and state the ETA for a fix.
5. Once the root cause is fixed, re-run the release pipeline to publish a corrected `0.1.1` patch rather than re-using the `0.1.0` tag.

